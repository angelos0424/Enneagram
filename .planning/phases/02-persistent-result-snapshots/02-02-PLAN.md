---
phase: 02-persistent-result-snapshots
plan: 02
type: execute
wave: 2
depends_on:
  - "02-01"
files_modified:
  - src/app/api/assessments/score/route.ts
  - src/app/results/[publicId]/page.tsx
  - src/app/results/[publicId]/result-snapshot-view.tsx
  - src/app/results/[publicId]/snapshot-metadata.ts
  - next.config.ts
  - test/assessment/score-route.test.ts
  - test/assessment/public-result.test.ts
autonomous: true
requirements:
  - RSLT-07
  - SHAR-03
  - SHAR-06
must_haves:
  truths:
    - "Submitting a completed assessment persists the immutable snapshot immediately and returns the permanent public link in the same server flow, per D-03 and D-04."
    - "Opening a public result URL shows the stored snapshot without rescoring, so later scoring/content changes do not drift existing links, per RSLT-07 and D-05/D-06."
    - "Public result pages default to privacy protection with noindex and strict referrer handling, per D-07 and D-08."
  artifacts:
    - path: "src/app/api/assessments/score/route.ts"
      provides: "Submit-time scoring plus snapshot persistence and public-link response payload"
    - path: "src/app/results/[publicId]/page.tsx"
      provides: "Public snapshot retrieval route using only stored data"
    - path: "src/app/results/[publicId]/result-snapshot-view.tsx"
      provides: "Immutable snapshot renderer for the full detailed result payload"
    - path: "src/app/results/[publicId]/snapshot-metadata.ts"
      provides: "Page metadata contract with noindex defaults"
    - path: "next.config.ts"
      provides: "Referrer-policy headers scoped to public result pages"
    - path: "test/assessment/public-result.test.ts"
      provides: "Regression coverage for immutable public reads and privacy defaults"
  key_links:
    - from: "src/app/api/assessments/score/route.ts"
      to: "src/db/repositories/assessment-result-repository.ts"
      via: "POST persists the freshly scored snapshot before responding"
      pattern: "save\\("
    - from: "src/app/results/[publicId]/page.tsx"
      to: "src/db/repositories/assessment-result-repository.ts"
      via: "Page loads snapshot by public id instead of rescoring"
      pattern: "findByPublicId"
    - from: "src/app/results/[publicId]/page.tsx"
      to: "src/app/results/[publicId]/snapshot-metadata.ts"
      via: "Page metadata applies noindex and page-level privacy defaults"
      pattern: "generateMetadata|robots"
    - from: "next.config.ts"
      to: "src/app/results/[publicId]/page.tsx"
      via: "Route-level referrer protection applies to public snapshot pages"
      pattern: "Referrer-Policy"
---

<objective>
Wire submit-time persistence and public snapshot retrieval so completed assessments immediately receive permanent links and public result pages render only the stored snapshot with privacy-first defaults.

Purpose: Satisfy RSLT-07, SHAR-03, and SHAR-06 while keeping Phase 2 narrowly focused on persistence, public retrieval, and privacy defaults rather than later share-loop UX or recommendation features.
Output: A persisted score-route response, a public snapshot page and renderer, metadata/privacy helpers, and regression tests proving the page reads immutable stored data rather than recalculating.
</objective>

<execution_context>
@/home/ubuntu/Project/Enneagram/.codex/get-shit-done/workflows/execute-plan.md
@/home/ubuntu/Project/Enneagram/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-persistent-result-snapshots/02-CONTEXT.md
@.planning/phases/01-assessment-contract-scoring-core/01-03-SUMMARY.md
@.planning/phases/01-assessment-contract-scoring-core/01-04-SUMMARY.md
@src/app/api/assessments/score/route.ts
@src/domain/assessment/result-snapshot.ts
@src/db/repositories/assessment-result-repository.ts
@src/app/layout.tsx
@next.config.ts

<interfaces>
From `src/app/api/assessments/score/route.ts`:
```typescript
export async function POST(request: Request) {
  const payload = await request.json();
  const submission = assessmentSubmissionSchema.parse(payload);
  const result = scoreAssessment(submission);
  return Response.json({ result }, { status: 200 });
}
```

From `src/db/repositories/assessment-result-repository.ts` after plan 01:
```typescript
export interface AssessmentResultRepository {
  save(snapshot: AssessmentResultSnapshotDraft): Promise<AssessmentResultRecord>;
  findByPublicId(publicId: string): Promise<AssessmentResultRecord | null>;
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Persist immutable snapshots in the scoring route and return the public link</name>
  <files>src/app/api/assessments/score/route.ts, test/assessment/score-route.test.ts</files>
  <behavior>
    - Test 1: A valid submission still returns the canonical scored result and now also returns the generated public result identifier or URL.
    - Test 2: The score route persists the snapshot immediately after scoring, not on later page entry or share click, per D-03 and D-04.
    - Test 3: Existing machine-readable 400 error responses remain unchanged for invalid payloads.
  </behavior>
  <action>Update `src/app/api/assessments/score/route.ts` to orchestrate Phase 1 scoring plus Phase 2 snapshot persistence in one server-authoritative flow. Build the immutable snapshot from the scored result and answers, persist it through the repository from Plan 01, and include the public share path in the successful JSON response without changing the existing error-code contract. Keep write-side logic inside the server route; do not push persistence to the client. Expand `test/assessment/score-route.test.ts` first to lock the new success payload and save-side behavior before modifying the route.</action>
  <verify>
    <automated>npm exec vitest run test/assessment/score-route.test.ts</automated>
  </verify>
  <done>Submitting an assessment now creates the stored snapshot and returns the permanent public link immediately, while invalid submissions still fail exactly as before.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add the public snapshot page that renders only stored data</name>
  <files>src/app/results/[publicId]/page.tsx, src/app/results/[publicId]/result-snapshot-view.tsx, test/assessment/public-result.test.ts</files>
  <behavior>
    - Test 1: Public page loading calls the repository by `publicId` and does not invoke `scoreAssessment`.
    - Test 2: The rendered view includes the full Phase 2-required detailed result fields: primary type, wing, normalized distribution, growth/stress directions, and description-card content per D-05 and D-06.
    - Test 3: Missing or unknown public ids resolve to the route's not-found behavior instead of exposing raw database failures.
  </behavior>
  <action>Create the public route at `src/app/results/[publicId]/page.tsx` and keep it immutable-snapshot-first: load the stored record via the repository method from Plan 01, map the stored fields into a renderable shape, and pass them to `result-snapshot-view.tsx`. The renderer should stay Phase-2-scoped: it must display the full saved result payload, but it should avoid later-phase share CTA loops, recommendation sections, or polished interpretation UX beyond what D-05 and D-06 require. Write `test/assessment/public-result.test.ts` first to lock repository-based loading and the explicit absence of rescoring.</action>
  <verify>
    <automated>npm exec vitest run test/assessment/public-result.test.ts && npm run typecheck</automated>
  </verify>
  <done>There is a public result page reachable by opaque id that renders the stored snapshot only, keeping old links stable even if future scoring or content modules change.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Apply privacy defaults to public snapshot pages</name>
  <files>src/app/results/[publicId]/snapshot-metadata.ts, src/app/results/[publicId]/page.tsx, next.config.ts, test/assessment/public-result.test.ts</files>
  <behavior>
    - Test 1: Public result metadata declares `noindex` defaults for search engines per D-07.
    - Test 2: Public result routes apply strict referrer protection per D-08.
    - Test 3: Privacy defaults are scoped to the public snapshot route and do not require Phase 4 share-preview work.
  </behavior>
  <action>Extract page metadata rules into `src/app/results/[publicId]/snapshot-metadata.ts` so tests can assert robots/noindex behavior directly. Wire `page.tsx` to use those metadata helpers. Update `next.config.ts` to apply a strict `Referrer-Policy` header to `/results/:publicId*` public pages; if you also add an `X-Robots-Tag` header, keep it consistent with page metadata, but do not expand into OG/share-preview work reserved for Phase 6. Extend `test/assessment/public-result.test.ts` to lock both `noindex` and referrer-policy behavior.</action>
  <verify>
    <automated>npm exec vitest run test/assessment/public-result.test.ts test/assessment/score-route.test.ts && npm run typecheck && npm run build</automated>
  </verify>
  <done>Public result pages ship with privacy-by-default search and referrer protections while remaining narrowly focused on immutable snapshot retrieval.</done>
</task>

</tasks>

<verification>
- Run `npm exec vitest run test/assessment/score-route.test.ts test/assessment/public-result.test.ts`
- Run `npm run typecheck`
- Run `npm run build`
- Confirm the public result page loads via `/results/{publicId}` and references stored snapshot fields, not `scoreAssessment`
</verification>

<success_criteria>
- The scoring route persists snapshots and returns a permanent public link in the same request cycle
- `/results/{publicId}` renders the stored snapshot without recalculating the result
- Public result pages default to `noindex` and strict referrer handling
- No Phase 4 share-loop UX, recommendation modules, or preview-card features are introduced
</success_criteria>

<output>
After completion, create `.planning/phases/02-persistent-result-snapshots/02-02-SUMMARY.md`
</output>
