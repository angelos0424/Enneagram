---
phase: 02-persistent-result-snapshots
plan: 01
type: execute
wave: 1
depends_on:
  - "01-04"
files_modified:
  - src/domain/assessment/result-snapshot.ts
  - src/domain/assessment/result-link.ts
  - src/db/schema.ts
  - src/db/repositories/assessment-result-repository.ts
  - test/assessment/result-persistence.test.ts
autonomous: true
requirements:
  - SHAR-01
  - OPER-02
must_haves:
  truths:
    - "Each completed assessment result has a non-guessable public identifier at persistence time, not later during sharing, per D-03 and D-04."
    - "Stored result snapshots keep the Phase 1 version trio and immutable scored payload while adding Phase 2 public/admin token boundaries, per D-01, D-02, and D-09."
    - "The repository can load snapshots by public identifier without exposing database UUIDs to later public routes."
  artifacts:
    - path: "src/domain/assessment/result-link.ts"
      provides: "Public/admin token generation and persistence-level link contract"
    - path: "src/domain/assessment/result-snapshot.ts"
      provides: "Snapshot draft shape expanded for persistent public retrieval while preserving version trio"
    - path: "src/db/schema.ts"
      provides: "Assessment results table with public/admin token columns for PostgreSQL persistence"
    - path: "src/db/repositories/assessment-result-repository.ts"
      provides: "Repository save/find methods for public snapshot lookup"
    - path: "test/assessment/result-persistence.test.ts"
      provides: "Regression coverage for tokenized immutable snapshot persistence"
  key_links:
    - from: "src/domain/assessment/result-link.ts"
      to: "src/domain/assessment/result-snapshot.ts"
      via: "Generated identifiers attached when building the persistence-ready snapshot"
      pattern: "publicId|adminToken"
    - from: "src/domain/assessment/result-snapshot.ts"
      to: "src/db/schema.ts"
      via: "Snapshot field names align with persisted token/version columns"
      pattern: "assessmentVersion|scoringVersion|copyVersion|publicId|adminToken"
    - from: "src/db/repositories/assessment-result-repository.ts"
      to: "src/db/schema.ts"
      via: "Repository persists and queries the public snapshot contract"
      pattern: "findByPublicId|assessmentResults"
---

<objective>
Extend the Phase 1 snapshot contract into a tokenized PostgreSQL persistence boundary that issues permanent non-guessable public links at save time, while preserving immutable snapshot semantics and downstream compatibility.

Purpose: Satisfy SHAR-01 and OPER-02 at the data-contract level before route and page work depend on it, and keep public routes on opaque identifiers instead of internal UUIDs per D-01, D-02, D-03, D-04, and D-09.
Output: A token/link contract, expanded snapshot persistence shape, updated schema/repository methods, and regression tests proving tokens are generated and stored without disturbing the version trio.
</objective>

<execution_context>
@/home/ubuntu/Project/Enneagram/.codex/get-shit-done/workflows/execute-plan.md
@/home/ubuntu/Project/Enneagram/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-persistent-result-snapshots/02-CONTEXT.md
@.planning/phases/01-assessment-contract-scoring-core/01-04-SUMMARY.md
@src/domain/assessment/result-snapshot.ts
@src/db/schema.ts
@src/db/repositories/assessment-result-repository.ts

<interfaces>
From `src/domain/assessment/result-snapshot.ts`:
```typescript
export type AssessmentResultSnapshotDraft = {
  assessmentVersion: string;
  scoringVersion: string;
  copyVersion: string;
  primaryType: EnneagramType;
  wingType: EnneagramType;
  growthType: EnneagramType;
  stressType: EnneagramType;
  rawScores: AssessmentScoreResult["rawScores"];
  normalizedScores: AssessmentScoreResult["normalizedScores"];
  nearbyTypes: NearbyTypeScore[];
  answers: AssessmentSubmission["answers"];
  createdAt: Date;
};
```

From `src/db/repositories/assessment-result-repository.ts`:
```typescript
export interface AssessmentResultRepository {
  save(snapshot: AssessmentResultSnapshotDraft): Promise<AssessmentResultRecord>;
  findById(id: string): Promise<AssessmentResultRecord | null>;
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define the opaque share-token snapshot contract</name>
  <files>src/domain/assessment/result-snapshot.ts, src/domain/assessment/result-link.ts, test/assessment/result-persistence.test.ts</files>
  <behavior>
    - Test 1: Snapshot persistence input includes the existing version trio plus a generated opaque `publicId` per D-01 through D-04.
    - Test 2: Snapshot persistence input also carries an admin-only management token per D-09 without leaking readable slugs or type labels.
    - Test 3: Token generation is deterministic in structure, not value: minimum length and allowed alphabet are asserted, but not exact token bytes.
  </behavior>
  <action>Create `src/domain/assessment/result-link.ts` to own token generation and any small helper types needed for public/admin identifiers. Use an opaque random token strategy, not database ids, type names, or readable slugs, per D-01 and D-02. Update `src/domain/assessment/result-snapshot.ts` so the persistence draft can carry `publicId` and `adminToken` alongside the Phase 1 version trio and immutable scored payload without renaming existing fields. Keep the draft backward-compatible for later phases by separating public-share identifiers from display data. Start by writing the failing regression cases in `test/assessment/result-persistence.test.ts`, then implement the minimal production code to satisfy them.</action>
  <verify>
    <automated>npm exec vitest run test/assessment/result-persistence.test.ts</automated>
  </verify>
  <done>The codebase has a stable domain contract for non-guessable public/admin identifiers attached to immutable result snapshots, with tests proving Phase 1 fields remain intact.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend PostgreSQL schema and repository lookup methods for public retrieval</name>
  <files>src/db/schema.ts, src/db/repositories/assessment-result-repository.ts, test/assessment/result-persistence.test.ts</files>
  <behavior>
    - Test 1: The `assessment_results` table exposes `publicId` and `adminToken` columns while keeping `assessmentVersion`, `scoringVersion`, and `copyVersion` unchanged.
    - Test 2: The repository can persist a tokenized snapshot and retrieve it by `publicId`.
    - Test 3: The public lookup path does not rely on internal UUIDs.
  </behavior>
  <action>Modify `src/db/schema.ts` so PostgreSQL stores the new opaque identifiers with uniqueness guarantees appropriate for permanent public links and management tokens, satisfying OPER-02 and D-09. Update `src/db/repositories/assessment-result-repository.ts` to add public-read methods that later routes/pages can call directly, such as `findByPublicId`, while preserving `save(snapshot)` and keeping downstream compatibility for future management actions. The repository should continue to return the immutable stored snapshot rather than reconstructing data from scorer logic. Keep the tests in `test/assessment/result-persistence.test.ts` as the contract lock for the schema column names and repository boundary.</action>
  <verify>
    <automated>npm exec vitest run test/assessment/result-persistence.test.ts && npm run typecheck</automated>
  </verify>
  <done>The database and repository layers can store immutable snapshots with opaque identifiers and later read them by public id, without exposing implementation-specific UUIDs to callers.</done>
</task>

</tasks>

<verification>
- Run `npm exec vitest run test/assessment/result-persistence.test.ts`
- Run `npm run typecheck`
- Confirm `src/db/repositories/assessment-result-repository.ts` contains `findByPublicId`
- Confirm no public path uses `id` as the share identifier
</verification>

<success_criteria>
- Every persisted snapshot has opaque public/admin identifiers generated at save time, not at first share interaction
- PostgreSQL remains the canonical persistence layer for permanent result storage
- The version trio and immutable scored payload remain unchanged while public retrieval support is added
</success_criteria>

<output>
After completion, create `.planning/phases/02-persistent-result-snapshots/02-01-SUMMARY.md`
</output>
