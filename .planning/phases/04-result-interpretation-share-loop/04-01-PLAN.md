---
phase: 04-result-interpretation-share-loop
plan: 01
type: execute
wave: 1
depends_on:
  - "03-04"
files_modified:
  - src/domain/assessment/types.ts
  - src/content/type-copy/ko/v1.ts
  - src/domain/assessment/result-copy.ts
  - src/app/results/[publicId]/page.tsx
  - test/assessment/result-contract.test.ts
  - test/assessment/public-result.test.ts
autonomous: true
requirements:
  - RSLT-02
  - RSLT-04
  - RSLT-05
  - RSLT-06
  - SHAR-07
must_haves:
  truths:
    - "Public result rendering still comes entirely from the stored snapshot, but now has enough versioned interpretation content to explain the result."
    - "The result-copy contract can describe wing, growth/stress, disclaimer, and recommendation content without hardcoding prose in the page component."
    - "Regression tests prove Phase 4 content expansion does not reintroduce rescoring or break immutable snapshot rendering."
  artifacts:
    - path: "src/content/type-copy/ko/v1.ts"
      provides: "Versioned interpretation content for cards, disclaimer, and recommendation sections"
    - path: "src/domain/assessment/types.ts"
      provides: "Expanded result-copy contract used by immutable snapshot rendering"
    - path: "src/app/results/[publicId]/page.tsx"
      provides: "Server-side mapping from stored snapshot record to richer view model fields"
    - path: "test/assessment/public-result.test.ts"
      provides: "Regression coverage for richer immutable result rendering"
  key_links:
    - from: "src/app/results/[publicId]/page.tsx"
      to: "src/domain/assessment/result-copy.ts"
      via: "Page resolves richer interpretation copy strictly from stored copyVersion"
      pattern: "resolveResultCopy"
    - from: "src/domain/assessment/result-copy.ts"
      to: "src/content/type-copy/ko/v1.ts"
      via: "Expanded type-copy catalog stays the single interpretation source of truth"
      pattern: "typeCopyDefinition"
    - from: "test/assessment/public-result.test.ts"
      to: "src/app/results/[publicId]/page.tsx"
      via: "Server-render tests lock richer snapshot output without rescoring"
      pattern: "scoreAssessment"
---

<objective>
Expand the immutable result-copy and view-model contract so Phase 4 can render a real interpretation experience without breaking the stored-snapshot boundary from Phase 2.

Purpose: Create the versioned content and mapping layer before visual redesign, so later UI plans do not hardcode copy or guess at unavailable fields.
Output: A richer copy contract, server-side snapshot mapping for result interpretation, and regression tests that keep public result rendering immutable.
</objective>

<execution_context>
@/home/ubuntu/Project/Enneagram/.codex/get-shit-done/workflows/execute-plan.md
@/home/ubuntu/Project/Enneagram/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-result-interpretation-share-loop/04-RESEARCH.md
@.planning/phases/04-result-interpretation-share-loop/04-VALIDATION.md
@.planning/phases/02-persistent-result-snapshots/02-VERIFICATION.md
@src/domain/assessment/types.ts
@src/content/type-copy/ko/v1.ts
@src/domain/assessment/result-copy.ts
@src/app/results/[publicId]/page.tsx
@test/assessment/result-contract.test.ts
@test/assessment/public-result.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Expand the versioned interpretation-copy contract for Phase 4 content</name>
  <read_first>
    @src/domain/assessment/types.ts
    @src/content/type-copy/ko/v1.ts
    @src/domain/assessment/result-copy.ts
    @test/assessment/result-contract.test.ts
  </read_first>
  <files>src/domain/assessment/types.ts, src/content/type-copy/ko/v1.ts, src/domain/assessment/result-copy.ts, test/assessment/result-contract.test.ts</files>
  <acceptance_criteria>
    - `TypeCopyEntry` (or adjacent result-copy types) can represent interpretation cards, disclaimer text, and recommendation items needed by Phase 4.
    - `src/content/type-copy/ko/v1.ts` provides those richer fields for every Enneagram type under the existing versioned catalog.
    - `test/assessment/result-contract.test.ts` guards the richer copy contract so missing Phase 4 fields fail fast.
  </acceptance_criteria>
  <action>Start by extending the result-copy type contract and add failing tests for the richer interpretation content Phase 4 needs. Keep the content versioned in the existing `typeCopyDefinition` catalog instead of scattering prose through result components. Include only the fields that downstream UI plans actually need: hero/summary support, detailed explanation cards, explicit interpretation disclaimer copy, and recommendation items for the shared-result loop.</action>
  <verify>
    <automated>npm exec vitest run test/assessment/result-contract.test.ts</automated>
  </verify>
  <done>Phase 4 has a versioned interpretation-content contract that downstream UI work can rely on without inventing ad hoc strings.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Map stored snapshots into the richer result-page view model without rescoring</name>
  <read_first>
    @src/app/results/[publicId]/page.tsx
    @src/domain/assessment/result-copy.ts
    @test/assessment/public-result.test.ts
  </read_first>
  <files>src/app/results/[publicId]/page.tsx, test/assessment/public-result.test.ts</files>
  <acceptance_criteria>
    - The public result page still loads by `publicId` and does not import or call scoring logic.
    - The page view model now exposes the richer interpretation, disclaimer, and recommendation fields derived from stored type ids plus `copyVersion`.
    - `test/assessment/public-result.test.ts` covers the richer immutable view-model mapping.
    - `npm run typecheck` passes after the mapping changes.
  </acceptance_criteria>
  <action>Update the public result page mapping so stored snapshot ids and `copyVersion` resolve into the richer interpretation fields needed by Phase 4, including labels or linked copy for wing, growth, and stress presentation where appropriate. Keep this strictly a read-only mapping layer over the stored snapshot: do not recalculate scores and do not introduce browser-only assumptions here. Add tests first so immutable rendering remains explicit.</action>
  <verify>
    <automated>npm exec vitest run test/assessment/public-result.test.ts && npm run typecheck</automated>
  </verify>
  <done>The server result route now produces a Phase 4-ready immutable view model from stored snapshot data and versioned copy only.</done>
</task>

</tasks>

<verification>
- Run `npm exec vitest run test/assessment/result-contract.test.ts test/assessment/public-result.test.ts`
- Run `npm run typecheck`
- Confirm `src/app/results/[publicId]/page.tsx` still does not call scoring logic
- Confirm richer disclaimer/recommendation fields come from `src/content/type-copy/ko/v1.ts`
</verification>

<success_criteria>
- Phase 4 interpretation content is versioned and immutable
- The result page has enough mapped data to render explanation, disclaimer, and recommendation sections
- Public result rendering remains a stored-snapshot read path, not a rescore path
</success_criteria>

<output>
After completion, create `.planning/phases/04-result-interpretation-share-loop/04-01-SUMMARY.md`
</output>
