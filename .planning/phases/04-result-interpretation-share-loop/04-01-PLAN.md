---
phase: 04-result-interpretation-share-loop
plan: 01
type: execute
wave: 1
depends_on:
  - "03-04"
files_modified:
  - src/domain/assessment/types.ts
  - src/content/type-copy/ko/v1.ts
  - src/domain/assessment/result-copy.ts
  - src/app/results/[publicId]/page.tsx
  - src/app/results/[publicId]/result-snapshot-view.tsx
  - test/assessment/public-result.test.ts
autonomous: true
requirements:
  - RSLT-05
  - RSLT-06
must_haves:
  truths:
    - "The public result page still resolves interpretation content strictly from the stored `copyVersion`, but the copy contract now includes enough structured content for detailed explanation cards and non-diagnostic guidance."
    - "The result page view model exposes named summary fields for primary type, wing, growth, stress, scores, and interpretation sections without rescoring or reading transient client state."
    - "Regression tests prove unsupported copy versions still fail loudly and richer result copy renders from immutable snapshot data."
  artifacts:
    - path: "src/content/type-copy/ko/v1.ts"
      provides: "Versioned Korean interpretation copy expanded for detailed cards and disclaimer content"
    - path: "src/domain/assessment/types.ts"
      provides: "Type-level contract for richer result interpretation content"
    - path: "src/domain/assessment/result-copy.ts"
      provides: "Versioned lookup boundary that remains immutable-snapshot compatible"
    - path: "src/app/results/[publicId]/page.tsx"
      provides: "Server-built result view model using stored snapshot data and expanded copy"
    - path: "test/assessment/public-result.test.ts"
      provides: "Regression coverage for richer immutable result rendering and disclaimer behavior"
  key_links:
    - from: "src/app/results/[publicId]/page.tsx"
      to: "src/domain/assessment/result-copy.ts"
      via: "Stored `copyVersion` resolves richer interpretation content"
      pattern: "resolveResultCopy|copyVersion"
    - from: "src/domain/assessment/result-copy.ts"
      to: "src/content/type-copy/ko/v1.ts"
      via: "Versioned catalog exposes detailed explanation/disclaimer copy"
      pattern: "typeCopyDefinition|entries"
    - from: "src/app/results/[publicId]/result-snapshot-view.tsx"
      to: "src/app/results/[publicId]/page.tsx"
      via: "Expanded immutable view model is rendered without rescoring"
      pattern: "ResultSnapshotViewModel|copy"
---

<objective>
Create the content and view-model foundation for Phase 4 by expanding the versioned interpretation-copy contract and the immutable public-result view model before major UI and sharing work land.

Purpose: Satisfy RSLT-05 and RSLT-06 at the contract layer first so later plans can build a richer result-first page without scattering hardcoded copy or reintroducing rescoring drift.
Output: Expanded versioned result copy, a richer immutable result view model, and regression tests proving the page still renders from stored snapshot data.
</objective>

<execution_context>
@/home/ubuntu/Project/Enneagram/.codex/get-shit-done/workflows/execute-plan.md
@/home/ubuntu/Project/Enneagram/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-result-interpretation-share-loop/04-RESEARCH.md
@.planning/phases/04-result-interpretation-share-loop/04-VALIDATION.md
@.planning/phases/02-persistent-result-snapshots/02-VERIFICATION.md
@src/domain/assessment/types.ts
@src/content/type-copy/ko/v1.ts
@src/domain/assessment/result-copy.ts
@src/app/results/[publicId]/page.tsx
@src/app/results/[publicId]/result-snapshot-view.tsx
@test/assessment/public-result.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Expand the versioned interpretation-copy contract for detailed result reading</name>
  <read_first>
    @src/domain/assessment/types.ts
    @src/content/type-copy/ko/v1.ts
    @src/domain/assessment/result-copy.ts
    @test/assessment/public-result.test.ts
  </read_first>
  <files>src/domain/assessment/types.ts, src/content/type-copy/ko/v1.ts, src/domain/assessment/result-copy.ts, test/assessment/public-result.test.ts</files>
  <acceptance_criteria>
    - `TypeCopyEntry` supports richer explanation content beyond title/summary.
    - The copy contract includes non-diagnostic guidance/disclaimer content that can be rendered on the public result page.
    - Unsupported `copyVersion` handling remains explicit and regression-tested.
  </acceptance_criteria>
  <action>Start by extending `test/assessment/public-result.test.ts` with failing expectations for richer interpretation content and disclaimer rendering. Then update `src/domain/assessment/types.ts`, `src/content/type-copy/ko/v1.ts`, and `src/domain/assessment/result-copy.ts` so each type entry can supply the detailed explanation and caution content Phase 4 needs while still resolving strictly by stored `copyVersion`.</action>
  <verify>
    <automated>npm exec vitest run test/assessment/public-result.test.ts</automated>
  </verify>
  <done>The codebase has a versioned, immutable interpretation-copy contract rich enough for Phase 4 result reading without hardcoded ad hoc UI copy.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build the richer immutable public-result view model from stored snapshot data</name>
  <read_first>
    @src/app/results/[publicId]/page.tsx
    @src/app/results/[publicId]/result-snapshot-view.tsx
    @src/domain/assessment/result-copy.ts
    @test/assessment/public-result.test.ts
  </read_first>
  <files>src/app/results/[publicId]/page.tsx, src/app/results/[publicId]/result-snapshot-view.tsx, test/assessment/public-result.test.ts</files>
  <acceptance_criteria>
    - The page builds a named immutable view model for primary type, wing, growth/stress, score distribution, nearby types, and new interpretation sections.
    - No rescoring is introduced; the page remains a public-snapshot reader.
    - Regression tests cover the expanded view-model-to-renderer path.
  </acceptance_criteria>
  <action>Update `src/app/results/[publicId]/page.tsx` and `src/app/results/[publicId]/result-snapshot-view.tsx` so the server route builds a richer `ResultSnapshotViewModel` from stored record fields plus versioned copy. Keep the transformation one-way from persisted snapshot to render props, with no imports from scoring logic. Extend `test/assessment/public-result.test.ts` to lock this rendering boundary.</action>
  <verify>
    <automated>npm exec vitest run test/assessment/public-result.test.ts && npm run typecheck</automated>
  </verify>
  <done>The public result page now has the immutable content primitives Phase 4 UI needs, and the snapshot boundary remains intact.</done>
</task>

</tasks>

<verification>
- Run `npm exec vitest run test/assessment/public-result.test.ts`
- Run `npm run typecheck`
- Confirm `src/app/results/[publicId]/page.tsx` still resolves copy via stored `copyVersion`
- Confirm no public-result path imports rescoring logic
</verification>

<success_criteria>
- Rich interpretation/disclaimer content is versioned and immutable-snapshot compatible
- Public result rendering gains named content primitives without breaking Phase 2 persistence guarantees
- Phase 4 UI work can build on a stable result-view contract instead of hardcoded strings
</success_criteria>

<output>
After completion, create `.planning/phases/04-result-interpretation-share-loop/04-01-SUMMARY.md`
</output>
