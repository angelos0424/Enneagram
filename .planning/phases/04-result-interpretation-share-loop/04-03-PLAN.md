---
phase: 04-result-interpretation-share-loop
plan: 03
type: execute
wave: 3
depends_on:
  - "04-02"
files_modified:
  - src/app/results/[publicId]/result-snapshot-view.tsx
  - src/features/result-sharing/result-share-actions.tsx
  - test/assessment/public-result.test.ts
  - test/e2e/public-result.spec.ts
autonomous: true
requirements:
  - SHAR-02
must_haves:
  truths:
    - "Users can share a public result page through an explicit action on the result page."
    - "The share action prefers native share when available and falls back to a copy-link path when it is not."
    - "The share interaction does not change the immutable public-result contract or require extra persistence."
  artifacts:
    - path: "src/features/result-sharing/result-share-actions.tsx"
      provides: "Client-only share action boundary for browser APIs"
    - path: "src/app/results/[publicId]/result-snapshot-view.tsx"
      provides: "Result-page placement for share actions"
    - path: "test/e2e/public-result.spec.ts"
      provides: "Direct public-result browser coverage for result-page share affordance"
  key_links:
    - from: "src/app/results/[publicId]/result-snapshot-view.tsx"
      to: "src/features/result-sharing/result-share-actions.tsx"
      via: "Server-rendered result page mounts a small client island for share actions"
      pattern: "ResultShareActions"
---

<objective>
Add share actions to the public result page without weakening the immutable server-rendered result route.

Purpose: Satisfy SHAR-02 with a small, isolated client boundary that can use `navigator.share` and clipboard APIs while keeping the page data and persistence model unchanged.
Output: A share action component, result-page integration, and browser verification for share affordance behavior.
</objective>

<execution_context>
@/home/ubuntu/Project/Enneagram/.codex/get-shit-done/workflows/execute-plan.md
@/home/ubuntu/Project/Enneagram/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-result-interpretation-share-loop/04-RESEARCH.md
@.planning/phases/04-result-interpretation-share-loop/04-VALIDATION.md
@src/app/results/[publicId]/result-snapshot-view.tsx
@src/app/results/[publicId]/snapshot-metadata.ts
@test/assessment/public-result.test.ts
@test/e2e/mobile-assessment.spec.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add test coverage for result-page share affordances and fallback behavior</name>
  <read_first>
    @src/app/results/[publicId]/result-snapshot-view.tsx
    @test/assessment/public-result.test.ts
    @test/e2e/public-result.spec.ts
  </read_first>
  <files>test/assessment/public-result.test.ts, test/e2e/public-result.spec.ts</files>
  <acceptance_criteria>
    - Tests describe a visible share affordance on the public result page.
    - Browser coverage reserves or implements a mobile scenario for share/copy behavior.
    - The tests do not assume a specific third-party share SDK.
  </acceptance_criteria>
  <action>Start by extending the public-result regression suite and mobile browser spec so SHAR-02 is explicit before implementation. The browser case should verify user-visible behavior around the share affordance and fallback link copy flow instead of trying to prove OS-native UI internals.</action>
  <verify>
    <automated>npm exec vitest run test/assessment/public-result.test.ts</automated>
  </verify>
  <done>Share behavior is specified in tests before browser API handling is added.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Implement a small client-side share action boundary on the public result page</name>
  <read_first>
    @src/app/results/[publicId]/result-snapshot-view.tsx
    @src/app/results/[publicId]/snapshot-metadata.ts
    @test/assessment/public-result.test.ts
    @test/e2e/public-result.spec.ts
  </read_first>
  <files>src/features/result-sharing/result-share-actions.tsx, src/app/results/[publicId]/result-snapshot-view.tsx, test/assessment/public-result.test.ts, test/e2e/public-result.spec.ts</files>
  <acceptance_criteria>
    - Share actions live in a dedicated client component instead of converting the whole page to a client boundary.
    - The result page offers native share when available and a copy-link fallback when it is not.
    - The share action uses the existing public result URL instead of creating new tokens or routes.
    - `npm run typecheck` passes after the share action is integrated.
  </acceptance_criteria>
  <action>Create a small client component for result-page sharing and mount it from the server-rendered result view. Keep the UX simple and mobile-first: one primary share action with clear fallback messaging. Reuse existing page metadata text where appropriate, but do not expand into OG-image or social-card work yet.</action>
  <verify>
    <automated>npm exec vitest run test/assessment/public-result.test.ts && npm run typecheck && npx playwright test test/e2e/public-result.spec.ts -g "shares or copies the public result link"</automated>
  </verify>
  <done>The public result page now supports explicit share actions without weakening the immutable public route or introducing extra persistence logic.</done>
</task>

</tasks>

<verification>
- Run `npm exec vitest run test/assessment/public-result.test.ts`
- Run `npm run typecheck`
- Run `npx playwright test test/e2e/public-result.spec.ts -g "shares or copies the public result link"`
- Confirm the share action is isolated in a client component
- Confirm share behavior uses the current public URL
</verification>

<success_criteria>
- SHAR-02 is satisfied by a visible share action on public results
- The immutable result route remains server-rendered except for the small share island
- Browser coverage proves the share affordance works on a mobile viewport
</success_criteria>

<output>
After completion, create `.planning/phases/04-result-interpretation-share-loop/04-03-SUMMARY.md`
</output>
