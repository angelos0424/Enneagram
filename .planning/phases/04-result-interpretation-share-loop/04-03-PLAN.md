---
phase: 04-result-interpretation-share-loop
plan: 03
type: execute
wave: 3
depends_on:
  - "04-02"
files_modified:
  - src/app/results/[publicId]/page.tsx
  - src/app/results/[publicId]/result-snapshot-view.tsx
  - src/app/results/[publicId]/public-result-share-controls.tsx
  - src/app/results/[publicId]/result-recommendations.tsx
  - test/assessment/public-result.test.ts
  - test/e2e/public-result.spec.ts
autonomous: true
requirements:
  - SHAR-02
  - SHAR-07
must_haves:
  truths:
    - "The public result page offers a practical sharing action via clipboard copy and, when available, the native Web Share path."
    - "The recommendation section proposes clear next actions for shared-result visitors instead of ending the page at static interpretation content."
    - "Browser coverage verifies direct public-result behavior without depending on rescoring or mutating the stored snapshot."
  artifacts:
    - path: "src/app/results/[publicId]/public-result-share-controls.tsx"
      provides: "Client share boundary with clipboard fallback and Web Share integration"
    - path: "src/app/results/[publicId]/result-recommendations.tsx"
      provides: "Recommendation section for next actions on the shared result page"
    - path: "test/e2e/public-result.spec.ts"
      provides: "Browser proof for direct public-result CTA/share loop behavior"
    - path: "test/assessment/public-result.test.ts"
      provides: "Regression coverage for share controls and recommendation rendering"
  key_links:
    - from: "src/app/results/[publicId]/public-result-share-controls.tsx"
      to: "src/app/results/[publicId]/page.tsx"
      via: "Client share controls consume the canonical public-result URL/context"
      pattern: "navigator.share|clipboard|publicId"
    - from: "src/app/results/[publicId]/result-recommendations.tsx"
      to: "src/app/results/[publicId]/result-snapshot-view.tsx"
      via: "Recommendation content composes below the immutable result content without altering snapshot semantics"
      pattern: "nearbyTypes|검사해보기"
    - from: "test/e2e/public-result.spec.ts"
      to: "src/app/results/[publicId]/public-result-share-controls.tsx"
      via: "Browser assertions prove copy/share fallback feedback and CTA behavior"
      pattern: "share|copy|검사해보기"
---

<objective>
Complete the share loop by adding explicit share actions, a recommendation section, and direct public-result browser coverage that proves the page works as a shared destination as well as a post-submit landing page.

Purpose: Satisfy SHAR-02 and SHAR-07 while preserving the immutable snapshot boundary established in earlier phases and reinforced in Plans 04-01/04-02.
Output: Client share controls, recommendation content, and direct public-result browser verification.
</objective>

<execution_context>
@/home/ubuntu/Project/Enneagram/.codex/get-shit-done/workflows/execute-plan.md
@/home/ubuntu/Project/Enneagram/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-result-interpretation-share-loop/04-RESEARCH.md
@.planning/phases/04-result-interpretation-share-loop/04-VALIDATION.md
@.planning/phases/04-result-interpretation-share-loop/04-02-PLAN.md
@src/app/results/[publicId]/page.tsx
@src/app/results/[publicId]/result-snapshot-view.tsx
@test/assessment/public-result.test.ts
@test/e2e/mobile-assessment.spec.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add client share controls with clipboard fallback</name>
  <read_first>
    @src/app/results/[publicId]/page.tsx
    @src/app/results/[publicId]/result-snapshot-view.tsx
    @test/assessment/public-result.test.ts
  </read_first>
  <files>src/app/results/[publicId]/page.tsx, src/app/results/[publicId]/result-snapshot-view.tsx, src/app/results/[publicId]/public-result-share-controls.tsx, test/assessment/public-result.test.ts</files>
  <acceptance_criteria>
    - Users can copy the public result URL from the result page.
    - If `navigator.share` is available, the page can use it without making it the only path.
    - Success/failure feedback is accessible and retryable.
  </acceptance_criteria>
  <action>Create a dedicated client component, such as `src/app/results/[publicId]/public-result-share-controls.tsx`, to own browser APIs. Start with failing tests around share/copy behavior or render contracts in `test/assessment/public-result.test.ts`, then implement clipboard-first sharing with optional `navigator.share` support. Keep the server page responsible for immutable snapshot data and hand only the canonical URL/context into the client island.</action>
  <verify>
    <automated>npm exec vitest run test/assessment/public-result.test.ts && npm run typecheck</automated>
  </verify>
  <done>The public result page offers practical sharing without moving snapshot rendering into the client or relying exclusively on fragile browser APIs.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add recommendation content and direct public-result browser coverage</name>
  <read_first>
    @src/app/results/[publicId]/result-snapshot-view.tsx
    @test/assessment/public-result.test.ts
    @test/e2e/mobile-assessment.spec.ts
  </read_first>
  <files>src/app/results/[publicId]/result-snapshot-view.tsx, src/app/results/[publicId]/result-recommendations.tsx, test/assessment/public-result.test.ts, test/e2e/public-result.spec.ts</files>
  <acceptance_criteria>
    - The page includes a recommendation section with clear next actions for the visitor.
    - Recommendation content composes with the immutable result page instead of replacing it.
    - Browser coverage verifies direct public-result entry, share/copy fallback feedback, and the restart CTA loop.
  </acceptance_criteria>
  <action>Add a recommendation section below the main result content, using nearby types and restart/share prompts to suggest next actions without mutating snapshot data. Create `test/e2e/public-result.spec.ts` for direct public-result coverage instead of relying only on the submit flow. Prefer deterministic browser assertions such as CTA routing and clipboard fallback feedback; keep native share-sheet behavior manual-only.</action>
  <verify>
    <automated>npm exec vitest run test/assessment/public-result.test.ts test/assessment/score-route.test.ts && npm run typecheck && npm run build && npx playwright test test/e2e/public-result.spec.ts</automated>
  </verify>
  <done>The shared result page closes the loop with explicit share actions, next-step recommendations, and direct browser verification of public-result behavior.</done>
</task>

</tasks>

<verification>
- Run `npm exec vitest run test/assessment/public-result.test.ts test/assessment/score-route.test.ts`
- Run `npm run typecheck`
- Run `npm run build`
- Run `npx playwright test test/e2e/public-result.spec.ts`
- Confirm share controls use the canonical public result URL
- Confirm the recommendation section appears below the immutable result content and includes a restart path
</verification>

<success_criteria>
- Users can share results directly from the public result page
- Shared-result visitors receive clear next-step recommendations, including restart
- Direct public-result browser coverage exists in addition to submit-to-result coverage
</success_criteria>

<output>
After completion, create `.planning/phases/04-result-interpretation-share-loop/04-03-SUMMARY.md`
</output>
