---
phase: 04-result-interpretation-share-loop
plan: 04
type: execute
wave: 4
depends_on:
  - "04-02"
  - "04-03"
files_modified:
  - src/app/api/assessment-session/route.ts
  - src/app/results/[publicId]/result-snapshot-view.tsx
  - src/app/results/[publicId]/public-result-restart-cta.tsx
  - src/app/results/[publicId]/result-recommendations.tsx
  - test/assessment/assessment-session-route.test.ts
  - test/assessment/public-result.test.ts
  - test/e2e/public-result.spec.ts
autonomous: true
requirements:
  - SHAR-04
  - SHAR-05
  - SHAR-07
must_haves:
  truths:
    - "The top `검사해보기` CTA is always visible on public result pages."
    - "Tapping the CTA uses an explicit fresh-start boundary that clears or bypasses any existing anonymous draft before returning visitors to `/`."
    - "The public result page closes with a recommendation section that suggests next actions without displacing the result-first hierarchy."
  artifacts:
    - path: "src/app/results/[publicId]/result-snapshot-view.tsx"
      provides: "Top CTA placement and recommendation section composition"
    - path: "src/app/results/[publicId]/public-result-restart-cta.tsx"
      provides: "Client restart boundary that performs explicit fresh-start behavior before navigating home"
    - path: "src/app/api/assessment-session/route.ts"
      provides: "Server boundary for clearing or replacing the canonical anonymous draft session"
    - path: "test/assessment/assessment-session-route.test.ts"
      provides: "Regression coverage for explicit fresh-start/reset semantics"
    - path: "test/e2e/public-result.spec.ts"
      provides: "Browser proof that shared-result visitors can return to a fresh assessment"
    - path: "test/assessment/public-result.test.ts"
      provides: "Regression coverage for CTA visibility and recommendation rendering"
  key_links:
    - from: "src/app/results/[publicId]/public-result-restart-cta.tsx"
      to: "src/app/api/assessment-session/route.ts"
      via: "Restart CTA clears or replaces the canonical draft before returning to the home assessment flow"
      pattern: "DELETE|reset|assessment-session"
---

<objective>
Complete the share loop by wiring the public-result CTA back to a fresh assessment start and finishing the recommendation section for shared-result visitors.

Purpose: Close the core growth loop promised by Phase 4 after the result page and share action surfaces already exist.
Output: Always-visible top CTA, explicit fresh-start restart boundary, recommendation section, and browser verification that shared-result visitors restart cleanly.
</objective>

<execution_context>
@/home/ubuntu/Project/Enneagram/.codex/get-shit-done/workflows/execute-plan.md
@/home/ubuntu/Project/Enneagram/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-result-interpretation-share-loop/04-RESEARCH.md
@.planning/phases/04-result-interpretation-share-loop/04-VALIDATION.md
@.planning/phases/03-mobile-assessment-flow/03-VERIFICATION.md
@src/app/api/assessment-session/route.ts
@src/app/results/[publicId]/result-snapshot-view.tsx
@src/app/results/[publicId]/public-result-restart-cta.tsx
@test/assessment/public-result.test.ts
@test/e2e/public-result.spec.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Lock CTA-loop and recommendation behavior in regression coverage</name>
  <read_first>
    @src/app/results/[publicId]/result-snapshot-view.tsx
    @src/app/page.tsx
    @test/assessment/public-result.test.ts
    @test/e2e/mobile-assessment.spec.ts
  </read_first>
  <files>test/assessment/public-result.test.ts, test/e2e/mobile-assessment.spec.ts</files>
  <acceptance_criteria>
    - Tests assert the public result page always exposes a top `검사해보기` CTA.
    - Tests cover recommendation section rendering from versioned copy content.
    - Tests cover the explicit reset/fresh-start boundary instead of assuming `/` alone is enough.
  </acceptance_criteria>
  <action>Extend result-page regression coverage and direct public-result browser coverage first so the share loop is explicit. The browser test should prove the user lands on a fresh anonymous assessment start after tapping the CTA from a public result page, not a stale resumed state.</action>
  <verify>
    <automated>npm exec vitest run test/assessment/public-result.test.ts test/assessment/assessment-session-route.test.ts</automated>
  </verify>
  <done>The CTA loop and recommendation finish are captured in tests before implementation.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Implement the shared-result restart loop and recommendation finish</name>
  <read_first>
    @src/app/results/[publicId]/result-snapshot-view.tsx
    @src/app/api/assessment-session/route.ts
    @test/assessment/public-result.test.ts
    @test/e2e/public-result.spec.ts
  </read_first>
  <files>src/app/api/assessment-session/route.ts, src/app/results/[publicId]/result-snapshot-view.tsx, src/app/results/[publicId]/public-result-restart-cta.tsx, src/app/results/[publicId]/result-recommendations.tsx, test/assessment/assessment-session-route.test.ts, test/assessment/public-result.test.ts, test/e2e/public-result.spec.ts</files>
  <acceptance_criteria>
    - The top CTA remains visible in the public result hero.
    - Shared-result visitors who use the CTA see a fresh assessment start, not resumed result-page or stale-draft confusion.
    - A recommendation section appears after the core interpretation content and is powered by the versioned copy contract from Plan 01.
    - `npm run build` passes after the loop is implemented.
  </acceptance_criteria>
  <action>Implement a dedicated fresh-start boundary, such as `DELETE /api/assessment-session` or an equivalent explicit reset path, so the public-result CTA can intentionally discard or replace an existing anonymous draft before returning to `/`. Keep the result page result-first: the CTA should be prominent but should not overshadow the main hero. The recommendation section should feel like a downstream suggestion panel, not a replacement for the core result explanation. Update direct public-result browser coverage to prove the full shared-result loop on a mobile viewport.</action>
  <verify>
    <automated>npm exec vitest run test/assessment/public-result.test.ts test/assessment/assessment-session-route.test.ts && npm run typecheck && npm run build && npx playwright test test/e2e/public-result.spec.ts -g "returns shared-result visitors to a fresh assessment"</automated>
  </verify>
  <done>The public result page now drives a clear share loop back into a fresh anonymous assessment experience and closes with recommendation content for next actions.</done>
</task>

</tasks>

<verification>
- Run `npm exec vitest run test/assessment/public-result.test.ts test/assessment/assessment-session-route.test.ts`
- Run `npm run typecheck`
- Run `npm run build`
- Run `npx playwright test test/e2e/public-result.spec.ts -g "returns shared-result visitors to a fresh assessment"`
- Confirm the public result page exposes `검사해보기` at the top
- Confirm the CTA uses an explicit fresh-start boundary before returning to `/`
</verification>

<success_criteria>
- SHAR-04 is satisfied by a top CTA on public results
- SHAR-05 is satisfied by a fresh restart into the anonymous assessment flow
- SHAR-07 is satisfied by a recommendation section grounded in versioned copy content
</success_criteria>

<output>
After completion, create `.planning/phases/04-result-interpretation-share-loop/04-04-SUMMARY.md`
</output>
