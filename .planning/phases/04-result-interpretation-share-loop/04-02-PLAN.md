---
phase: 04-result-interpretation-share-loop
plan: 02
type: execute
wave: 2
depends_on:
  - "04-01"
files_modified:
  - src/app/results/[publicId]/page.tsx
  - src/app/results/[publicId]/result-snapshot-view.tsx
  - src/app/globals.css
  - test/assessment/public-result.test.ts
  - test/e2e/mobile-assessment.spec.ts
autonomous: true
requirements:
  - RSLT-01
  - RSLT-02
  - RSLT-03
  - RSLT-04
  - SHAR-04
  - SHAR-05
must_haves:
  truths:
    - "The public result page is result-first: the primary type hero is visually dominant at the top, ahead of supporting data."
    - "The same mobile page exposes wing, growth direction, stress direction, and score distribution without making the user hunt across separate routes."
    - "A top-level `검사해보기` CTA is always visible on shared/public result pages and routes back into the anonymous assessment start flow."
  artifacts:
    - path: "src/app/results/[publicId]/result-snapshot-view.tsx"
      provides: "Mobile-first result layout with hero, summary cards, score distribution, and top CTA"
    - path: "src/app/results/[publicId]/page.tsx"
      provides: "Server data wiring for the richer result-first layout"
    - path: "test/assessment/public-result.test.ts"
      provides: "Regression coverage for result-first hierarchy and CTA presence"
    - path: "test/e2e/mobile-assessment.spec.ts"
      provides: "Browser proof that submit lands on the richer result page and the top CTA returns to `/`"
  key_links:
    - from: "src/app/results/[publicId]/result-snapshot-view.tsx"
      to: "src/app/page.tsx"
      via: "Top CTA links shared-result visitors back into the anonymous assessment flow"
      pattern: "검사해보기|href=\"/\""
    - from: "test/e2e/mobile-assessment.spec.ts"
      to: "src/app/results/[publicId]/result-snapshot-view.tsx"
      via: "Browser assertions prove result-page landing content and restart CTA behavior"
      pattern: "redirects to the saved public result page after submit|검사해보기"
---

<objective>
Turn the immutable result view model into the actual Phase 4 user experience: a mobile-first, result-first public page that shows primary type first, keeps supporting interpretation data on the same screen, and always offers a restart CTA for shared-result visitors.

Purpose: Satisfy the core reading and restart-loop requirements before adding share polish, keeping the user’s first interaction after submit or share visit centered on understanding the result.
Output: A mobile-first public result layout, persistent top CTA loop back to `/`, and browser coverage for landing on and exiting from the page.
</objective>

<execution_context>
@/home/ubuntu/Project/Enneagram/.codex/get-shit-done/workflows/execute-plan.md
@/home/ubuntu/Project/Enneagram/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-result-interpretation-share-loop/04-RESEARCH.md
@.planning/phases/04-result-interpretation-share-loop/04-VALIDATION.md
@.planning/phases/04-result-interpretation-share-loop/04-01-PLAN.md
@src/app/page.tsx
@src/app/results/[publicId]/page.tsx
@src/app/results/[publicId]/result-snapshot-view.tsx
@src/app/globals.css
@test/assessment/public-result.test.ts
@test/e2e/mobile-assessment.spec.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement the result-first mobile layout for immutable public results</name>
  <read_first>
    @src/app/results/[publicId]/result-snapshot-view.tsx
    @src/app/results/[publicId]/page.tsx
    @test/assessment/public-result.test.ts
  </read_first>
  <files>src/app/results/[publicId]/result-snapshot-view.tsx, src/app/results/[publicId]/page.tsx, src/app/globals.css, test/assessment/public-result.test.ts</files>
  <acceptance_criteria>
    - The page hero shows the primary type first and clearly dominates the viewport.
    - Supporting cards for wing, growth, stress, and score distribution appear on the same page.
    - The page includes explicit interpretation caution/non-diagnostic guidance.
  </acceptance_criteria>
  <action>Start by extending `test/assessment/public-result.test.ts` with failing assertions for the result-first information hierarchy and supporting sections. Then redesign `src/app/results/[publicId]/result-snapshot-view.tsx` into a polished mobile-first layout, using `src/app/globals.css` only if shared styling primitives are required. Keep all content sourced from the immutable view model created in Plan 04-01.</action>
  <verify>
    <automated>npm exec vitest run test/assessment/public-result.test.ts && npm run typecheck</automated>
  </verify>
  <done>The public result page communicates the primary result and supporting interpretation data in the order users need to read it on mobile.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add the persistent top CTA loop and browser proof for result-page landing</name>
  <read_first>
    @src/app/results/[publicId]/result-snapshot-view.tsx
    @test/e2e/mobile-assessment.spec.ts
    @test/assessment/public-result.test.ts
  </read_first>
  <files>src/app/results/[publicId]/result-snapshot-view.tsx, test/assessment/public-result.test.ts, test/e2e/mobile-assessment.spec.ts</files>
  <acceptance_criteria>
    - A top-level `검사해보기` CTA is visible on shared/public result pages.
    - The CTA routes to `/` and starts the anonymous assessment loop again.
    - Browser coverage proves submit lands on the richer result page and the CTA returns to the assessment start.
  </acceptance_criteria>
  <action>Add the restart CTA near the top of the public result page so a visitor can always begin a fresh assessment. Extend `test/e2e/mobile-assessment.spec.ts` so the submit flow asserts the richer result landing state and the CTA loop back to `/`. Keep the CTA simple and deterministic: route to the anonymous home assessment flow rather than adding special query params or session mutation.</action>
  <verify>
    <automated>npm exec vitest run test/assessment/public-result.test.ts && npm run build && npx playwright test test/e2e/mobile-assessment.spec.ts -g "redirects to the saved public result page after submit"</automated>
  </verify>
  <done>Shared-result visitors can immediately restart the anonymous flow from a prominent top CTA, and the result-page landing loop is browser-verified.</done>
</task>

</tasks>

<verification>
- Run `npm exec vitest run test/assessment/public-result.test.ts`
- Run `npm run typecheck`
- Run `npm run build`
- Run `npx playwright test test/e2e/mobile-assessment.spec.ts -g "redirects to the saved public result page after submit"`
- Confirm the result hero precedes wing/growth/stress/distribution sections
- Confirm a top `검사해보기` CTA routes to `/`
</verification>

<success_criteria>
- Users can understand the primary result immediately on mobile
- Supporting interpretation data is available on the same page without navigation sprawl
- Shared-result visitors always see an obvious restart CTA back into the anonymous flow
</success_criteria>

<output>
After completion, create `.planning/phases/04-result-interpretation-share-loop/04-02-SUMMARY.md`
</output>
