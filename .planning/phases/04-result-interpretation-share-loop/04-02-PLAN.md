---
phase: 04-result-interpretation-share-loop
plan: 02
type: execute
wave: 2
depends_on:
  - "04-01"
files_modified:
  - src/app/results/[publicId]/result-snapshot-view.tsx
  - src/app/results/[publicId]/page.tsx
  - src/app/globals.css
  - test/assessment/public-result.test.ts
autonomous: true
requirements:
  - RSLT-01
  - RSLT-02
  - RSLT-03
  - RSLT-04
  - RSLT-05
  - RSLT-06
must_haves:
  truths:
    - "The public result page is mobile-first and result-first: the primary type hero appears before supporting detail."
    - "The same page clearly presents wing, growth/stress, score distribution, explanation cards, and interpretation disclaimer."
  artifacts:
    - path: "src/app/results/[publicId]/result-snapshot-view.tsx"
      provides: "Mobile-first result interpretation layout"
    - path: "src/app/globals.css"
      provides: "Shared styling primitives needed by the public result experience"
    - path: "test/assessment/public-result.test.ts"
      provides: "Regression coverage for result-first page structure"
  key_links:
    - from: "src/app/results/[publicId]/result-snapshot-view.tsx"
      to: "src/app/results/[publicId]/page.tsx"
      via: "Richer immutable view model is rendered in a result-first layout"
      pattern: "ResultSnapshotView"
    - from: "src/app/results/[publicId]/result-snapshot-view.tsx"
      to: "src/content/type-copy/ko/v1.ts"
      via: "Interpretation cards and disclaimer copy come from the versioned catalog"
      pattern: "copy"
---

<objective>
Turn the minimal public snapshot renderer into a deliberate mobile-first result page that leads with the primary type and keeps all interpretation details on the same page.

Purpose: Satisfy the visible result-reading requirements before share actions are layered on top, while preserving the immutable snapshot route and the new content contract from Plan 01.
Output: A result-first public page layout with top CTA, key stats, score distribution, explanation cards, and disclaimer content.
</objective>

<execution_context>
@/home/ubuntu/Project/Enneagram/.codex/get-shit-done/workflows/execute-plan.md
@/home/ubuntu/Project/Enneagram/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-result-interpretation-share-loop/04-RESEARCH.md
@.planning/phases/04-result-interpretation-share-loop/04-VALIDATION.md
@.planning/phases/04-result-interpretation-share-loop/04-01-PLAN.md
@src/app/results/[publicId]/page.tsx
@src/app/results/[publicId]/result-snapshot-view.tsx
@src/app/globals.css
@test/assessment/public-result.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Lock the result-first public page structure in tests</name>
  <read_first>
    @src/app/results/[publicId]/result-snapshot-view.tsx
    @test/assessment/public-result.test.ts
  </read_first>
  <files>test/assessment/public-result.test.ts</files>
  <acceptance_criteria>
    - Tests assert the public page exposes a top hero for the primary type before the lower-detail sections.
        - Tests cover visible wing, growth/stress, score distribution, explanation-card, and disclaimer content.
    - The assertions are resilient to layout refactors and focus on user-visible semantics, not brittle class names.
  </acceptance_criteria>
      <action>Extend `test/assessment/public-result.test.ts` first so Phase 4's visible page hierarchy is explicitly locked before any UI rewrite. Assert the presence and order of the hero, detail sections, and disclaimer content using server-rendered markup semantics rather than styling details.</action>
  <verify>
    <automated>npm exec vitest run test/assessment/public-result.test.ts</automated>
  </verify>
  <done>The intended Phase 4 public result structure is captured in tests before implementation begins.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Implement the mobile-first result interpretation layout</name>
  <read_first>
    @src/app/results/[publicId]/result-snapshot-view.tsx
    @src/app/results/[publicId]/page.tsx
    @src/app/globals.css
    @test/assessment/public-result.test.ts
  </read_first>
  <files>src/app/results/[publicId]/result-snapshot-view.tsx, src/app/results/[publicId]/page.tsx, src/app/globals.css, test/assessment/public-result.test.ts</files>
  <acceptance_criteria>
    - The hero section leads with the primary type title and summary.
    - The page keeps wing, growth/stress, score distribution, explanation cards, and disclaimer on the same public route.
    - `npm run build` passes after the public page redesign.
  </acceptance_criteria>
  <action>Rewrite `src/app/results/[publicId]/result-snapshot-view.tsx` into a purposeful mobile-first experience that feels like a product page instead of a raw debug render. Use the richer immutable view model from Plan 01. The primary type hero should dominate first impression, followed by supporting result data, with the disclaimer clearly visible later in the page. Keep the page aligned with the existing visual language from the assessment flow instead of introducing a disconnected design system. Do not rely on restart-loop semantics in this plan; the explicit fresh-start CTA boundary belongs in Plan 04-04.</action>
  <verify>
    <automated>npm exec vitest run test/assessment/public-result.test.ts && npm run typecheck && npm run build</automated>
  </verify>
  <done>The public result page satisfies the core reading and interpretation requirements in a mobile-first, result-first layout.</done>
</task>

</tasks>

<verification>
- Run `npm exec vitest run test/assessment/public-result.test.ts`
- Run `npm run typecheck`
- Run `npm run build`
- Confirm the primary type hero appears before score distribution and disclaimer sections
</verification>

<success_criteria>
- RSLT-01 through RSLT-06 are visually satisfied on one page
- The page remains immutable-snapshot driven and mobile-first
</success_criteria>

<output>
After completion, create `.planning/phases/04-result-interpretation-share-loop/04-02-SUMMARY.md`
</output>
