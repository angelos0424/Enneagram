# Phase 1: Assessment Contract & Scoring Core - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-03-29
**Phase:** 1-Assessment Contract & Scoring Core
**Areas discussed:** 문항 소스 관리, 응답 형식과 점수 계산, 결과 판정 규칙, 버전 관리 엄격도, 리커트 응답 라벨

---

## 문항 소스 관리

| Option | Description | Selected |
|--------|-------------|----------|
| 코드 파일 고정 | 문항, 선택지, 유형 설명을 `ts/json` 파일로 관리하고 배포로만 변경 | ✓ |
| 코드 + 시드 데이터 | 초기 원본은 코드에 두되, DB 시드로 적재해서 런타임은 DB를 기준으로 사용 | |
| 처음부터 DB 관리 | 운영자가 바로 DB 기반으로 문항/설명을 수정 가능하게 시작 | |

**User's choice:** 코드 파일 고정
**Notes:** Phase 1에서는 콘텐츠 계약을 먼저 잠그고, 운영 수정 기능은 뒤로 미룬다.

---

## 응답 형식과 점수 계산

| Option | Description | Selected |
|--------|-------------|----------|
| 5점 리커트 | 매우 아니다 ~ 매우 그렇다 계열 척도로 응답하고 유형별 가중치에 반영 | ✓ |
| 강제 선택형 | 두 문장 중 더 가까운 쪽을 고르게 해서 유형 성향을 비교 | |
| 혼합형 | 기본은 5점 척도지만 일부 문항은 강제 선택형으로 섞음 | |

**User's choice:** 5점 리커트
**Notes:** 가중치 기반 점수 엔진을 전제로 한다.

---

## 결과 판정 규칙

| Option | Description | Selected |
|--------|-------------|----------|
| 단순 최고점 규칙 | 가장 높은 점수를 주 유형으로, 인접 유형 중 높은 점수를 날개로 결정 | |
| 최고점 + 근접 점수 표시 | 주 유형과 날개는 정하되, 근접한 상위 유형이 있으면 결과 데이터에 함께 남김 | ✓ |
| 보수적 판정 | 근접 점수 차가 작으면 주 유형은 보여주되 경계 결과로 표시 가능하게 함 | |

**User's choice:** 최고점 + 근접 점수 표시
**Notes:** 최종 판정은 단순하게 유지하되 설명력 강화를 위해 근접 유형 정보를 함께 저장한다.

---

## 버전 관리 엄격도

| Option | Description | Selected |
|--------|-------------|----------|
| 전체 버전 저장 | 문항 세트 버전, 점수 계산 버전, 결과 카피 버전을 모두 결과와 함께 저장 | ✓ |
| 계산 버전만 저장 | 문항과 카피는 현재 기준으로 보고, 점수 계산 버전만 추적 | |
| 문항 세트 버전 + 계산 버전만 저장 | 카피는 현재 버전 기준으로 렌더링하고, 문항과 산식만 고정 | |

**User's choice:** 전체 버전 저장
**Notes:** 영구 공유 링크의 무결성을 위해 결과 당시 기준을 재현 가능해야 한다.

---

## 리커트 응답 라벨

| Option | Description | Selected |
|--------|-------------|----------|
| 동의형 | `전혀 그렇지 않다 / 그렇지 않다 / 보통이다 / 그렇다 / 매우 그렇다` | |
| 빈도형 | `전혀 아니다 / 드물다 / 보통이다 / 자주 그렇다 / 항상 그렇다` | |
| 적합도형 | `전혀 나와 맞지 않는다 / 별로 맞지 않는다 / 반반이다 / 꽤 맞는다 / 매우 잘 맞는다` | ✓ |

**User's choice:** 적합도형
**Notes:** 심리 검사 문맥에서 한국어로 더 자연스럽고 자기보고식 응답에 적합한 톤으로 판단했다.

---

## the agent's Discretion

- 파일 포맷과 타입 모델링 방식
- 점수 정규화와 근접 유형 데이터 구조
- 초기 scaffold와 테스트 fixture 구성

## Deferred Ideas

None
