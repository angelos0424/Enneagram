---
phase: 01-assessment-contract-scoring-core
plan: 02
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - src/domain/assessment/types.ts
  - src/domain/assessment/constants.ts
  - src/content/assessments/ko/v1.ts
  - src/content/type-copy/ko/v1.ts
  - test/assessment/definition.test.ts
  - test/assessment/fixtures.ts
autonomous: true
requirements:
  - ASMT-01
must_haves:
  truths:
    - "A versioned Korean assessment definition exists in code as the single source of truth for prompts, options, and scoring metadata."
    - "Assessment contract changes are guarded by automated tests and type checks before scoring implementation begins."
    - "Phase 1 policy decisions are explicit in code: 5-point Likert labels, equal-top tie-break rule, normalization formula, nearby-type retention size, and persistence scope."
  artifacts:
    - path: "src/content/assessments/ko/v1.ts"
      provides: "Versioned Korean question catalog with 5-point option labels and scoring metadata"
    - path: "src/domain/assessment/constants.ts"
      provides: "Locked Phase 1 scoring policy constants"
    - path: "test/assessment/definition.test.ts"
      provides: "Automated contract coverage for the authoritative assessment definition"
  key_links:
    - from: "src/content/assessments/ko/v1.ts"
      to: "src/domain/assessment/types.ts"
      via: "AssessmentDefinition typing"
      pattern: "satisfies AssessmentDefinition"
    - from: "src/domain/assessment/constants.ts"
      to: "test/assessment/definition.test.ts"
      via: "Policy assertions for versions and locked values"
      pattern: "NEARBY_TYPE_LIMIT|NORMALIZATION_FORMULA|PRIMARY_TYPE_TIE_BREAK"
    - from: "test/assessment/fixtures.ts"
      to: "test/assessment/scoring.test.ts"
      via: "Shared scoring fixture builders for the next plan"
      pattern: "buildUniformAnswers|buildTypeDominantAnswers|buildEqualTopAnswers"
---

<objective>
Define the authoritative Korean assessment contracts and the test fixtures that downstream scoring and persistence plans will consume.

Purpose: Lock the Phase 1 source-of-truth files before any scoring or persistence code is written, so later result snapshots do not drift and D-01 through D-11 are represented explicitly in code.
Output: Versioned assessment/type-copy content modules, locked scoring policy constants, shared fixtures, and definition-level tests.
</objective>

<execution_context>
@/home/ubuntu/Project/Enneagram/.codex/get-shit-done/workflows/execute-plan.md
@/home/ubuntu/Project/Enneagram/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-assessment-contract-scoring-core/01-CONTEXT.md
@.planning/phases/01-assessment-contract-scoring-core/01-RESEARCH.md
@.planning/phases/01-assessment-contract-scoring-core/01-VALIDATION.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define the authoritative assessment and scoring policy contracts per D-01 through D-11</name>
  <files>src/domain/assessment/types.ts, src/domain/assessment/constants.ts, src/content/assessments/ko/v1.ts, src/content/type-copy/ko/v1.ts</files>
  <read_first>.planning/phases/01-assessment-contract-scoring-core/01-CONTEXT.md, .planning/phases/01-assessment-contract-scoring-core/01-RESEARCH.md, src/domain/assessment/types.ts, src/domain/assessment/constants.ts, src/content/assessments/ko/v1.ts, src/content/type-copy/ko/v1.ts</read_first>
  <action>Implement the Phase 1 code-first contracts required by D-01, D-03, D-04, D-05, D-09, D-10, and D-11. In `src/domain/assessment/types.ts`, define `EnneagramType = 1 | 2 | 3 | 4 | 5 | 6 | 7 | 8 | 9`, `LikertValue = 1 | 2 | 3 | 4 | 5`, `AssessmentDefinition`, `AssessmentQuestion`, `LikertOption`, `NearbyTypeScore`, and a minimal `TypeCopyEntry` shape. In `src/domain/assessment/constants.ts`, lock these exact values: `ASSESSMENT_VERSION = "ko-enneagram-v1"`, `SCORING_VERSION = "scoring-ko-v1"`, `COPY_VERSION = "copy-ko-v1"`, `PRIMARY_TYPE_TIE_BREAK = "lowest-type-id"`, `WING_TIE_BREAK = "lower-adjacent-type-id"`, `NORMALIZATION_FORMULA = "round((rawScore / totalRawScore) * 1000) / 10"`, `NEARBY_TYPE_LIMIT = 3`, and `PHASE_1_PERSISTENCE_SCOPE = "schema-and-repository-contract-only"`. In `src/content/assessments/ko/v1.ts`, export the exact agreed labels `전혀 나와 맞지 않는다 / 별로 맞지 않는다 / 반반이다 / 꽤 맞는다 / 매우 잘 맞는다`, a single `assessmentDefinition` object for `ko-enneagram-v1`, and an initial pilot bank of `18` question records with ids `q_001` through `q_018`; each question must include `typeWeights` as `Record<EnneagramType, readonly [number, number, number, number, number]>` so each Likert response contributes deterministically to the 9 type totals. In `src/content/type-copy/ko/v1.ts`, create nine minimal `TypeCopyEntry` objects keyed by type id with `title`, `summary`, and `disclaimerTone: "interpretive"` so Phase 3 and Phase 4 can reference a real `copyVersion` without inventing it later.</action>
  <verify><automated>npm run typecheck</automated></verify>
  <acceptance_criteria>
    - `src/domain/assessment/constants.ts` contains `PRIMARY_TYPE_TIE_BREAK = "lowest-type-id"`
    - `src/domain/assessment/constants.ts` contains `NORMALIZATION_FORMULA = "round((rawScore / totalRawScore) * 1000) / 10"`
    - `src/domain/assessment/constants.ts` contains `NEARBY_TYPE_LIMIT = 3`
    - `src/domain/assessment/constants.ts` contains `PHASE_1_PERSISTENCE_SCOPE = "schema-and-repository-contract-only"`
    - `src/content/assessments/ko/v1.ts` contains all five exact Korean option labels
    - `src/content/assessments/ko/v1.ts` contains `id: "q_018"`
    - `src/content/type-copy/ko/v1.ts` contains `copyVersion: COPY_VERSION` or `version: COPY_VERSION`
  </acceptance_criteria>
  <done>The codebase has one authoritative Korean assessment definition, one authoritative type-copy version, and explicit constants for every Phase 1 policy choice that must not be left implicit.</done>
</task>

<task type="auto">
  <name>Task 2: Add contract-drift fixtures and definition tests for the authoritative assessment definition</name>
  <files>test/assessment/fixtures.ts, test/assessment/definition.test.ts</files>
  <read_first>.planning/phases/01-assessment-contract-scoring-core/01-VALIDATION.md, src/domain/assessment/constants.ts, src/content/assessments/ko/v1.ts, src/content/type-copy/ko/v1.ts, test/assessment/fixtures.ts, test/assessment/definition.test.ts</read_first>
  <action>Create `test/assessment/fixtures.ts` with named answer builders that later scoring tests will reuse: `buildUniformAnswers(value)`, `buildTypeDominantAnswers(typeId)`, and `buildEqualTopAnswers([typeA, typeB])`. Then create `test/assessment/definition.test.ts` to assert the authoritative definition loads exactly one assessment version, exposes the five agreed Korean labels, exports 18 ordered questions, and keeps `ASSESSMENT_VERSION`, `SCORING_VERSION`, `COPY_VERSION`, `NEARBY_TYPE_LIMIT`, and `PHASE_1_PERSISTENCE_SCOPE` synchronized between constants and content modules. Keep this test focused on contract drift, not scoring behavior.</action>
  <verify><automated>npm exec vitest run test/assessment/definition.test.ts</automated></verify>
  <acceptance_criteria>
    - `test/assessment/fixtures.ts` contains `buildUniformAnswers`
    - `test/assessment/fixtures.ts` contains `buildTypeDominantAnswers`
    - `test/assessment/definition.test.ts` contains `describe("assessmentDefinition"`
    - `test/assessment/definition.test.ts` asserts `NEARBY_TYPE_LIMIT`
    - `test/assessment/definition.test.ts` asserts `PHASE_1_PERSISTENCE_SCOPE`
  </acceptance_criteria>
  <done>Automated guards exist for the source-of-truth definition, and the fixture helpers needed by the scoring plan are ready.</done>
</task>

</tasks>

<verification>
- Run `npm exec vitest run test/assessment/definition.test.ts`
- Run `npm run typecheck`
- Confirm the five Korean Likert labels and the `18` question ids exist in `src/content/assessments/ko/v1.ts`
</verification>

<success_criteria>

- All tasks completed
- The authoritative Korean assessment definition, copy version, and scoring policy constants exist in code
- Definition-level tests and reusable fixtures guard contract changes before scoring work begins
- The tie-break rule, normalization formula, nearby-type limit, and persistence scope are explicit and test-covered
  </success_criteria>

<output>
After completion, create `.planning/phases/01-assessment-contract-scoring-core/01-02-SUMMARY.md`
</output>
