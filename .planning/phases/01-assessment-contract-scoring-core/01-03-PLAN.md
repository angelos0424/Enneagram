---
phase: 01-assessment-contract-scoring-core
plan: 03
type: execute
wave: 3
depends_on:
  - "01-02"
files_modified:
  - src/domain/assessment/schema.ts
  - src/domain/assessment/mappings.ts
  - src/domain/assessment/normalization.ts
  - src/domain/assessment/scoring.ts
  - src/app/api/assessments/score/route.ts
  - test/assessment/scoring.test.ts
autonomous: true
requirements:
  - ASMT-02
  - ASMT-03
  - ASMT-04
  - ASMT-05
must_haves:
  truths:
    - "The server can deterministically turn a validated answer payload into a primary type, normalized score distribution, wing, and growth/stress directions."
    - "Equal top raw scores resolve the same way every time, and nearby types preserve ambiguity context instead of discarding it."
    - "A server route, not client code, is the authority for running the scoring engine."
  artifacts:
    - path: "src/domain/assessment/scoring.ts"
      provides: "Pure scoring engine that derives raw scores, normalized scores, primary type, wing, nearby types, and directions"
    - path: "src/domain/assessment/mappings.ts"
      provides: "Fixed enneagram growth/stress direction and wing adjacency maps"
    - path: "src/app/api/assessments/score/route.ts"
      provides: "Server-side scoring entrypoint with payload validation"
    - path: "test/assessment/scoring.test.ts"
      provides: "Deterministic regression coverage for scoring behaviors"
  key_links:
    - from: "src/app/api/assessments/score/route.ts"
      to: "src/domain/assessment/schema.ts"
      via: "Zod request parsing"
      pattern: "submissionSchema.parse|safeParse"
    - from: "src/app/api/assessments/score/route.ts"
      to: "src/domain/assessment/scoring.ts"
      via: "POST handler invokes scoring engine"
      pattern: "scoreAssessment"
    - from: "src/domain/assessment/scoring.ts"
      to: "src/domain/assessment/normalization.ts"
      via: "Raw totals converted to chart-ready distribution"
      pattern: "normalize"
---

<objective>
Implement the deterministic scoring engine and a server-side scoring endpoint on top of the contracts defined in Plan 02.

Purpose: Satisfy the core Phase 1 promise that the server, not the client, computes the canonical result from the versioned questionnaire.
Output: A pure scoring module, fixed type mappings, request validation, and automated regression tests for primary type, score distribution, wing, and direction logic.
</objective>

<execution_context>
@/home/ubuntu/Project/Enneagram/.codex/get-shit-done/workflows/execute-plan.md
@/home/ubuntu/Project/Enneagram/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-assessment-contract-scoring-core/01-RESEARCH.md
@src/domain/assessment/types.ts
@src/domain/assessment/constants.ts
@src/content/assessments/ko/v1.ts
@test/assessment/fixtures.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write scoring regression tests that lock the Phase 1 math rules</name>
  <files>test/assessment/scoring.test.ts</files>
  <read_first>.planning/phases/01-assessment-contract-scoring-core/01-VALIDATION.md, src/domain/assessment/constants.ts, src/content/assessments/ko/v1.ts, test/assessment/fixtures.ts, test/assessment/scoring.test.ts</read_first>
  <action>Create `test/assessment/scoring.test.ts` before writing the scoring implementation. Cover at least these exact behaviors: `highest raw total wins primary type`; `equal top raw totals resolve with PRIMARY_TYPE_TIE_BREAK = lowest-type-id`; `normalized scores use round((rawScore / totalRawScore) * 1000) / 10 and stay chart-ready`; `wing is chosen from the two adjacent types of the primary type, breaking equal adjacent totals with lower-adjacent-type-id`; `growth` and `stress` use fixed maps keyed by primary type`; and `nearbyTypes` keeps exactly the top 3 non-primary candidates sorted by raw score descending then type id ascending. Use fixtures that exercise a dominant result, a balanced tie, and an ambiguous close-result case.</action>
  <verify><automated>npm exec vitest run test/assessment/scoring.test.ts</automated></verify>
  <acceptance_criteria>
    - `test/assessment/scoring.test.ts` contains the text `lowest-type-id`
    - `test/assessment/scoring.test.ts` contains the text `round((rawScore / totalRawScore) * 1000) / 10`
    - `test/assessment/scoring.test.ts` contains `nearbyTypes`
    - `test/assessment/scoring.test.ts` contains assertions for both `growthType` and `stressType`
  </acceptance_criteria>
  <done>The test suite explicitly defines the scoring rules so the implementation cannot drift from the locked Phase 1 contract.</done>
</task>

<task type="auto">
  <name>Task 2: Implement the pure scoring engine and fixed enneagram mappings</name>
  <files>src/domain/assessment/schema.ts, src/domain/assessment/mappings.ts, src/domain/assessment/normalization.ts, src/domain/assessment/scoring.ts</files>
  <read_first>src/domain/assessment/types.ts, src/domain/assessment/constants.ts, src/content/assessments/ko/v1.ts, test/assessment/scoring.test.ts, src/domain/assessment/schema.ts, src/domain/assessment/mappings.ts, src/domain/assessment/normalization.ts, src/domain/assessment/scoring.ts</read_first>
  <action>Implement a framework-free scoring pipeline. In `src/domain/assessment/schema.ts`, define a Zod `assessmentSubmissionSchema` for `{ assessmentVersion: string; answers: Array<{ questionId: string; value: 1|2|3|4|5 }> }`. In `src/domain/assessment/mappings.ts`, encode the fixed direction map `{1:{growth:7,stress:4},2:{growth:4,stress:8},3:{growth:6,stress:9},4:{growth:1,stress:2},5:{growth:8,stress:7},6:{growth:9,stress:3},7:{growth:5,stress:1},8:{growth:2,stress:5},9:{growth:3,stress:6}}` and adjacency map so each type sees its two neighbors in the enneagram ring. In `src/domain/assessment/normalization.ts`, export a helper implementing the exact formula `round((rawScore / totalRawScore) * 1000) / 10`, returning `0` for every type when total raw score is `0`. In `src/domain/assessment/scoring.ts`, validate full question coverage for the requested `assessmentVersion`, sum raw totals by applying the selected tuple element from each question’s `typeWeights`, resolve primary type by highest raw total then lower type id, resolve wing from adjacent types only, derive growth/stress from the fixed map, and return `nearbyTypes` limited to 3 entries. The returned result shape must already contain `assessmentVersion`, `scoringVersion`, and `copyVersion` so Plan 04 can serialize it without adding fields later.</action>
  <verify><automated>npm exec vitest run test/assessment/scoring.test.ts</automated></verify>
  <acceptance_criteria>
    - `src/domain/assessment/mappings.ts` contains `1:{growth:7,stress:4}` or the spaced equivalent
    - `src/domain/assessment/normalization.ts` contains `Math.round((rawScore / totalRawScore) * 1000) / 10`
    - `src/domain/assessment/scoring.ts` contains `NEARBY_TYPE_LIMIT`
    - `src/domain/assessment/scoring.ts` contains logic that sorts by raw score and then type id
    - `src/domain/assessment/scoring.ts` returns `assessmentVersion`, `scoringVersion`, and `copyVersion`
  </acceptance_criteria>
  <done>The scoring domain is deterministic, pure, and fully covered by tests for primary type, score distribution, wing selection, nearby types, and direction mapping.</done>
</task>

<task type="auto">
  <name>Task 3: Expose a server-authoritative scoring endpoint with explicit request validation</name>
  <files>src/app/api/assessments/score/route.ts</files>
  <read_first>src/domain/assessment/schema.ts, src/domain/assessment/scoring.ts, src/app/api/assessments/score/route.ts</read_first>
  <action>Create `src/app/api/assessments/score/route.ts` with a `POST` handler that parses JSON using `assessmentSubmissionSchema`, calls the pure scoring function, and returns `Response.json({ result }, { status: 200 })`. Return `400` with machine-readable error codes for invalid payload shape, unknown `assessmentVersion`, duplicate `questionId`, or incomplete answer coverage. Do not persist anything in this route; this endpoint exists only to make the server the scoring authority in Phase 1.</action>
  <verify><automated>npm exec vitest run test/assessment/scoring.test.ts && npm run typecheck</automated></verify>
  <acceptance_criteria>
    - `src/app/api/assessments/score/route.ts` contains `export async function POST`
    - `src/app/api/assessments/score/route.ts` contains `assessmentSubmissionSchema`
    - `src/app/api/assessments/score/route.ts` contains `status: 400`
    - `src/app/api/assessments/score/route.ts` contains `Response.json({ result }`
  </acceptance_criteria>
  <done>The codebase has a server-side scoring entrypoint that can be consumed later by the assessment flow without moving scoring logic into the client.</done>
</task>

</tasks>

<verification>
- Run `npm exec vitest run test/assessment/scoring.test.ts`
- Run `npm run typecheck`
- Confirm `src/app/api/assessments/score/route.ts` does not import any DB module
</verification>

<success_criteria>

- All tasks completed
- Scoring behavior is deterministic and test-covered
- The equal-top tie-break rule, normalization formula, nearby-type limit, and wing rule are encoded in implementation and tests
- A validated server route computes canonical results without any client-side scoring logic
  </success_criteria>

<output>
After completion, create `.planning/phases/01-assessment-contract-scoring-core/01-03-SUMMARY.md`
</output>
