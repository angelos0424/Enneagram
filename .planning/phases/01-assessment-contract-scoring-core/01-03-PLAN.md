---
phase: 01-assessment-contract-scoring-core
plan: 03
type: execute
wave: 3
depends_on:
  - "01-02"
files_modified:
  - .env.example
  - drizzle.config.ts
  - src/env.ts
  - src/domain/assessment/result-snapshot.ts
  - src/db/schema.ts
  - src/db/client.ts
  - src/db/repositories/assessment-result-repository.ts
  - test/assessment/result-contract.test.ts
autonomous: true
requirements:
  - ASMT-06
must_haves:
  truths:
    - "A scored result can be transformed into a persistence-ready snapshot that carries assessment, scoring, and copy versions together."
    - "The database contract for assessment results exists before public share pages are built."
    - "Phase 1 does not silently drift into Phase 2: it defines schema and repository boundaries without adding share-page retrieval or public-link writes."
  artifacts:
    - path: "src/domain/assessment/result-snapshot.ts"
      provides: "Persistence-ready snapshot builder carrying version trio and scored payload"
    - path: "src/db/schema.ts"
      provides: "Drizzle table contract for versioned assessment results"
    - path: "src/db/repositories/assessment-result-repository.ts"
      provides: "Repository boundary for saving and loading result snapshots"
    - path: "test/assessment/result-contract.test.ts"
      provides: "Regression coverage for version fields and persistence scope"
  key_links:
    - from: "src/domain/assessment/scoring.ts"
      to: "src/domain/assessment/result-snapshot.ts"
      via: "Scored result serialized into persistence draft"
      pattern: "buildAssessmentResultSnapshot"
    - from: "src/domain/assessment/result-snapshot.ts"
      to: "src/db/schema.ts"
      via: "Snapshot field names match Drizzle columns"
      pattern: "assessmentVersion|scoringVersion|copyVersion"
    - from: "src/db/repositories/assessment-result-repository.ts"
      to: "src/db/schema.ts"
      via: "Repository uses assessmentResults table contract"
      pattern: "assessmentResults"
---

<objective>
Define the persistence-ready snapshot contract and Drizzle schema boundary required for immutable result storage, without pulling Phase 2 public-link features into Phase 1.

Purpose: Satisfy ASMT-06 now by locking the data shape that later snapshot persistence will use, while keeping permanent-share behavior in the next phase.
Output: Environment/database scaffolding, a result snapshot builder, a repository boundary, and tests that prove version fields are preserved end to end.
</objective>

<execution_context>
@/home/ubuntu/Project/Enneagram/.codex/get-shit-done/workflows/execute-plan.md
@/home/ubuntu/Project/Enneagram/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-assessment-contract-scoring-core/01-CONTEXT.md
@src/domain/assessment/constants.ts
@src/domain/assessment/scoring.ts
@src/domain/assessment/types.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the persistence-ready result snapshot builder and environment contracts</name>
  <files>.env.example, drizzle.config.ts, src/env.ts, src/domain/assessment/result-snapshot.ts</files>
  <read_first>.planning/phases/01-assessment-contract-scoring-core/01-CONTEXT.md, src/domain/assessment/constants.ts, src/domain/assessment/scoring.ts, .env.example, drizzle.config.ts, src/env.ts, src/domain/assessment/result-snapshot.ts</read_first>
  <action>Lock the Phase 1 persistence boundary exactly as `schema-and-repository-contract-only`, per the constant from Plan 01. Create `.env.example` with `DATABASE_URL=` and `NODE_ENV=` only. In `src/env.ts`, validate those env vars with Zod. In `drizzle.config.ts`, point migrations to `./drizzle` and schema to `./src/db/schema.ts`. In `src/domain/assessment/result-snapshot.ts`, export a `buildAssessmentResultSnapshot` helper that accepts the scored result plus the submitted answers and returns a persistence draft containing `assessmentVersion`, `scoringVersion`, `copyVersion`, `primaryType`, `wingType`, `growthType`, `stressType`, `rawScores`, `normalizedScores`, `nearbyTypes`, `answers`, and `createdAt`. Do not include `shareId`, public URL fields, or a route handler that writes to the database in this phase.</action>
  <verify><automated>npm run typecheck</automated></verify>
  <acceptance_criteria>
    - `.env.example` contains `DATABASE_URL=`
    - `drizzle.config.ts` contains `schema: "./src/db/schema.ts"`
    - `src/domain/assessment/result-snapshot.ts` contains `buildAssessmentResultSnapshot`
    - `src/domain/assessment/result-snapshot.ts` contains `assessmentVersion`, `scoringVersion`, and `copyVersion`
    - `src/domain/assessment/result-snapshot.ts` does not contain `shareId`
  </acceptance_criteria>
  <done>The repo has an explicit persistence draft shape and the minimum environment contract needed to support Drizzle in later phases.</done>
</task>

<task type="auto">
  <name>Task 2: Define the Drizzle schema and repository boundary for immutable assessment results</name>
  <files>src/db/schema.ts, src/db/client.ts, src/db/repositories/assessment-result-repository.ts</files>
  <read_first>src/domain/assessment/result-snapshot.ts, src/env.ts, src/db/schema.ts, src/db/client.ts, src/db/repositories/assessment-result-repository.ts</read_first>
  <action>Create `src/db/schema.ts` with a single `assessmentResults` table containing columns `id`, `assessmentVersion`, `scoringVersion`, `copyVersion`, `primaryType`, `wingType`, `growthType`, `stressType`, `rawScores`, `normalizedScores`, `nearbyTypes`, `answers`, and `createdAt`. Use Drizzle `jsonb` for the structured score fields and answers. In `src/db/client.ts`, create the PostgreSQL/Drizzle client using `DATABASE_URL`. In `src/db/repositories/assessment-result-repository.ts`, define an `AssessmentResultRepository` interface plus a `DrizzleAssessmentResultRepository` implementation skeleton whose public methods are `save(snapshot)` and `findById(id)`. The implementation may include the exact Drizzle insert/select statements, but do not wire this repository into any route yet and do not create a public-share lookup path in Phase 1.</action>
  <verify><automated>npm run typecheck</automated></verify>
  <acceptance_criteria>
    - `src/db/schema.ts` contains `assessmentResults`
    - `src/db/schema.ts` contains `assessmentVersion`, `scoringVersion`, and `copyVersion`
    - `src/db/schema.ts` contains `rawScores` and `normalizedScores`
    - `src/db/repositories/assessment-result-repository.ts` contains `interface AssessmentResultRepository`
    - `src/db/repositories/assessment-result-repository.ts` contains `save(` and `findById(`
  </acceptance_criteria>
  <done>The codebase has a concrete database contract and repository boundary for immutable assessment snapshots, without prematurely adding Phase 2 share behavior.</done>
</task>

<task type="auto">
  <name>Task 3: Add persistence-contract regression tests for ASMT-06</name>
  <files>test/assessment/result-contract.test.ts</files>
  <read_first>.planning/phases/01-assessment-contract-scoring-core/01-VALIDATION.md, src/domain/assessment/constants.ts, src/domain/assessment/result-snapshot.ts, src/db/schema.ts, test/assessment/result-contract.test.ts</read_first>
  <action>Create `test/assessment/result-contract.test.ts` to assert that `buildAssessmentResultSnapshot` always preserves `assessmentVersion`, `scoringVersion`, and `copyVersion`, that the snapshot includes `rawScores`, `normalizedScores`, and `nearbyTypes`, and that the persistence boundary remains Phase-1-scoped by not requiring `shareId` or public URL fields. Add one schema-level assertion that the Drizzle table exposes the same version field names used by the snapshot builder so later writes do not rename them silently.</action>
  <verify><automated>npm exec vitest run test/assessment/result-contract.test.ts && npm run typecheck</automated></verify>
  <acceptance_criteria>
    - `test/assessment/result-contract.test.ts` contains `assessmentVersion`
    - `test/assessment/result-contract.test.ts` contains `scoringVersion`
    - `test/assessment/result-contract.test.ts` contains `copyVersion`
    - `test/assessment/result-contract.test.ts` contains an assertion that `shareId` is absent or undefined
    - `test/assessment/result-contract.test.ts` references `assessmentResults`
  </acceptance_criteria>
  <done>ASMT-06 is protected by automated regression coverage that keeps version fields and persistence scope stable before Phase 2 expands into public snapshots.</done>
</task>

</tasks>

<verification>
- Run `npm exec vitest run test/assessment/result-contract.test.ts`
- Run `npm run typecheck`
- Confirm no file under `src/app/api/` writes to the database in Phase 1
</verification>

<success_criteria>

- All tasks completed
- The scored-result snapshot carries `assessmentVersion`, `scoringVersion`, and `copyVersion`
- The Drizzle schema and repository contract exist for immutable result storage
- Phase 1 remains limited to schema/repository contract work and does not add public share-link behavior
  </success_criteria>

<output>
After completion, create `.planning/phases/01-assessment-contract-scoring-core/01-03-SUMMARY.md`
</output>
