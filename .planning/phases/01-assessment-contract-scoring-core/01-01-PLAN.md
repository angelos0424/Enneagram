---
phase: 01-assessment-contract-scoring-core
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - tsconfig.json
  - next.config.ts
  - next-env.d.ts
  - postcss.config.mjs
  - eslint.config.mjs
  - vitest.config.ts
  - src/app/globals.css
  - src/app/layout.tsx
  - src/app/page.tsx
autonomous: true
requirements:
  - ASMT-01
must_haves:
  truths:
    - "Assessment contract changes are guarded by repeatable type checks and test commands before scoring work begins."
    - "The repo can run the Phase 1 validation commands in a real Next.js and TypeScript workspace instead of relying on placeholder files."
    - "Phase 1 starts from a thin bootstrap shell only, without leaking assessment flow, persistence writes, or result UI into the foundation step."
  artifacts:
    - path: "package.json"
      provides: "Pinned Next.js, TypeScript, and Vitest scripts for Phase 1 validation"
    - path: "vitest.config.ts"
      provides: "Test runner configuration for assessment contract and scoring tests"
    - path: "src/app/page.tsx"
      provides: "Minimal bootstrap app entrypoint confirming the workspace runs"
  key_links:
    - from: "package.json"
      to: "vitest.config.ts"
      via: "test script invokes configured Vitest runner"
      pattern: "test:assessment|vitest run"
    - from: "tsconfig.json"
      to: "src/app/page.tsx"
      via: "TypeScript and src-based app resolution"
      pattern: "\"@/\\*\"|src/app"
    - from: "src/app/globals.css"
      to: "src/app/layout.tsx"
      via: "global stylesheet imported by the root layout"
      pattern: "globals.css"
---

<objective>
Bootstrap the greenfield Next.js/TypeScript workspace and validation harness that later assessment-contract and scoring plans will build on.

Purpose: Keep the environment/setup work separate from the assessment-domain contracts so Phase 1 stays within context budget and the scoring plans can assume a working toolchain.
Output: A runnable Next.js 15 + TypeScript + Vitest baseline with exact scripts, path aliases, and a minimal placeholder app shell.
</objective>

<execution_context>
@/home/ubuntu/Project/Enneagram/.codex/get-shit-done/workflows/execute-plan.md
@/home/ubuntu/Project/Enneagram/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-assessment-contract-scoring-core/01-CONTEXT.md
@.planning/phases/01-assessment-contract-scoring-core/01-RESEARCH.md
@.planning/phases/01-assessment-contract-scoring-core/01-VALIDATION.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bootstrap the minimal Next.js 15 + Vitest workspace for domain-first development</name>
  <files>package.json, tsconfig.json, next.config.ts, next-env.d.ts, postcss.config.mjs, eslint.config.mjs, vitest.config.ts, src/app/globals.css, src/app/layout.tsx, src/app/page.tsx</files>
  <read_first>CLAUDE.md, .planning/ROADMAP.md, .planning/phases/01-assessment-contract-scoring-core/01-RESEARCH.md, .planning/phases/01-assessment-contract-scoring-core/01-VALIDATION.md, package.json</read_first>
  <action>Create a `src/`-based Next.js 15.5.3 application baseline with React 19.1.1, TypeScript 5.9.2, Tailwind CSS 4, Zod 4.1.5, and Vitest 3.2.4. In `package.json`, add exact scripts `dev`, `build`, `lint`, `typecheck`, `test`, and `test:assessment`, and set the Node engine to `>=24.0.0`. Add `vitest.config.ts` to run tests under `test/**/*.test.ts`, configure path alias `@/* -> ./src/*` in `tsconfig.json`, and add a minimal `src/app/page.tsx` placeholder that renders a plain Phase 1 bootstrap message rather than product UI. In `src/app/globals.css`, use Tailwind 4 via `@import "tailwindcss";`. Keep this task intentionally thin: no assessment definition files yet, no scoring logic, no result page, and no database write code.</action>
  <verify><automated>npm run typecheck && npm exec vitest run --passWithNoTests</automated></verify>
  <acceptance_criteria>
    - `package.json` contains `"next": "15.5.3"` and `"vitest": "3.2.4"`
    - `package.json` contains scripts `"typecheck": "tsc --noEmit"` and `"test:assessment": "vitest run test/assessment"`
    - `tsconfig.json` contains the alias path `"@/*": ["./src/*"]`
    - `src/app/page.tsx` contains the exact text `Assessment engine bootstrap`
    - `src/app/globals.css` contains the exact line `@import "tailwindcss";`
  </acceptance_criteria>
  <done>The repo installs as a valid Next.js TypeScript app, baseline validation scripts exist, and later assessment plans can rely on a working toolchain instead of creating it ad hoc.</done>
</task>

</tasks>

<verification>
- Run `npm run typecheck`
- Run `npm exec vitest run --passWithNoTests`
- Confirm `src/app/page.tsx` renders only the bootstrap placeholder and not Phase 3 UI
</verification>

<success_criteria>

- All tasks completed
- The repo has a working Next.js + TypeScript + Vitest baseline
- Phase 1 validation commands can run before assessment-domain files are introduced
- Bootstrap work is isolated from assessment contracts and scoring logic
  </success_criteria>

<output>
After completion, create `.planning/phases/01-assessment-contract-scoring-core/01-01-SUMMARY.md`
</output>
