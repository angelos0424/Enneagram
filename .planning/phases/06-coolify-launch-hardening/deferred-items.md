# Deferred Items

- 2026-03-29: `docker build -t enneagram-phase06 .` surfaced an npm warning that `next@15.5.3` has a known security vulnerability and should be upgraded to a patched release. This plan locked the deployment artifact only and did not widen scope to a framework upgrade.
