---
phase: 06-coolify-launch-hardening
plan: 01
type: execute
wave: 1
depends_on:
  - "05-04"
files_modified:
  - Dockerfile
  - .dockerignore
  - next.config.ts
  - test/ops/deployment-contract.test.ts
autonomous: true
requirements:
  - OPER-01
must_haves:
  truths:
    - "The web app can build as a deterministic standalone container image for Coolify."
    - "The application deployment boundary stays separate from PostgreSQL state instead of bundling database runtime into the app image."
    - "The repository itself declares the production build contract instead of depending on Coolify buildpack inference."
  artifacts:
    - path: "Dockerfile"
      provides: "Explicit production image contract for the Next.js web app"
    - path: ".dockerignore"
      provides: "Deterministic and minimal Docker build context"
    - path: "next.config.ts"
      provides: "Standalone Next.js build output for container runtime"
    - path: "test/ops/deployment-contract.test.ts"
      provides: "Regression coverage for deployment-facing config and artifacts"
  key_links:
    - from: "Dockerfile"
      to: "next.config.ts"
      via: "Container runtime expects `output: \"standalone\"` build artifacts"
      pattern: "standalone|server.js"
---

<objective>
Lock the deterministic Coolify deployment artifact for the existing anonymous Next.js app.

Purpose: Phase 06 cannot harden operations until the repo itself defines how the web application is built and run in production, per OPER-01, without relying on implicit buildpack behavior.
Output: Checked-in Dockerfile, Docker context rules, standalone Next.js output, and deployment contract tests.
</objective>

<execution_context>
@/home/ubuntu/Project/Enneagram/.codex/get-shit-done/workflows/execute-plan.md
@/home/ubuntu/Project/Enneagram/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/06-coolify-launch-hardening/06-RESEARCH.md
@.planning/phases/06-coolify-launch-hardening/06-VALIDATION.md
@package.json
@next.config.ts

<interfaces>
From package.json:
```json
{
  "engines": { "node": ">=24.0.0" },
  "scripts": {
    "build": "next build",
    "typecheck": "tsc --noEmit"
  }
}
```

From next.config.ts:
```ts
const nextConfig: NextConfig = {
  async headers() {
    return [
      {
        source: "/results/:publicId*",
        headers: [{ key: "Referrer-Policy", value: "no-referrer" }],
      },
    ];
  },
};
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define the deployment artifact contract in tests</name>
  <read_first>
    @package.json
    @next.config.ts
    @.planning/phases/06-coolify-launch-hardening/06-RESEARCH.md
  </read_first>
  <files>test/ops/deployment-contract.test.ts</files>
  <acceptance_criteria>
    - The test locks `output: "standalone"` in `next.config.ts`.
    - The test makes explicit that the repo must contain both `Dockerfile` and `.dockerignore`.
    - The test asserts the Docker contract is app-only and does not encode PostgreSQL-in-container assumptions, per OPER-01.
  </acceptance_criteria>
  <action>Start with failing tests that describe the deployment artifact boundary before editing the build config. Make the assertions concrete enough that a missing Dockerfile, missing `.dockerignore`, or non-standalone Next output fails immediately. Preserve the existing public-result `Referrer-Policy` rule while extending the config.</action>
  <verify>
    <automated>npm exec vitest run test/ops/deployment-contract.test.ts</automated>
  </verify>
  <done>The repository has a regression-guarded deployment contract for Dockerfile-based Coolify deploys.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Implement the standalone Docker build path for Coolify</name>
  <read_first>
    @test/ops/deployment-contract.test.ts
    @next.config.ts
  </read_first>
  <files>Dockerfile, .dockerignore, next.config.ts, test/ops/deployment-contract.test.ts</files>
  <acceptance_criteria>
    - `next.config.ts` adds `output: "standalone"` without removing the existing result-route header policy.
    - The Dockerfile is multi-stage, aligned to Node 24, and runs the standalone Next server as the web process.
    - `.dockerignore` excludes transient/dev-only context such as `.git`, `.next`, `node_modules`, and local planning noise.
    - `npm run build` and `docker build -t enneagram-phase06 .` both pass.
  </acceptance_criteria>
  <action>Implement the deterministic Coolify build path per OPER-01. Keep the app image focused on the existing anonymous product and do not introduce compose files, embedded Postgres, or Nixpacks assumptions. The final image should expose a single web process and remain compatible with later health-check wiring.</action>
  <verify>
    <automated>npm exec vitest run test/ops/deployment-contract.test.ts && npm run build && docker build -t enneagram-phase06 .</automated>
  </verify>
  <done>The repo can produce a deterministic Coolify-ready container image for the web app.</done>
</task>

</tasks>

<verification>
- Run `npm exec vitest run test/ops/deployment-contract.test.ts`
- Run `npm run build`
- Run `docker build -t enneagram-phase06 .`
- Confirm `next.config.ts` still contains the route-scoped `Referrer-Policy: no-referrer` header for `/results/:publicId*`
</verification>

<success_criteria>
- OPER-01 has a checked-in, deterministic web-app deployment artifact
- The app image is clearly separate from PostgreSQL state ownership
</success_criteria>

<output>
After completion, create `.planning/phases/06-coolify-launch-hardening/06-01-SUMMARY.md`
</output>
