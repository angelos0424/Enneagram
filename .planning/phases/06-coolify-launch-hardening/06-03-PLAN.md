---
phase: 06-coolify-launch-hardening
plan: 03
type: execute
wave: 3
depends_on:
  - "06-02"
files_modified:
  - src/app/layout.tsx
  - src/app/results/[publicId]/snapshot-metadata.ts
  - src/app/results/[publicId]/page.tsx
  - src/app/results/[publicId]/opengraph-image.tsx
  - test/assessment/public-result.test.ts
  - test/e2e/public-result.spec.ts
autonomous: true
requirements:
  - OPER-04
must_haves:
  truths:
    - "Public result pages generate server-owned share metadata and OG images for preview crawlers."
    - "The share-preview upgrade does not weaken the current privacy defaults for public result pages."
    - "Preview content stays limited to immutable result summary data rather than raw answers or admin-only fields."
  artifacts:
    - path: "src/app/results/[publicId]/snapshot-metadata.ts"
      provides: "Privacy-safe metadata builder with Open Graph and Twitter fields"
    - path: "src/app/results/[publicId]/opengraph-image.tsx"
      provides: "Result-specific OG image route for share previews"
    - path: "test/assessment/public-result.test.ts"
      provides: "Regression coverage for noindex plus metadata/OG behavior"
    - path: "test/e2e/public-result.spec.ts"
      provides: "Browser proof that public-result behavior survives the metadata changes"
  key_links:
    - from: "src/app/results/[publicId]/snapshot-metadata.ts"
      to: "src/env.ts"
      via: "Metadata uses `APP_ORIGIN` to emit absolute URLs"
      pattern: "APP_ORIGIN|metadataBase"
    - from: "src/app/results/[publicId]/page.tsx"
      to: "src/app/results/[publicId]/opengraph-image.tsx"
      via: "Server-generated metadata references the route-scoped OG image"
      pattern: "openGraph|twitter|opengraph-image"
---

<objective>
Add server-generated share-preview metadata to immutable public result pages without breaking privacy defaults.

Purpose: OPER-04 requires real crawler-visible metadata, not client-side tags. This plan upgrades the existing public result route to emit Open Graph and Twitter metadata plus an OG image while preserving the anonymous, privacy-first product behavior already established in earlier phases.
Output: `APP_ORIGIN`-backed metadata, route-scoped OG image generation, and regression coverage for preview-safe public result pages.
</objective>

<execution_context>
@/home/ubuntu/Project/Enneagram/.codex/get-shit-done/workflows/execute-plan.md
@/home/ubuntu/Project/Enneagram/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-coolify-launch-hardening/06-RESEARCH.md
@.planning/phases/06-coolify-launch-hardening/06-VALIDATION.md
@src/app/results/[publicId]/page.tsx
@src/app/results/[publicId]/snapshot-metadata.ts
@test/assessment/public-result.test.ts
@test/e2e/public-result.spec.ts
@.planning/phases/02-persistent-result-snapshots/02-02-SUMMARY.md
@.planning/phases/04-result-interpretation-share-loop/04-03-SUMMARY.md

<interfaces>
From src/app/results/[publicId]/page.tsx:
```ts
export async function generateMetadata({
  params,
}: PublicResultPageProps) {
  const { publicId } = await params;
  return buildSnapshotMetadata(publicId);
}
```

From src/app/results/[publicId]/snapshot-metadata.ts:
```ts
export function buildSnapshotMetadata(publicId: string): Metadata {
  return {
    title: "에니어그램 결과 스냅샷",
    description: `공개 결과 스냅샷 ${publicId}`,
    robots: { index: false, follow: false },
  };
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define the public-result metadata and OG contract in tests</name>
  <read_first>
    @src/app/results/[publicId]/snapshot-metadata.ts
    @src/app/results/[publicId]/page.tsx
    @test/assessment/public-result.test.ts
  </read_first>
  <files>test/assessment/public-result.test.ts</files>
  <acceptance_criteria>
    - Tests define that public result metadata still includes `robots.index = false` and `robots.follow = false`.
    - Tests define absolute `metadataBase`, `openGraph`, and `twitter` fields sourced from immutable snapshot data and `APP_ORIGIN`.
    - Tests define that preview metadata/OG image content excludes raw answers and admin tokens.
  </acceptance_criteria>
  <action>Extend the existing public-result test suite first. The failing tests should lock the privacy-preserving preview behavior before implementation: absolute URLs, snapshot-derived title/description, route-scoped OG image references, and no leakage of answer payloads or admin-only tokens. Keep the share-preview surface additive to the current public result route.</action>
  <verify>
    <automated>npm exec vitest run test/assessment/public-result.test.ts</automated>
  </verify>
  <done>The preview contract is explicit and regression-guarded before any metadata files change.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Implement server-generated result metadata and the OG image route</name>
  <read_first>
    @src/app/results/[publicId]/page.tsx
    @src/app/results/[publicId]/snapshot-metadata.ts
    @test/assessment/public-result.test.ts
  </read_first>
  <files>src/app/layout.tsx, src/app/results/[publicId]/snapshot-metadata.ts, src/app/results/[publicId]/page.tsx, src/app/results/[publicId]/opengraph-image.tsx, test/assessment/public-result.test.ts, test/e2e/public-result.spec.ts</files>
  <acceptance_criteria>
    - The metadata builder emits absolute Open Graph and Twitter metadata using `APP_ORIGIN`.
    - `src/app/results/[publicId]/opengraph-image.tsx` renders a privacy-safe summary card from the stored snapshot.
    - Existing result-page behavior and share actions continue to work after the metadata changes.
    - `npm run build` passes with the new metadata route.
  </acceptance_criteria>
  <action>Implement OPER-04 with route-level server metadata. Use the existing immutable snapshot repository boundary rather than rescoring or exposing raw payloads. If layout-level metadata needs adjustment to support `metadataBase`, keep it minimal and do not weaken the result-page privacy defaults from Phase 02. Reuse the current public-result copy and sharing patterns instead of inventing a new share surface.</action>
  <verify>
    <automated>npm exec vitest run test/assessment/public-result.test.ts && npm run typecheck && npm run build && npx playwright test test/e2e/public-result.spec.ts</automated>
  </verify>
  <done>Public result links now produce crawler-visible server metadata and OG previews while staying privacy-first.</done>
</task>

</tasks>

<verification>
- Run `npm exec vitest run test/assessment/public-result.test.ts`
- Run `npm run typecheck`
- Run `npm run build`
- Run `npx playwright test test/e2e/public-result.spec.ts`
- Confirm metadata still declares `robots.noindex`
- Confirm OG/Twitter fields and OG image URLs are absolute and derived from the stored result snapshot
</verification>

<success_criteria>
- OPER-04 is satisfied through server-generated metadata and OG images
- Public result previews remain immutable and privacy-first
</success_criteria>

<output>
After completion, create `.planning/phases/06-coolify-launch-hardening/06-03-SUMMARY.md`
</output>
