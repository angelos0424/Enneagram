---
phase: 06-coolify-launch-hardening
plan: 04
type: execute
wave: 4
depends_on:
  - "06-01"
  - "06-02"
  - "06-03"
files_modified:
  - docs/operations/postgres-backup-restore.md
  - docs/operations/postgres-restore-checklist.md
  - scripts/ops/rehearse-postgres-restore.sh
  - test/ops/postgres-restore-runbook.test.ts
autonomous: false
requirements:
  - OPER-03
user_setup:
  - service: coolify
    why: "Actual database backup schedule and restore target live in the operator-managed Coolify instance"
    dashboard_config:
      - task: "Configure scheduled backups on the Coolify PostgreSQL service"
        location: "Coolify Database -> Backups"
      - task: "Provision a non-production PostgreSQL target for restore rehearsal"
        location: "Coolify Database resources or equivalent isolated PostgreSQL target"
  - service: s3-compatible-storage
    why: "Scheduled PostgreSQL backups need external object storage"
    env_vars:
      - name: "Backup bucket credentials and destination path"
        source: "Operator-managed S3-compatible provider"
must_haves:
  truths:
    - "Operators have an explicit backup configuration and restore drill for the PostgreSQL data that backs result snapshots, drafts, and admin stats events."
    - "Recovery is verified against the real application tables instead of being assumed from a backup schedule."
    - "The launch runbook keeps anonymous/public behavior intact while focusing only on operational recovery."
  artifacts:
    - path: "docs/operations/postgres-backup-restore.md"
      provides: "Backup policy, dump expectations, and restore runbook"
    - path: "docs/operations/postgres-restore-checklist.md"
      provides: "Operator checklist and evidence log for restore rehearsal"
    - path: "scripts/ops/rehearse-postgres-restore.sh"
      provides: "CLI helper for non-production restore rehearsal and post-restore table checks"
    - path: "test/ops/postgres-restore-runbook.test.ts"
      provides: "Regression coverage that the runbook/script reference actual app tables and recovery steps"
  key_links:
    - from: "scripts/ops/rehearse-postgres-restore.sh"
      to: "docs/operations/postgres-backup-restore.md"
      via: "Runbook and script must stay in sync on dump format, target version, and verification queries"
      pattern: "pg_restore|assessment_results|assessment_draft_sessions|admin_stats_events"
    - from: "docs/operations/postgres-restore-checklist.md"
      to: "Coolify PostgreSQL service"
      via: "Restore rehearsal records backup source, target DB, verification timestamp, and operator outcome"
      pattern: "backup id|restore target|verified at"
---

<objective>
Make backup and restore operationally explicit and verifiable for launch.

Purpose: OPER-03 is the real launch gate for a product built on permanent anonymous result links. Backup existence alone is insufficient; the repo needs a restore runbook, a rehearsal helper, and a concrete restore drill checkpoint against the actual PostgreSQL tables the app depends on.
Output: Backup/restore runbook, restore checklist, rehearsal helper script, regression coverage, and an operator restore-drill checkpoint.
</objective>

<execution_context>
@/home/ubuntu/Project/Enneagram/.codex/get-shit-done/workflows/execute-plan.md
@/home/ubuntu/Project/Enneagram/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-coolify-launch-hardening/06-RESEARCH.md
@.planning/phases/06-coolify-launch-hardening/06-VALIDATION.md
@src/db/schema.ts
@drizzle
@.planning/phases/02-persistent-result-snapshots/02-02-SUMMARY.md
@.planning/phases/05-aggregate-admin-stats/05-02-SUMMARY.md

<interfaces>
From the current persisted app state:
```ts
assessment_results
assessment_draft_sessions
admin_stats_events
```

From research-backed restore guidance:
```bash
pg_restore --verbose --clean -h <host> -U <user> -d <database> <dump-file>
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define the backup and restore contract in tests and operator artifacts</name>
  <read_first>
    @src/db/schema.ts
    @.planning/phases/06-coolify-launch-hardening/06-RESEARCH.md
  </read_first>
  <files>docs/operations/postgres-backup-restore.md, docs/operations/postgres-restore-checklist.md, scripts/ops/rehearse-postgres-restore.sh, test/ops/postgres-restore-runbook.test.ts</files>
  <acceptance_criteria>
    - Tests define that the runbook and rehearsal script reference the real app tables `assessment_results`, `assessment_draft_sessions`, and `admin_stats_events`.
    - Tests define that the runbook covers scheduled backups, restore target constraints, and post-restore verification queries.
    - The checklist reserves fields for backup source, restore target, operator, timestamp, and outcome so the drill leaves evidence.
  </acceptance_criteria>
  <action>Start by writing failing tests for the recovery contract. Then add the operator artifacts and CLI helper shell that the tests target. This plan must stay concrete: scheduled backups, explicit restore command flow, and post-restore table checks are required. Do not reduce OPER-03 to narrative documentation with no executable or checklist support.</action>
  <verify>
    <automated>npm exec vitest run test/ops/postgres-restore-runbook.test.ts && npm run typecheck</automated>
  </verify>
  <done>The repo contains a regression-guarded recovery contract tied to the actual PostgreSQL tables the app uses.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Rehearse a non-production restore using the runbook</name>
  <what-built>Backup/restore runbook, restore checklist, and rehearsal helper tied to the app’s real PostgreSQL tables.</what-built>
  <action>Use the repo-owned runbook and rehearsal helper to prove a real backup can be restored into a non-production PostgreSQL target. This checkpoint exists because OPER-03 is not satisfied by docs or backup scheduling alone; the operator must verify that the recovery path works against a real dump and target database.</action>
  <how-to-verify>
    1. In Coolify, trigger or locate a recent PostgreSQL backup for the production-shaped database service.
    2. Provision an isolated non-production PostgreSQL target on the same major version when possible.
    3. Follow `docs/operations/postgres-backup-restore.md` and run `scripts/ops/rehearse-postgres-restore.sh` against the non-production target.
    4. Confirm the script or documented verification queries succeed for `assessment_results`, `assessment_draft_sessions`, and `admin_stats_events`.
    5. Record the backup identifier, restore target, timestamp, operator name, and pass/fail outcome in `docs/operations/postgres-restore-checklist.md`.
  </how-to-verify>
  <verify>
    <automated>npm exec vitest run test/ops/postgres-restore-runbook.test.ts && npm run build</automated>
  </verify>
  <done>A non-production restore drill has been completed and recorded with enough evidence to trust the backup/recovery path for launch.</done>
  <resume-signal>Type `approved` with the recorded restore timestamp, or describe the failure details to replan/fix the runbook.</resume-signal>
</task>

</tasks>

<verification>
- Run `npm exec vitest run test/ops/postgres-restore-runbook.test.ts`
- Run `npm run typecheck`
- Run `npm run build`
- Complete one non-production restore drill and record the outcome in `docs/operations/postgres-restore-checklist.md`
- Confirm the drill verifies `assessment_results`, `assessment_draft_sessions`, and `admin_stats_events`
</verification>

<success_criteria>
- OPER-03 is backed by both repo-owned recovery artifacts and one rehearsed restore path
- Backup/restore is explicit, evidence-bearing, and tied to the actual PostgreSQL state the product depends on
</success_criteria>

<output>
After completion, create `.planning/phases/06-coolify-launch-hardening/06-04-SUMMARY.md`
</output>
