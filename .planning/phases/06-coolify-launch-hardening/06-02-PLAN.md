---
phase: 06-coolify-launch-hardening
plan: 02
type: execute
wave: 2
depends_on:
  - "06-01"
files_modified:
  - src/env.ts
  - .env.example
  - src/app/api/health/route.ts
  - docs/operations/coolify-deploy.md
  - test/ops/deployment-contract.test.ts
  - test/ops/health-route.test.ts
autonomous: true
requirements:
  - OPER-01
must_haves:
  truths:
    - "Coolify can supply the runtime values the app needs, including a production origin for absolute metadata URLs."
    - "The application exposes a cheap process-level health endpoint that does not flap with transient PostgreSQL issues."
    - "Operators have a repo-owned deployment runbook for the separated web-app plus PostgreSQL topology."
  artifacts:
    - path: "src/env.ts"
      provides: "Validated runtime env contract including production origin"
    - path: "src/app/api/health/route.ts"
      provides: "Lightweight liveness endpoint for Coolify health checks"
    - path: "docs/operations/coolify-deploy.md"
      provides: "Coolify web-app and PostgreSQL deployment runbook"
    - path: "test/ops/health-route.test.ts"
      provides: "Regression coverage for process-only health semantics"
  key_links:
    - from: "src/app/api/health/route.ts"
      to: "docs/operations/coolify-deploy.md"
      via: "Coolify health checks target `/api/health` and expect a process-level 200 response"
      pattern: "api/health|200"
    - from: "src/env.ts"
      to: "docs/operations/coolify-deploy.md"
      via: "Runbook maps validated env names into Coolify application settings"
      pattern: "APP_ORIGIN|DATABASE_URL|ADMIN_PASSWORD|ADMIN_SESSION_SECRET"
---

<objective>
Define the runtime contract Coolify will operate against.

Purpose: The deployment artifact from Plan 06-01 is not enough on its own; the running service still needs an explicit env contract, a stable health target, and a runbook that matches the separated app/database topology required by OPER-01.
Output: Production origin env validation, `/api/health`, deployment runbook, and env/health regression tests.
</objective>

<execution_context>
@/home/ubuntu/Project/Enneagram/.codex/get-shit-done/workflows/execute-plan.md
@/home/ubuntu/Project/Enneagram/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-coolify-launch-hardening/06-RESEARCH.md
@.planning/phases/06-coolify-launch-hardening/06-VALIDATION.md
@src/env.ts
@.env.example
@next.config.ts

<interfaces>
From src/env.ts:
```ts
const envSchema = z.object({
  DATABASE_URL: z.string().min(1),
  NODE_ENV: z.enum(["development", "test", "production"]),
  ADMIN_PASSWORD: z.string().min(8),
  ADMIN_SESSION_SECRET: z.string().min(32),
});

export function getEnv(source: NodeJS.ProcessEnv = process.env): AppEnv;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Lock the runtime env and health contract in tests</name>
  <read_first>
    @src/env.ts
    @.env.example
    @.planning/phases/06-coolify-launch-hardening/06-RESEARCH.md
  </read_first>
  <files>test/ops/deployment-contract.test.ts, test/ops/health-route.test.ts</files>
  <acceptance_criteria>
    - Tests make `APP_ORIGIN` required for production-facing metadata generation per OPER-01.
    - Tests make explicit that `/api/health` returns process-level success without querying PostgreSQL.
    - Tests lock the expected response shape for the health route so Coolify configuration remains stable.
  </acceptance_criteria>
  <action>Extend the ops test surface before implementation. Reuse the deployment-contract suite for env assertions and add a dedicated health-route suite that would fail if the route starts depending on database reachability. This plan intentionally keeps the anonymous product behavior unchanged while making the running service operable.</action>
  <verify>
    <automated>npm exec vitest run test/ops/deployment-contract.test.ts test/ops/health-route.test.ts</automated>
  </verify>
  <done>The runtime contract is explicit enough to wire Coolify safely before code and docs are updated.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Implement the production origin env, health endpoint, and Coolify runbook</name>
  <read_first>
    @test/ops/deployment-contract.test.ts
    @test/ops/health-route.test.ts
    @src/env.ts
  </read_first>
  <files>src/env.ts, .env.example, src/app/api/health/route.ts, docs/operations/coolify-deploy.md, test/ops/deployment-contract.test.ts, test/ops/health-route.test.ts</files>
  <acceptance_criteria>
    - `src/env.ts` validates `APP_ORIGIN` in addition to the existing secrets and DB URL.
    - `.env.example` documents the launch-required env surface without adding unnecessary variables.
    - `GET /api/health` returns a lightweight JSON payload with a stable 200 status and no database query.
    - `docs/operations/coolify-deploy.md` explains the separated web-app/PostgreSQL topology, health check target, exposed port, and env mapping.
  </acceptance_criteria>
  <action>Implement the runtime contract for OPER-01. Use `APP_ORIGIN` as the explicit production origin env because the repo currently lacks any absolute-origin metadata source. Keep the health route process-level only, and document the exact Coolify settings operators must apply for the app and separate PostgreSQL service. Do not loosen the existing privacy posture or add new product features.</action>
  <verify>
    <automated>npm exec vitest run test/ops/deployment-contract.test.ts test/ops/health-route.test.ts && npm run typecheck && npm run build</automated>
  </verify>
  <done>The app now exposes a Coolify-safe runtime contract with explicit env mapping and a stable health endpoint.</done>
</task>

</tasks>

<verification>
- Run `npm exec vitest run test/ops/deployment-contract.test.ts test/ops/health-route.test.ts`
- Run `npm run typecheck`
- Run `npm run build`
- Confirm the health route response stays process-only and does not include live database status
- Confirm the runbook maps `APP_ORIGIN`, `DATABASE_URL`, `ADMIN_PASSWORD`, and `ADMIN_SESSION_SECRET` into Coolify settings
</verification>

<success_criteria>
- OPER-01 includes runtime env and health boundaries, not just a container build
- Operators have a repo-owned Coolify deployment runbook for the separated app and PostgreSQL services
</success_criteria>

<output>
After completion, create `.planning/phases/06-coolify-launch-hardening/06-02-SUMMARY.md`
</output>
