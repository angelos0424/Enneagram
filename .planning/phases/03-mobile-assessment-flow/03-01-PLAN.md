---
phase: 03-mobile-assessment-flow
plan: 01
type: execute
wave: 1
depends_on:
  - "02-02"
files_modified:
  - src/app/page.tsx
  - src/app/layout.tsx
  - src/app/globals.css
  - src/features/assessment/types.ts
  - src/features/assessment/assessment-flow.ts
  - src/features/assessment/assessment-experience.tsx
  - test/assessment/mobile-flow.test.ts
autonomous: true
requirements:
  - FLOW-01
  - FLOW-02
  - FLOW-03
must_haves:
  truths:
    - "An anonymous user can open `/` and immediately start the assessment without login or setup."
    - "The assessment renders a mobile-first question flow that uses the authoritative Korean question order and likert choices."
    - "The user can always see current progress and cannot reach an enabled final submit action until every required question has an answer."
  artifacts:
    - path: "src/app/page.tsx"
      provides: "Assessment entry route wired to the Phase 3 client experience"
    - path: "src/features/assessment/assessment-experience.tsx"
      provides: "Mobile-first anonymous assessment UI shell"
    - path: "src/features/assessment/assessment-flow.ts"
      provides: "Pure progress, navigation, and completion helpers keyed by question id"
    - path: "test/assessment/mobile-flow.test.ts"
      provides: "Regression coverage for start, progress visibility, and submit gating"
  key_links:
    - from: "src/features/assessment/assessment-experience.tsx"
      to: "src/content/assessments/ko/v1.ts"
      via: "Direct use of assessmentDefinition and likertOptions"
      pattern: "assessmentDefinition|likertOptions"
    - from: "src/features/assessment/assessment-flow.ts"
      to: "src/domain/assessment/schema.ts"
      via: "Client answer shape mirrors the server submission contract"
      pattern: "questionId|value|assessmentVersion"
    - from: "src/app/page.tsx"
      to: "src/features/assessment/assessment-experience.tsx"
      via: "Home route delegates to the assessment client shell"
      pattern: "AssessmentExperience"
---

<objective>
Create the first anonymous mobile assessment surface on `/`, with a contract-driven question flow, visible progress, and a disabled-until-complete finish state that stays strictly inside Phase 3 scope.

Purpose: Satisfy FLOW-01 through FLOW-03 before draft recovery and submission are layered on top, while keeping the client aligned with the Phase 1 assessment contract and the Phase 2 result destination.
Output: A mobile-first assessment page, reusable progress helpers, and regression tests that lock anonymous entry, question progression, and submit gating.
</objective>

<execution_context>
@/home/ubuntu/Project/Enneagram/.codex/get-shit-done/workflows/execute-plan.md
@/home/ubuntu/Project/Enneagram/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-mobile-assessment-flow/03-RESEARCH.md
@.planning/phases/03-mobile-assessment-flow/03-VALIDATION.md
@.planning/phases/02-persistent-result-snapshots/02-02-SUMMARY.md
@src/app/page.tsx
@src/app/layout.tsx
@src/app/globals.css
@src/content/assessments/ko/v1.ts
@src/domain/assessment/schema.ts

<interfaces>
From `src/content/assessments/ko/v1.ts`:
```typescript
export const likertOptions = [
  { value: 1, label: "전혀 나와 맞지 않는다" },
  { value: 2, label: "별로 맞지 않는다" },
  { value: 3, label: "반반이다" },
  { value: 4, label: "꽤 맞는다" },
  { value: 5, label: "매우 잘 맞는다" },
] as const;

export const assessmentDefinition = {
  version: ASSESSMENT_VERSION,
  locale: "ko-KR",
  scoringVersion: SCORING_VERSION,
  copyVersion: COPY_VERSION,
  likertOptions,
  questions: assessmentQuestions,
} as const;
```

From `src/domain/assessment/schema.ts`:
```typescript
export type AssessmentSubmission = {
  assessmentVersion: string;
  answers: Array<{ questionId: string; value: 1 | 2 | 3 | 4 | 5 }>;
};
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define the client-side assessment flow contracts and mobile-flow tests</name>
  <read_first>
    @src/content/assessments/ko/v1.ts
    @src/domain/assessment/schema.ts
    @test/assessment/score-route.test.ts
  </read_first>
  <files>src/features/assessment/types.ts, src/features/assessment/assessment-flow.ts, test/assessment/mobile-flow.test.ts</files>
  <acceptance_criteria>
    - `test/assessment/mobile-flow.test.ts` exists and covers anonymous start, answered-count progress, and disabled submit before completion.
    - `src/features/assessment/assessment-flow.ts` exports helpers that derive progress and completion from `assessmentDefinition.questions` without duplicating a second question list.
    - `src/features/assessment/types.ts` defines the client answer/draft-facing types using `questionId`, `value`, and `assessmentVersion` names that stay compatible with `AssessmentSubmission`.
  </acceptance_criteria>
  <action>Create `src/features/assessment/types.ts` as the Phase 3 client contract for answer maps, progress state, and any tiny helper types needed by later draft/submit plans. Create `src/features/assessment/assessment-flow.ts` with pure helpers that derive question order, answered count, progress label data, current-question navigation, and completion state directly from `assessmentDefinition`; do not create a duplicate JSON payload layer or hardcode question counts. Start with failing coverage in `test/assessment/mobile-flow.test.ts` for FLOW-01 through FLOW-03 so later UI work builds against an explicit contract instead of inferred behavior.</action>
  <verify>
    <automated>npm exec vitest run test/assessment/mobile-flow.test.ts</automated>
  </verify>
  <done>Phase 3 has a test-locked client contract for anonymous start, mobile question progression, and progress visibility that later plans can extend without re-deciding the shape.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Replace the bootstrap home page with the mobile-first assessment shell</name>
  <read_first>
    @src/app/page.tsx
    @src/app/layout.tsx
    @src/app/globals.css
    @src/content/assessments/ko/v1.ts
    @src/features/assessment/assessment-flow.ts
    @test/assessment/mobile-flow.test.ts
  </read_first>
  <files>src/app/page.tsx, src/app/layout.tsx, src/app/globals.css, src/features/assessment/assessment-experience.tsx, test/assessment/mobile-flow.test.ts</files>
  <acceptance_criteria>
    - `/` renders an anonymous assessment entry screen instead of the bootstrap placeholder.
    - The UI shows one mobile-sized question at a time with visible progress text or bar and likert answer controls sourced from `likertOptions`.
    - A final CTA exists but remains disabled until all required questions are answered.
    - `npm run typecheck` passes after the shell is wired.
  </acceptance_criteria>
  <action>Create `src/features/assessment/assessment-experience.tsx` as the client component mounted by `src/app/page.tsx`. The experience must provide immediate anonymous entry, a mobile-first question card layout, visible progress, back/next behavior, and clear selected-state rendering for the five likert values from the authoritative content module. Update `src/app/layout.tsx` and `src/app/globals.css` only as needed to replace the bootstrap metadata and establish a mobile-safe visual foundation for this flow; keep styling intentional and narrow-screen friendly, but do not introduce Phase 4 result/share UI. Reuse the Task 1 helpers instead of recomputing progress inline.</action>
  <verify>
    <automated>npm exec vitest run test/assessment/mobile-flow.test.ts && npm run typecheck</automated>
  </verify>
  <done>The home route is now a working anonymous mobile assessment shell with visible progress and gated completion behavior, backed by passing tests and typecheck.</done>
</task>

</tasks>

<verification>
- Run `npm exec vitest run test/assessment/mobile-flow.test.ts`
- Run `npm run typecheck`
- Confirm `src/app/page.tsx` no longer renders `Assessment engine bootstrap`
- Confirm `src/features/assessment/assessment-experience.tsx` imports `assessmentDefinition` or `likertOptions` directly from the Phase 1 source of truth
</verification>

<success_criteria>
- FLOW-01 is satisfied by a direct anonymous start on `/`
- FLOW-02 is satisfied by a mobile-first question UI using the canonical Korean content
- FLOW-03 is satisfied by explicit progress visibility and a completion gate tied to required-answer coverage
</success_criteria>

<output>
After completion, create `.planning/phases/03-mobile-assessment-flow/03-01-SUMMARY.md`
</output>
