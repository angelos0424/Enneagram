---
phase: 03-mobile-assessment-flow
plan: 04
type: execute
wave: 4
depends_on:
  - "03-03"
files_modified:
  - src/app/api/assessments/score/route.ts
  - src/app/api/assessment-session/draft/route.ts
  - src/db/repositories/assessment-draft-session-repository.ts
  - src/domain/assessment/draft-session.ts
  - src/features/assessment/submit-assessment.ts
  - src/features/assessment/use-assessment-draft.ts
  - src/features/assessment/assessment-experience.tsx
  - test/assessment/assessment-submit.test.ts
  - test/assessment/assessment-session-route.test.ts
  - test/assessment/score-route.test.ts
  - test/assessment/mobile-flow.test.ts
  - test/e2e/mobile-assessment.spec.ts
autonomous: true
requirements:
  - FLOW-05
must_haves:
  truths:
    - "Once every required question is answered, the user can submit the assessment from the mobile flow."
    - "Successful submission persists the immutable result and finalizes the canonical anonymous draft session on the server before redirect."
    - "The client redirects using the `publicResult.href` returned by the server and preserves the draft when submit fails."
  artifacts:
    - path: "src/app/api/assessments/score/route.ts"
      provides: "Server-authoritative submit route that also finalizes the anonymous draft session"
    - path: "src/db/repositories/assessment-draft-session-repository.ts"
      provides: "Canonical draft cleanup operation for successful submit finalization"
    - path: "src/features/assessment/submit-assessment.ts"
      provides: "Typed client submit boundary using the existing score route response contract"
    - path: "test/assessment/assessment-submit.test.ts"
      provides: "Regression coverage for submit success/failure, redirect contract, and draft finalization behavior"
    - path: "test/e2e/mobile-assessment.spec.ts"
      provides: "Browser coverage for completed submit redirect and post-submit cleanup behavior"
  key_links:
    - from: "src/app/api/assessments/score/route.ts"
      to: "src/db/repositories/assessment-draft-session-repository.ts"
      via: "Successful scoring clears or finalizes the canonical draft session on the server"
      pattern: "deleteDraft|clearDraft|finalize"
    - from: "src/app/api/assessments/score/route.ts"
      to: "src/domain/assessment/draft-session.ts"
      via: "Submit route reads the anonymous session cookie through the same helper boundary as draft routes"
      pattern: "assessment_session|cookies\\("
    - from: "src/features/assessment/submit-assessment.ts"
      to: "src/app/api/assessments/score/route.ts"
      via: "Client uses the existing score route and consumes `publicResult.href`"
      pattern: "publicResult.href"
    - from: "test/e2e/mobile-assessment.spec.ts"
      to: "src/features/assessment/assessment-experience.tsx"
      via: "Browser test proves complete submit reaches the stored result page and starts fresh afterward"
      pattern: "submits completed assessment|redirect"
---

<objective>
Complete the mobile assessment flow by connecting the client submit path to the existing score route, adding canonical draft finalization on the server, and verifying that successful submission redirects to the persisted public result while failed submission preserves retryable state.

Purpose: Satisfy FLOW-05 without violating the anonymous-session architecture from Plan 03, so submit success cleans up the canonical draft row and cookie-backed session state instead of pretending client-only cleanup is authoritative.
Output: Submit route finalization logic, client submit/error wiring, and contract plus browser coverage for redirect and post-submit cleanup.
</objective>

<execution_context>
@/home/ubuntu/Project/Enneagram/.codex/get-shit-done/workflows/execute-plan.md
@/home/ubuntu/Project/Enneagram/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-mobile-assessment-flow/03-RESEARCH.md
@.planning/phases/03-mobile-assessment-flow/03-VALIDATION.md
@/home/ubuntu/Project/Enneagram/CLAUDE.md
@.planning/phases/02-persistent-result-snapshots/02-02-SUMMARY.md
@src/app/api/assessments/score/route.ts
@src/app/api/assessment-session/draft/route.ts
@src/db/repositories/assessment-draft-session-repository.ts
@src/domain/assessment/draft-session.ts
@src/domain/assessment/schema.ts
@src/features/assessment/assessment-experience.tsx
@src/features/assessment/use-assessment-draft.ts
@test/e2e/mobile-assessment.spec.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend the server submit path to finalize canonical draft sessions on success</name>
  <read_first>
    @src/app/api/assessments/score/route.ts
    @src/db/repositories/assessment-draft-session-repository.ts
    @src/domain/assessment/draft-session.ts
    @src/domain/assessment/schema.ts
    @test/assessment/score-route.test.ts
    @test/assessment/assessment-session-route.test.ts
  </read_first>
  <files>src/app/api/assessments/score/route.ts, src/app/api/assessment-session/draft/route.ts, src/db/repositories/assessment-draft-session-repository.ts, src/domain/assessment/draft-session.ts, test/assessment/score-route.test.ts, test/assessment/assessment-session-route.test.ts, test/assessment/assessment-submit.test.ts</files>
  <acceptance_criteria>
    - On a successful score-route response, the canonical anonymous draft is deleted or marked finalized through the server repository boundary.
    - The submit path reads the anonymous cookie session through the shared helper boundary instead of ad hoc cookie parsing.
    - Failed submits do not clear the canonical draft.
    - Tests cover both success cleanup and failure preservation.
  </acceptance_criteria>
  <action>Update `src/app/api/assessments/score/route.ts` so the existing authoritative scoring/persistence route also finalizes the anonymous draft session from Plan 03 when a submit succeeds. Reuse the shared cookie helper boundary from `src/domain/assessment/draft-session.ts` and add whatever repository method is required on `src/db/repositories/assessment-draft-session-repository.ts` to delete or finalize the canonical draft row. If a small route adjustment is needed in `src/app/api/assessment-session/draft/route.ts` to support explicit server cleanup semantics, include it here. Start by adding failing tests that prove successful submit clears the canonical draft while failed submit leaves it intact.</action>
  <verify>
    <automated>npm exec vitest run test/assessment/score-route.test.ts test/assessment/assessment-session-route.test.ts test/assessment/assessment-submit.test.ts</automated>
  </verify>
  <done>The server submit boundary now owns canonical draft finalization, keeping cleanup authoritative and aligned with the anonymous cookie + DB-row architecture.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire submit UI, retry states, and browser redirect verification to the finalized session flow</name>
  <read_first>
    @src/features/assessment/assessment-experience.tsx
    @src/features/assessment/use-assessment-draft.ts
    @src/features/assessment/submit-assessment.ts
    @src/app/api/assessments/score/route.ts
    @test/assessment/assessment-submit.test.ts
    @test/assessment/mobile-flow.test.ts
    @test/e2e/mobile-assessment.spec.ts
  </read_first>
  <files>src/features/assessment/submit-assessment.ts, src/features/assessment/use-assessment-draft.ts, src/features/assessment/assessment-experience.tsx, test/assessment/assessment-submit.test.ts, test/assessment/mobile-flow.test.ts, test/e2e/mobile-assessment.spec.ts</files>
  <acceptance_criteria>
    - The final CTA is enabled only for complete assessments and shows a pending state during submit.
    - Success redirects with the exact `publicResult.href` returned by the server.
    - Failed submit leaves the user on the assessment flow with answers preserved and a retryable error message.
    - The Playwright mobile spec passes a completed submit redirect scenario.
    - `npm run build` passes after the submit wiring lands.
  </acceptance_criteria>
  <action>Create or update `src/features/assessment/submit-assessment.ts` as the only client submit boundary. Update `src/features/assessment/assessment-experience.tsx` and `src/features/assessment/use-assessment-draft.ts` so the UI reflects pending/error states, relies on the server for canonical cleanup, and only performs local cleanup as a mirror of the successful server finalization. Keep the redirect source strict: use `publicResult.href` from the response, never interpolate a result URL on the client. Extend the Playwright spec from Plan 03 so it verifies full completion submit on a mobile viewport and confirms the flow starts fresh after success.</action>
  <verify>
    <automated>npm exec vitest run test/assessment/assessment-submit.test.ts test/assessment/mobile-flow.test.ts && npm run typecheck && npm run build && npx playwright test test/e2e/mobile-assessment.spec.ts -g "submits completed assessment"</automated>
  </verify>
  <done>The mobile assessment flow can submit completed answers, preserve retryable state on failure, and redirect to the stored public result after authoritative server-side draft finalization.</done>
</task>

</tasks>

<verification>
- Run `npm exec vitest run test/assessment/score-route.test.ts test/assessment/assessment-session-route.test.ts test/assessment/assessment-submit.test.ts test/assessment/mobile-flow.test.ts`
- Run `npm run typecheck`
- Run `npm run build`
- Run `npx playwright test test/e2e/mobile-assessment.spec.ts -g "submits completed assessment"`
- Confirm `src/app/api/assessments/score/route.ts` finalizes the canonical draft session on success
- Confirm `src/features/assessment/submit-assessment.ts` uses `publicResult.href`
</verification>

<success_criteria>
- FLOW-05 is satisfied by a complete-answer submit path from mobile flow to stored result page
- Canonical draft cleanup/finalization occurs on the server, not only in the client
- Browser-level verification proves the submit redirect and post-submit fresh-start behavior
</success_criteria>

<output>
After completion, create `.planning/phases/03-mobile-assessment-flow/03-04-SUMMARY.md`
</output>
