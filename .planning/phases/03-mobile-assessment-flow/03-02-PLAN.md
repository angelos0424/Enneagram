---
phase: 03-mobile-assessment-flow
plan: 02
type: execute
wave: 2
depends_on:
  - "03-01"
files_modified:
  - src/features/assessment/types.ts
  - src/features/assessment/draft-storage.ts
  - src/features/assessment/use-assessment-draft.ts
  - src/features/assessment/assessment-experience.tsx
  - test/assessment/draft-persistence.test.ts
  - test/assessment/mobile-flow.test.ts
autonomous: true
requirements:
  - FLOW-04
must_haves:
  truths:
    - "If the user refreshes or briefly leaves mid-assessment, their in-progress answers and progress are restored locally."
    - "Draft recovery only occurs when the stored draft matches the current assessment version."
    - "Stale drafts are ignored or cleared instead of silently hydrating incompatible answers."
  artifacts:
    - path: "src/features/assessment/draft-storage.ts"
      provides: "Version-aware local draft load/save/clear boundary"
    - path: "src/features/assessment/use-assessment-draft.ts"
      provides: "Client hook that hydrates, autosaves, and clears draft state"
    - path: "src/features/assessment/assessment-experience.tsx"
      provides: "Recovery-aware assessment UI"
    - path: "test/assessment/draft-persistence.test.ts"
      provides: "Regression coverage for local draft persistence semantics"
  key_links:
    - from: "src/features/assessment/draft-storage.ts"
      to: "src/content/assessments/ko/v1.ts"
      via: "Storage key and recovery guard use the authoritative assessment version"
      pattern: "enneagram-assessment-draft|assessmentVersion"
    - from: "src/features/assessment/use-assessment-draft.ts"
      to: "src/features/assessment/draft-storage.ts"
      via: "Hydration and autosave happen through the storage boundary, not inline localStorage calls"
      pattern: "loadAssessmentDraft|saveAssessmentDraft|clearAssessmentDraft"
    - from: "src/features/assessment/assessment-experience.tsx"
      to: "src/features/assessment/use-assessment-draft.ts"
      via: "The page restores answers and progress after refresh using the dedicated hook"
      pattern: "useAssessmentDraft"
---

<objective>
Add anonymous local draft persistence and deterministic recovery to the mobile assessment flow, keyed by assessment version so refresh and temporary interruption do not force the user to restart.

Purpose: Satisfy FLOW-04 without expanding scope into server-side sessions or multi-device storage, while preserving the Phase 1 content contract and keeping incomplete drafts out of the permanent snapshot table.
Output: A version-aware browser draft boundary, recovery wiring in the assessment client, and tests that lock save, restore, mismatch discard, and clear behavior.
</objective>

<execution_context>
@/home/ubuntu/Project/Enneagram/.codex/get-shit-done/workflows/execute-plan.md
@/home/ubuntu/Project/Enneagram/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-mobile-assessment-flow/03-RESEARCH.md
@.planning/phases/03-mobile-assessment-flow/03-VALIDATION.md
@src/content/assessments/ko/v1.ts
@src/features/assessment/types.ts
@src/features/assessment/assessment-flow.ts
@src/features/assessment/assessment-experience.tsx

<interfaces>
From `src/content/assessments/ko/v1.ts`:
```typescript
export const assessmentDefinition = {
  version: ASSESSMENT_VERSION,
  questions: assessmentQuestions,
} as const;
```

Recommended Phase 3 draft shape from research:
```typescript
type AssessmentDraft = {
  assessmentVersion: string;
  answers: Array<{ questionId: string; value: 1 | 2 | 3 | 4 | 5 }>;
  updatedAt: string;
};
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define the version-aware browser draft storage contract and tests</name>
  <read_first>
    @src/content/assessments/ko/v1.ts
    @src/features/assessment/types.ts
    @.planning/phases/03-mobile-assessment-flow/03-RESEARCH.md
  </read_first>
  <files>src/features/assessment/types.ts, src/features/assessment/draft-storage.ts, test/assessment/draft-persistence.test.ts</files>
  <acceptance_criteria>
    - `src/features/assessment/draft-storage.ts` exports explicit load/save/clear functions instead of scattering `localStorage` access through components.
    - The storage key format is `enneagram-assessment-draft:${assessmentVersion}`.
    - `test/assessment/draft-persistence.test.ts` covers matching-version restore, version-mismatch discard, malformed payload rejection, and clear-after-success semantics.
  </acceptance_criteria>
  <action>Extend `src/features/assessment/types.ts` with the minimal persisted-draft shape needed by this phase. Create `src/features/assessment/draft-storage.ts` to own serialization, deserialization, storage-key construction, and defensive invalidation rules for anonymous local drafts. Use the exact storage-key and version-guard strategy from research: version match restores, version mismatch invalidates. Start by writing the failing persistence tests in `test/assessment/draft-persistence.test.ts`; do not rely on ad hoc browser checks to define the contract.</action>
  <verify>
    <automated>npm exec vitest run test/assessment/draft-persistence.test.ts</automated>
  </verify>
  <done>The codebase has a single reusable browser-draft boundary that safely persists anonymous assessment answers and rejects stale or invalid stored data.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire draft recovery and autosave into the mobile assessment experience</name>
  <read_first>
    @src/features/assessment/assessment-experience.tsx
    @src/features/assessment/assessment-flow.ts
    @src/features/assessment/draft-storage.ts
    @test/assessment/draft-persistence.test.ts
    @test/assessment/mobile-flow.test.ts
  </read_first>
  <files>src/features/assessment/use-assessment-draft.ts, src/features/assessment/assessment-experience.tsx, test/assessment/draft-persistence.test.ts, test/assessment/mobile-flow.test.ts</files>
  <acceptance_criteria>
    - Refreshing a partially answered assessment restores prior answers and progress through automated coverage.
    - The current question after restore is deterministic: continue at the first unanswered question, or the final step if all answers are present.
    - A stale-version draft is cleared or ignored without polluting the current session.
    - `npm run typecheck` passes after the recovery wiring lands.
  </acceptance_criteria>
  <action>Create `src/features/assessment/use-assessment-draft.ts` to connect the Plan 01 flow state with the draft-storage boundary, handling hydration on mount, autosave on answer changes, and explicit clear behavior hooks for the later submit plan. Update `src/features/assessment/assessment-experience.tsx` to use that hook, restore progress deterministically, and show resume-safe behavior after refresh without requiring login. Keep recovery local-only; do not add cookies, server draft rows, or cross-device session logic. Update the existing mobile-flow and new draft-persistence tests so FLOW-04 is locked at the interaction layer as well as the storage layer.</action>
  <verify>
    <automated>npm exec vitest run test/assessment/draft-persistence.test.ts test/assessment/mobile-flow.test.ts && npm run typecheck</automated>
  </verify>
  <done>The assessment flow now restores anonymous in-progress work after refresh or temporary interruption, while rejecting stale-version drafts and preserving the existing mobile progression behavior.</done>
</task>

</tasks>

<verification>
- Run `npm exec vitest run test/assessment/draft-persistence.test.ts test/assessment/mobile-flow.test.ts`
- Run `npm run typecheck`
- Confirm `src/features/assessment/assessment-experience.tsx` does not call `localStorage` directly
- Confirm `src/features/assessment/draft-storage.ts` contains the `enneagram-assessment-draft:` key prefix
</verification>

<success_criteria>
- FLOW-04 is satisfied by deterministic local draft recovery after refresh or temporary interruption
- Recovery only occurs for the active assessment version
- The draft boundary stays local and anonymous, with no database/session scope creep
</success_criteria>

<output>
After completion, create `.planning/phases/03-mobile-assessment-flow/03-02-SUMMARY.md`
</output>
