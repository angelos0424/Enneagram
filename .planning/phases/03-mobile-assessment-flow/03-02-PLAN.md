---
phase: 03-mobile-assessment-flow
plan: 02
type: execute
wave: 2
depends_on:
  - "03-01"
files_modified:
  - package.json
  - package-lock.json
  - playwright.config.ts
  - test/e2e/mobile-assessment.spec.ts
  - src/db/schema.ts
  - drizzle/0002_phase3_assessment_drafts.sql
  - drizzle/meta/0002_snapshot.json
  - drizzle/meta/_journal.json
  - src/db/repositories/assessment-draft-session-repository.ts
  - src/domain/assessment/draft-session.ts
  - src/domain/assessment/draft-schema.ts
  - src/features/assessment/types.ts
  - test/assessment/assessment-session-route.test.ts
autonomous: true
requirements:
  - FLOW-04
must_haves:
  truths:
    - "Phase 3 has browser-level verification infrastructure for mobile refresh and redirect behavior before route/client recovery wiring expands."
    - "Anonymous assessment drafts have a canonical PostgreSQL persistence boundary keyed by an opaque HttpOnly session token."
    - "Cookie naming, cookie options, schema shape, and repository operations are locked before client hydration logic depends on them."
  artifacts:
    - path: "playwright.config.ts"
      provides: "Mobile browser verification harness for assessment flow execution"
    - path: "test/e2e/mobile-assessment.spec.ts"
      provides: "Initial browser coverage scaffold for anonymous start, refresh recovery, and submit redirect"
    - path: "src/db/schema.ts"
      provides: "PostgreSQL draft-session table for anonymous assessment recovery"
    - path: "src/db/repositories/assessment-draft-session-repository.ts"
      provides: "Repository boundary for draft-session create/load/update/delete"
    - path: "src/domain/assessment/draft-session.ts"
      provides: "Opaque cookie/session helper boundary for anonymous assessment state"
    - path: "src/domain/assessment/draft-schema.ts"
      provides: "Route payload validation surface for session bootstrap and draft updates"
    - path: "test/assessment/assessment-session-route.test.ts"
      provides: "Regression coverage for the canonical server draft/session contract"
  key_links:
    - from: "src/domain/assessment/draft-session.ts"
      to: "src/db/repositories/assessment-draft-session-repository.ts"
      via: "Opaque session token shape and repository lookup contract are defined before routes are added"
      pattern: "assessment_session|sessionToken"
    - from: "src/db/repositories/assessment-draft-session-repository.ts"
      to: "src/db/schema.ts"
      via: "Repository CRUD aligns with the draft-session table and migration artifacts"
      pattern: "assessmentDraftSessions|findBySessionToken|createDraftSession"
    - from: "test/e2e/mobile-assessment.spec.ts"
      to: "playwright.config.ts"
      via: "Mobile browser projects execute the new assessment flow harness"
      pattern: "devices|Mobile Chrome"
---

<objective>
Establish the infrastructure and persistence boundary required for canonical anonymous draft recovery: browser-level verification harness, opaque session-cookie helpers, PostgreSQL draft-session schema, and repository contracts.

Purpose: Break the large draft-recovery plan into a bounded prerequisite step so later route/client recovery work builds on a locked session model instead of inventing it on the fly.
Output: Playwright setup, draft-session schema and migration artifacts, cookie/session helper utilities, repository CRUD boundary, and tests for the canonical draft-session contract.
</objective>

<execution_context>
@/home/ubuntu/Project/Enneagram/.codex/get-shit-done/workflows/execute-plan.md
@/home/ubuntu/Project/Enneagram/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-mobile-assessment-flow/03-RESEARCH.md
@.planning/phases/03-mobile-assessment-flow/03-VALIDATION.md
@/home/ubuntu/Project/Enneagram/CLAUDE.md
@src/db/schema.ts
@src/db/repositories/assessment-result-repository.ts
@src/db/client.ts
@src/domain/assessment/schema.ts
@src/features/assessment/types.ts
@package.json
@vitest.config.ts

<interfaces>
From `CLAUDE.md`:
```text
On assessment start, set an HttpOnly anonymous session cookie using Next.js cookies().
Keep the cookie value opaque, random, and non-guessable.
A cookie + DB row is enough.
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add browser-level mobile verification infrastructure for anonymous session flows</name>
  <read_first>
    @package.json
    @vitest.config.ts
    @.planning/phases/03-mobile-assessment-flow/03-VALIDATION.md
    @src/app/page.tsx
  </read_first>
  <files>package.json, package-lock.json, playwright.config.ts, test/e2e/mobile-assessment.spec.ts</files>
  <acceptance_criteria>
    - `package.json` includes `@playwright/test` and a runnable script for mobile assessment browser checks.
    - `playwright.config.ts` defines at least one mobile browser project suitable for phone-sized verification.
    - `test/e2e/mobile-assessment.spec.ts` includes an anonymous entry case now and placeholders/structure ready for refresh recovery and submit redirect.
    - `npx playwright test --list` succeeds.
  </acceptance_criteria>
  <action>Add the browser-level test harness Phase 3 needs by installing `@playwright/test`, creating `playwright.config.ts`, and adding `test/e2e/mobile-assessment.spec.ts`. The initial spec must at minimum verify the anonymous mobile entry surface from Plan 01 and establish the same file for later refresh-recovery and submit-redirect cases. Keep this step focused on harness readiness, not route or client recovery implementation.</action>
  <verify>
    <automated>npx playwright test --list</automated>
  </verify>
  <done>Phase 3 now has an executable mobile browser harness ready before cookie-backed recovery and submit flows are wired.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Define the canonical anonymous draft-session schema, repository, and cookie helper boundary</name>
  <read_first>
    @/home/ubuntu/Project/Enneagram/CLAUDE.md
    @src/db/schema.ts
    @src/db/repositories/assessment-result-repository.ts
    @src/db/client.ts
    @src/domain/assessment/schema.ts
  </read_first>
  <files>src/db/schema.ts, drizzle/0002_phase3_assessment_drafts.sql, drizzle/meta/0002_snapshot.json, drizzle/meta/_journal.json, src/db/repositories/assessment-draft-session-repository.ts, src/domain/assessment/draft-session.ts, src/domain/assessment/draft-schema.ts, src/features/assessment/types.ts, test/assessment/assessment-session-route.test.ts</files>
  <acceptance_criteria>
    - `src/db/schema.ts` adds a draft-session table storing an opaque session token, `assessmentVersion`, draft answers/progress, and timestamps.
    - `src/domain/assessment/draft-session.ts` centralizes cookie naming, cookie options, and session-token creation/lookup for the assessment flow.
    - `src/db/repositories/assessment-draft-session-repository.ts` exposes create/load/update/delete methods needed by later start/load/update/finalize routes.
    - Checked-in Drizzle migration artifacts exist for the draft-session table.
    - `test/assessment/assessment-session-route.test.ts` locks the canonical server contract instead of localStorage semantics.
  </acceptance_criteria>
  <action>Add the missing server draft backbone required by FLOW-04 and `CLAUDE.md`. Extend `src/db/schema.ts` with a dedicated anonymous assessment draft table and generate checked-in Drizzle migration artifacts. Create `src/db/repositories/assessment-draft-session-repository.ts` for canonical draft CRUD, `src/domain/assessment/draft-session.ts` for opaque HttpOnly cookie/session helpers, and `src/domain/assessment/draft-schema.ts` for the route payload validation surface. Update `src/features/assessment/types.ts` only as needed so client draft/session types stay aligned with the canonical server representation. Start by writing failing route/repository-facing tests in `test/assessment/assessment-session-route.test.ts`; do not preserve the previous local-only draft contract.</action>
  <verify>
    <automated>npm exec vitest run test/assessment/assessment-session-route.test.ts && npm run typecheck && npm exec drizzle-kit generate && test -f drizzle/0002_phase3_assessment_drafts.sql && test -f drizzle/meta/0002_snapshot.json</automated>
  </verify>
  <done>The codebase has a server-owned anonymous session model with cookie helpers, PostgreSQL persistence, migration artifacts, and locked repository tests.</done>
</task>

</tasks>

<verification>
- Run `npx playwright test --list`
- Run `npm exec vitest run test/assessment/assessment-session-route.test.ts`
- Run `npm run typecheck`
- Run `npm exec drizzle-kit generate`
- Confirm `src/domain/assessment/draft-session.ts` defines the anonymous cookie boundary
- Confirm `drizzle/0002_phase3_assessment_drafts.sql` exists
</verification>

<success_criteria>
- Phase 3 has mobile browser verification infrastructure before recovery wiring expands
- FLOW-04 prerequisites are satisfied by a canonical cookie + PostgreSQL draft-session boundary
- Later route/client recovery work can build on a locked schema and repository contract
</success_criteria>

<output>
After completion, create `.planning/phases/03-mobile-assessment-flow/03-02-SUMMARY.md`
</output>
