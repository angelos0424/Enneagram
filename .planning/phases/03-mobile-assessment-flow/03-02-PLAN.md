---
phase: 03-mobile-assessment-flow
plan: 02
type: execute
wave: 2
depends_on:
  - "03-01"
files_modified:
  - package.json
  - package-lock.json
  - playwright.config.ts
  - test/e2e/mobile-assessment.spec.ts
  - src/db/schema.ts
  - drizzle/0002_phase3_assessment_drafts.sql
  - drizzle/meta/0002_snapshot.json
  - drizzle/meta/_journal.json
  - src/db/repositories/assessment-draft-session-repository.ts
  - src/domain/assessment/draft-session.ts
  - src/domain/assessment/draft-schema.ts
  - src/app/api/assessment-session/route.ts
  - src/app/api/assessment-session/draft/route.ts
  - src/features/assessment/types.ts
  - src/features/assessment/use-assessment-draft.ts
  - src/features/assessment/assessment-experience.tsx
  - test/assessment/assessment-session-route.test.ts
  - test/assessment/mobile-flow.test.ts
autonomous: true
requirements:
  - FLOW-04
must_haves:
  truths:
    - "If the user refreshes or briefly leaves mid-assessment, the in-progress assessment restores from a canonical anonymous server draft."
    - "Draft recovery is keyed by an opaque HttpOnly cookie session and the current assessment version, not by local browser storage alone."
    - "The mobile flow can start, load, and update draft progress through explicit route handlers that survive page refresh."
  artifacts:
    - path: "playwright.config.ts"
      provides: "Browser verification harness for mobile resume and redirect flows"
    - path: "test/e2e/mobile-assessment.spec.ts"
      provides: "Mobile browser regression coverage for anonymous start and cookie-backed refresh recovery"
    - path: "src/db/schema.ts"
      provides: "PostgreSQL table for anonymous assessment draft sessions"
    - path: "src/db/repositories/assessment-draft-session-repository.ts"
      provides: "Repository boundary for creating, loading, updating, and deleting anonymous draft rows"
    - path: "src/domain/assessment/draft-session.ts"
      provides: "Cookie/session helper boundary for opaque anonymous assessment sessions"
    - path: "src/app/api/assessment-session/route.ts"
      provides: "Start/load route that creates or reuses the anonymous draft session"
    - path: "src/app/api/assessment-session/draft/route.ts"
      provides: "Route that persists incremental draft answer updates"
    - path: "src/features/assessment/use-assessment-draft.ts"
      provides: "Client recovery hook backed by the server draft routes"
    - path: "test/assessment/assessment-session-route.test.ts"
      provides: "Regression coverage for cookie-backed draft route behavior"
  key_links:
    - from: "src/domain/assessment/draft-session.ts"
      to: "src/app/api/assessment-session/route.ts"
      via: "Route handlers mint and read the opaque HttpOnly session cookie through one helper boundary"
      pattern: "cookies\\(|assessment_session"
    - from: "src/app/api/assessment-session/route.ts"
      to: "src/db/repositories/assessment-draft-session-repository.ts"
      via: "Start/load requests resolve the canonical draft from PostgreSQL"
      pattern: "findBySessionToken|createDraftSession"
    - from: "src/app/api/assessment-session/draft/route.ts"
      to: "src/db/schema.ts"
      via: "Incremental answer updates persist into the draft-session table"
      pattern: "updateDraftAnswers|assessment_draft"
    - from: "src/features/assessment/use-assessment-draft.ts"
      to: "src/app/api/assessment-session/route.ts"
      via: "Client hydration and resume read from the route-backed canonical session"
      pattern: "/api/assessment-session"
    - from: "test/e2e/mobile-assessment.spec.ts"
      to: "src/features/assessment/assessment-experience.tsx"
      via: "Browser tests prove cookie-backed refresh recovery in the real flow"
      pattern: "refresh|resume|Mobile Chrome"
---

<objective>
Replace the incorrect local-only recovery plan with the project-standard anonymous session architecture: HttpOnly cookie identity, PostgreSQL-backed canonical draft persistence, route handlers for start/load/update, and browser verification that refresh recovery actually works on a mobile viewport.

Purpose: Satisfy FLOW-04 in a way that matches `CLAUDE.md`, preserves the existing Next.js + Drizzle architecture, and gives later submit work a real server-side draft to finalize.
Output: Playwright mobile harness, anonymous draft schema/repository/cookie helpers, route handlers for session lifecycle, and client wiring that resumes from the canonical server draft.
</objective>

<execution_context>
@/home/ubuntu/Project/Enneagram/.codex/get-shit-done/workflows/execute-plan.md
@/home/ubuntu/Project/Enneagram/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-mobile-assessment-flow/03-RESEARCH.md
@.planning/phases/03-mobile-assessment-flow/03-VALIDATION.md
@/home/ubuntu/Project/Enneagram/CLAUDE.md
@src/db/schema.ts
@src/db/repositories/assessment-result-repository.ts
@src/db/client.ts
@src/content/assessments/ko/v1.ts
@src/domain/assessment/schema.ts
@src/features/assessment/types.ts
@src/features/assessment/assessment-experience.tsx
@vitest.config.ts

<interfaces>
From `CLAUDE.md`:
```text
On assessment start, set an HttpOnly anonymous session cookie using Next.js cookies().
Keep the cookie value opaque, random, and non-guessable.
A cookie + DB row is enough.
```

From `src/domain/assessment/schema.ts`:
```typescript
export type AssessmentSubmission = {
  assessmentVersion: string;
  answers: Array<{ questionId: string; value: 1 | 2 | 3 | 4 | 5 }>;
};
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add browser-level mobile verification infrastructure for anonymous session recovery</name>
  <read_first>
    @package.json
    @vitest.config.ts
    @.planning/phases/03-mobile-assessment-flow/03-VALIDATION.md
    @src/app/page.tsx
  </read_first>
  <files>package.json, package-lock.json, playwright.config.ts, test/e2e/mobile-assessment.spec.ts</files>
  <acceptance_criteria>
    - `package.json` includes `@playwright/test` and a runnable script for mobile assessment browser checks.
    - `playwright.config.ts` defines at least one mobile browser project suitable for phone-sized verification.
    - `test/e2e/mobile-assessment.spec.ts` includes an anonymous entry case now and a structure ready to extend for refresh recovery and submit redirect.
    - `npx playwright test --list` succeeds.
  </acceptance_criteria>
  <action>Add the browser-level test harness this phase is missing by installing `@playwright/test`, creating `playwright.config.ts`, and adding `test/e2e/mobile-assessment.spec.ts`. The initial spec should at minimum verify the anonymous mobile entry surface from Plan 01 and establish the same file for the refresh-recovery and submit-redirect cases added later in this phase. Keep the harness narrow and project-specific; do not add a second component-test stack here unless implementation proves it is required.</action>
  <verify>
    <automated>npx playwright test --list</automated>
  </verify>
  <done>Phase 3 now has an executable browser harness for real mobile-flow verification instead of relying only on route or unit tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Define the canonical anonymous draft-session persistence and cookie helper boundary</name>
  <read_first>
    @/home/ubuntu/Project/Enneagram/CLAUDE.md
    @src/db/schema.ts
    @src/db/repositories/assessment-result-repository.ts
    @src/db/client.ts
    @src/domain/assessment/schema.ts
  </read_first>
  <files>src/db/schema.ts, drizzle/0002_phase3_assessment_drafts.sql, drizzle/meta/0002_snapshot.json, drizzle/meta/_journal.json, src/db/repositories/assessment-draft-session-repository.ts, src/domain/assessment/draft-session.ts, src/domain/assessment/draft-schema.ts, src/features/assessment/types.ts, test/assessment/assessment-session-route.test.ts</files>
  <acceptance_criteria>
    - `src/db/schema.ts` adds a draft-session table storing an opaque session token, `assessmentVersion`, draft answers/progress, and timestamps.
    - `src/domain/assessment/draft-session.ts` centralizes cookie naming, cookie options, and session-token creation/lookup for the assessment flow.
    - `src/db/repositories/assessment-draft-session-repository.ts` exposes create/load/update/delete methods needed by start/load/update/finalize routes.
    - Checked-in Drizzle migration artifacts exist for the draft-session table.
    - `test/assessment/assessment-session-route.test.ts` locks the canonical server contract instead of localStorage semantics.
  </acceptance_criteria>
  <action>Add the missing server draft backbone required by FLOW-04 and `CLAUDE.md`. Extend `src/db/schema.ts` with a dedicated anonymous assessment draft table and generate the checked-in Drizzle migration artifacts. Create `src/db/repositories/assessment-draft-session-repository.ts` for canonical draft CRUD, `src/domain/assessment/draft-session.ts` for opaque HttpOnly cookie/session helpers, and `src/domain/assessment/draft-schema.ts` for the route payload validation surface. Update `src/features/assessment/types.ts` only as needed so client draft types stay aligned with the canonical server representation. Start by writing failing route/repository-facing tests in `test/assessment/assessment-session-route.test.ts`; do not preserve the previous local-only draft contract.</action>
  <verify>
    <automated>npm exec vitest run test/assessment/assessment-session-route.test.ts && npm run typecheck && npm exec drizzle-kit generate && git diff --exit-code -- drizzle src/db/schema.ts</automated>
  </verify>
  <done>The codebase has a server-owned anonymous assessment session model with a cookie helper boundary, PostgreSQL persistence, and locked schema/repository tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add start/load/update draft routes and wire cookie-backed refresh recovery into the mobile flow</name>
  <read_first>
    @src/features/assessment/assessment-experience.tsx
    @src/features/assessment/types.ts
    @src/content/assessments/ko/v1.ts
    @src/domain/assessment/draft-session.ts
    @src/domain/assessment/draft-schema.ts
    @src/db/repositories/assessment-draft-session-repository.ts
    @test/assessment/assessment-session-route.test.ts
    @test/assessment/mobile-flow.test.ts
    @test/e2e/mobile-assessment.spec.ts
  </read_first>
  <files>src/app/api/assessment-session/route.ts, src/app/api/assessment-session/draft/route.ts, src/features/assessment/use-assessment-draft.ts, src/features/assessment/assessment-experience.tsx, test/assessment/assessment-session-route.test.ts, test/assessment/mobile-flow.test.ts, test/e2e/mobile-assessment.spec.ts</files>
  <acceptance_criteria>
    - `GET` or `POST` on the assessment-session route create or reuse the anonymous cookie session and return the current canonical draft state.
    - Draft updates persist through a dedicated route instead of direct browser-only storage calls.
    - Refreshing the mobile flow restores answers and progress from the server-backed draft.
    - The Playwright mobile spec includes and passes a refresh-recovery scenario.
    - `npm run typecheck` passes after the client and route wiring land.
  </acceptance_criteria>
  <action>Create `src/app/api/assessment-session/route.ts` for session bootstrap/load and `src/app/api/assessment-session/draft/route.ts` for incremental draft writes, both backed by the new cookie + repository boundaries. Update `src/features/assessment/use-assessment-draft.ts` and `src/features/assessment/assessment-experience.tsx` so hydration and autosave talk to those routes instead of treating the browser as the source of truth. Keep any local mirror strictly optional and subordinate if one is added later; the canonical recovery path in this plan is server-backed. Extend the Vitest route coverage and the Playwright mobile spec so FLOW-04 is verified in both contract and real-browser refresh scenarios.</action>
  <verify>
    <automated>npm exec vitest run test/assessment/assessment-session-route.test.ts test/assessment/mobile-flow.test.ts && npm run typecheck && npx playwright test test/e2e/mobile-assessment.spec.ts -g "restores draft after refresh"</automated>
  </verify>
  <done>The assessment flow now resumes from a canonical anonymous cookie-backed server draft after refresh, with automated route, client, and real-browser coverage proving it.</done>
</task>

</tasks>

<verification>
- Run `npm exec vitest run test/assessment/assessment-session-route.test.ts test/assessment/mobile-flow.test.ts`
- Run `npm run typecheck`
- Run `npx playwright test test/e2e/mobile-assessment.spec.ts -g "restores draft after refresh"`
- Confirm `src/features/assessment/use-assessment-draft.ts` fetches `/api/assessment-session` instead of relying on local-only storage
- Confirm `src/domain/assessment/draft-session.ts` defines the anonymous cookie boundary
</verification>

<success_criteria>
- FLOW-04 is satisfied by canonical cookie + PostgreSQL draft recovery, matching `CLAUDE.md`
- Anonymous session identity is opaque, random, and managed by route handlers
- Browser-level verification proves refresh recovery on a mobile viewport
</success_criteria>

<output>
After completion, create `.planning/phases/03-mobile-assessment-flow/03-02-SUMMARY.md`
</output>
