---
phase: 03-mobile-assessment-flow
plan: 03
type: execute
wave: 3
depends_on:
  - "03-02"
files_modified:
  - src/features/assessment/submit-assessment.ts
  - src/features/assessment/use-assessment-draft.ts
  - src/features/assessment/assessment-experience.tsx
  - test/assessment/assessment-submit.test.ts
  - test/assessment/draft-persistence.test.ts
  - test/assessment/mobile-flow.test.ts
autonomous: true
requirements:
  - FLOW-05
must_haves:
  truths:
    - "Once every required question is answered, the user can submit the assessment from the mobile flow."
    - "Successful submission navigates to the stored public result page returned by the server response."
    - "A successful submit clears the local draft, while a failed submit keeps answers intact and shows a retryable error state."
  artifacts:
    - path: "src/features/assessment/submit-assessment.ts"
      provides: "Typed client submit boundary for the score route"
    - path: "src/features/assessment/assessment-experience.tsx"
      provides: "Submit-ready assessment UI with loading and error states"
    - path: "test/assessment/assessment-submit.test.ts"
      provides: "Regression coverage for submit success, route-error handling, and redirect contract"
  key_links:
    - from: "src/features/assessment/submit-assessment.ts"
      to: "src/app/api/assessments/score/route.ts"
      via: "Client submit payload matches the server submission schema and consumes the existing success/error response shape"
      pattern: "publicResult.href|INVALID_PAYLOAD_SHAPE"
    - from: "src/features/assessment/assessment-experience.tsx"
      to: "src/features/assessment/submit-assessment.ts"
      via: "Final CTA posts only when the flow is complete and uses the returned href for navigation"
      pattern: "submitAssessment|router.push"
    - from: "src/features/assessment/assessment-experience.tsx"
      to: "src/features/assessment/use-assessment-draft.ts"
      via: "Successful submit clears the local draft before redirect"
      pattern: "clearDraft"
---

<objective>
Finish the mobile assessment flow by wiring complete-answer submission to the existing server-authoritative scoring route, handling retryable failures, clearing the local draft on success, and redirecting to the returned public result URL.

Purpose: Satisfy FLOW-05 while preserving the Phase 2 rule that the server owns result persistence and the client must navigate using `publicResult.href` instead of constructing result URLs itself.
Output: A typed submit helper, submit/error states in the assessment UI, and tests that lock redirect, draft clearing, and failure behavior.
</objective>

<execution_context>
@/home/ubuntu/Project/Enneagram/.codex/get-shit-done/workflows/execute-plan.md
@/home/ubuntu/Project/Enneagram/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-mobile-assessment-flow/03-RESEARCH.md
@.planning/phases/03-mobile-assessment-flow/03-VALIDATION.md
@.planning/phases/02-persistent-result-snapshots/02-02-SUMMARY.md
@src/app/api/assessments/score/route.ts
@src/app/results/[publicId]/page.tsx
@src/domain/assessment/schema.ts
@src/features/assessment/assessment-experience.tsx
@src/features/assessment/use-assessment-draft.ts

<interfaces>
From `src/app/api/assessments/score/route.ts`:
```typescript
return Response.json(
  {
    result,
    publicResult: {
      publicId: savedResult.publicId,
      href: `/results/${savedResult.publicId}`,
    },
  },
  { status: 200 },
);
```

From `src/domain/assessment/schema.ts`:
```typescript
export type AssessmentSubmission = {
  assessmentVersion: string;
  answers: Array<{ questionId: string; value: 1 | 2 | 3 | 4 | 5 }>;
};
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define the client submit boundary and redirect/error contract tests</name>
  <read_first>
    @src/app/api/assessments/score/route.ts
    @src/domain/assessment/schema.ts
    @test/assessment/score-route.test.ts
    @.planning/phases/02-persistent-result-snapshots/02-02-SUMMARY.md
  </read_first>
  <files>src/features/assessment/submit-assessment.ts, test/assessment/assessment-submit.test.ts</files>
  <acceptance_criteria>
    - `src/features/assessment/submit-assessment.ts` posts the full `AssessmentSubmission` payload to `/api/assessments/score`.
    - The success path returns and exposes `publicResult.href` from the server response instead of constructing `/results/...` on the client.
    - `test/assessment/assessment-submit.test.ts` covers success, machine-readable route errors, and unexpected-response fallback behavior.
  </acceptance_criteria>
  <action>Create `src/features/assessment/submit-assessment.ts` as the only client fetch boundary for Phase 3 submission. It must accept the full `AssessmentSubmission`, call `/api/assessments/score`, parse the existing success shape, and normalize route failures into a typed retryable error surface the UI can render. Start with failing tests in `test/assessment/assessment-submit.test.ts` that prove the redirect target always comes from `publicResult.href` and that known server error codes remain machine-readable to the client.</action>
  <verify>
    <automated>npm exec vitest run test/assessment/assessment-submit.test.ts test/assessment/score-route.test.ts</automated>
  </verify>
  <done>The codebase has a test-locked submit helper that respects the existing score-route contract and gives the UI a stable redirect/error interface.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire completion submit, draft clearing, and result redirect into the assessment UI</name>
  <read_first>
    @src/features/assessment/assessment-experience.tsx
    @src/features/assessment/use-assessment-draft.ts
    @src/features/assessment/submit-assessment.ts
    @test/assessment/assessment-submit.test.ts
    @test/assessment/draft-persistence.test.ts
    @test/assessment/mobile-flow.test.ts
  </read_first>
  <files>src/features/assessment/use-assessment-draft.ts, src/features/assessment/assessment-experience.tsx, test/assessment/assessment-submit.test.ts, test/assessment/draft-persistence.test.ts, test/assessment/mobile-flow.test.ts</files>
  <acceptance_criteria>
    - The final CTA becomes enabled only when the assessment is complete and enters a pending state while submitting.
    - On a successful response, the local draft is cleared before navigation to the exact `publicResult.href` returned by the server.
    - On a failed response, the user stays on the assessment page with answers preserved and a visible retry message.
    - `npm run build` passes after the submit wiring lands.
  </acceptance_criteria>
  <action>Update `src/features/assessment/assessment-experience.tsx` to call `submitAssessment` only when every required answer exists, show pending/error states suitable for mobile usage, and navigate with Next navigation APIs using the server-returned href. Extend `src/features/assessment/use-assessment-draft.ts` with the explicit clear hook needed for success cleanup, but do not clear on failure. Update the Phase 3 tests so FLOW-05 is locked across completion gating, submission success, redirect behavior, and draft preservation-on-error. Do not change the server route semantics unless tests prove an integration mismatch; the client should conform to the existing server-authoritative boundary.</action>
  <verify>
    <automated>npm exec vitest run test/assessment/assessment-submit.test.ts test/assessment/mobile-flow.test.ts test/assessment/draft-persistence.test.ts && npm run typecheck && npm run build</automated>
  </verify>
  <done>The mobile assessment flow can now submit completed answers, clear the recovered draft on success, surface retryable errors on failure, and redirect to the persisted public result page returned by the server.</done>
</task>

</tasks>

<verification>
- Run `npm exec vitest run test/assessment/assessment-submit.test.ts test/assessment/mobile-flow.test.ts test/assessment/draft-persistence.test.ts`
- Run `npm run typecheck`
- Run `npm run build`
- Confirm `src/features/assessment/submit-assessment.ts` reads `publicResult.href`
- Confirm `src/features/assessment/assessment-experience.tsx` does not manually interpolate `/results/${...}`
</verification>

<success_criteria>
- FLOW-05 is satisfied by a complete-answer submit path from mobile assessment to stored public result page
- The client respects the existing server-authoritative route contract
- Successful completion clears anonymous draft state while failed completion preserves it for retry
</success_criteria>

<output>
After completion, create `.planning/phases/03-mobile-assessment-flow/03-03-SUMMARY.md`
</output>
