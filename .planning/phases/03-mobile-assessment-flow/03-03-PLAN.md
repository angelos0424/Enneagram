---
phase: 03-mobile-assessment-flow
plan: 03
type: execute
wave: 3
depends_on:
  - "03-02"
files_modified:
  - src/app/api/assessment-session/route.ts
  - src/app/api/assessment-session/draft/route.ts
  - src/features/assessment/use-assessment-draft.ts
  - src/features/assessment/assessment-experience.tsx
  - test/assessment/assessment-session-route.test.ts
  - test/assessment/mobile-flow.test.ts
  - test/e2e/mobile-assessment.spec.ts
autonomous: true
requirements:
  - FLOW-04
must_haves:
  truths:
    - "Refreshing or briefly leaving the assessment restores progress from the canonical anonymous server draft."
    - "The client hydrates and autosaves through dedicated route handlers rather than treating browser storage as the source of truth."
    - "Real mobile-browser coverage proves refresh recovery works end-to-end."
  artifacts:
    - path: "src/app/api/assessment-session/route.ts"
      provides: "Start/load route that creates or reuses the anonymous draft session"
    - path: "src/app/api/assessment-session/draft/route.ts"
      provides: "Incremental draft-update route for answer persistence"
    - path: "src/features/assessment/use-assessment-draft.ts"
      provides: "Client recovery hook backed by the canonical server routes"
    - path: "src/features/assessment/assessment-experience.tsx"
      provides: "Recovery-aware assessment UI wired to route-backed draft state"
    - path: "test/e2e/mobile-assessment.spec.ts"
      provides: "Browser coverage for draft restore after refresh on a mobile viewport"
  key_links:
    - from: "src/app/api/assessment-session/route.ts"
      to: "src/db/repositories/assessment-draft-session-repository.ts"
      via: "Bootstrap/load requests resolve the canonical draft from PostgreSQL"
      pattern: "findBySessionToken|createDraftSession"
    - from: "src/app/api/assessment-session/draft/route.ts"
      to: "src/domain/assessment/draft-schema.ts"
      via: "Incremental draft writes validate payloads through the draft schema contract"
      pattern: "assessmentDraftUpdateSchema|assessmentDraftBootstrapSchema"
    - from: "src/features/assessment/use-assessment-draft.ts"
      to: "src/app/api/assessment-session/route.ts"
      via: "Client hydration and resume read from the route-backed canonical session"
      pattern: "/api/assessment-session"
    - from: "test/e2e/mobile-assessment.spec.ts"
      to: "src/features/assessment/assessment-experience.tsx"
      via: "Browser test proves cookie-backed refresh recovery in the real flow"
      pattern: "restores draft after refresh"
---

<objective>
Wire the canonical anonymous draft-session boundary into the real assessment flow by adding start/load/update routes, client hydration/autosave, and end-to-end mobile refresh recovery coverage.

Purpose: Finish FLOW-04 in a bounded execution unit after the schema/repository/cookie contract already exists, avoiding the oversized cross-layer plan that the checker rejected.
Output: Route-backed session bootstrap and draft-update handlers, a client hook that hydrates/autosaves through those routes, and mobile-browser proof that refresh recovery works.
</objective>

<execution_context>
@/home/ubuntu/Project/Enneagram/.codex/get-shit-done/workflows/execute-plan.md
@/home/ubuntu/Project/Enneagram/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-mobile-assessment-flow/03-RESEARCH.md
@.planning/phases/03-mobile-assessment-flow/03-VALIDATION.md
@/home/ubuntu/Project/Enneagram/CLAUDE.md
@src/content/assessments/ko/v1.ts
@src/domain/assessment/draft-session.ts
@src/domain/assessment/draft-schema.ts
@src/db/repositories/assessment-draft-session-repository.ts
@src/features/assessment/types.ts
@src/features/assessment/assessment-experience.tsx
@test/assessment/assessment-session-route.test.ts
@test/e2e/mobile-assessment.spec.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add assessment-session bootstrap and draft-update route handlers</name>
  <read_first>
    @src/domain/assessment/draft-session.ts
    @src/domain/assessment/draft-schema.ts
    @src/db/repositories/assessment-draft-session-repository.ts
    @test/assessment/assessment-session-route.test.ts
  </read_first>
  <files>src/app/api/assessment-session/route.ts, src/app/api/assessment-session/draft/route.ts, test/assessment/assessment-session-route.test.ts</files>
  <acceptance_criteria>
    - `GET` or `POST` on `/api/assessment-session` creates or reuses the anonymous cookie session and returns the current canonical draft state.
    - Draft updates persist through `/api/assessment-session/draft` instead of browser-only storage calls.
    - `test/assessment/assessment-session-route.test.ts` covers bootstrap/load, update, version guard, and unauthenticated-anonymous cookie behavior.
  </acceptance_criteria>
  <action>Create `src/app/api/assessment-session/route.ts` for session bootstrap/load and `src/app/api/assessment-session/draft/route.ts` for incremental draft writes, both backed by the cookie helper and repository boundary from Plan 02. Extend `test/assessment/assessment-session-route.test.ts` first so the route contract proves session creation/reuse, canonical draft load, and validated draft updates before client hydration logic is attached.</action>
  <verify>
    <automated>npm exec vitest run test/assessment/assessment-session-route.test.ts && npm run typecheck</automated>
  </verify>
  <done>The assessment flow has route handlers that own anonymous session bootstrap and canonical draft updates before any client hydration/autosave wiring is added.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire route-backed hydration and mobile refresh recovery into the assessment UI</name>
  <read_first>
    @src/features/assessment/assessment-experience.tsx
    @src/features/assessment/assessment-flow.ts
    @src/features/assessment/types.ts
    @src/app/api/assessment-session/route.ts
    @src/app/api/assessment-session/draft/route.ts
    @test/assessment/mobile-flow.test.ts
    @test/e2e/mobile-assessment.spec.ts
  </read_first>
  <files>src/features/assessment/use-assessment-draft.ts, src/features/assessment/assessment-experience.tsx, test/assessment/mobile-flow.test.ts, test/e2e/mobile-assessment.spec.ts</files>
  <acceptance_criteria>
    - `src/features/assessment/use-assessment-draft.ts` fetches `/api/assessment-session` for hydration and `/api/assessment-session/draft` for autosave.
    - Refreshing a partially answered assessment restores answers and progress from the server-backed draft.
    - Any local cache, if introduced, is clearly secondary and not required for canonical recovery.
    - The Playwright mobile spec passes a `"restores draft after refresh"` scenario.
  </acceptance_criteria>
  <action>Update `src/features/assessment/use-assessment-draft.ts` and `src/features/assessment/assessment-experience.tsx` so hydration and autosave talk to the new route handlers instead of treating the browser as the source of truth. Keep recovery deterministic: continue at the first unanswered question or the final step when all answers are present. Extend `test/assessment/mobile-flow.test.ts` and `test/e2e/mobile-assessment.spec.ts` so FLOW-04 is proven in both integration coverage and a real mobile-browser refresh flow.</action>
  <verify>
    <automated>npm exec vitest run test/assessment/assessment-session-route.test.ts test/assessment/mobile-flow.test.ts && npm run typecheck && npx playwright test test/e2e/mobile-assessment.spec.ts -g "restores draft after refresh"</automated>
  </verify>
  <done>The assessment UI now resumes from the canonical anonymous server draft after refresh, with route, client, and real-browser coverage proving the behavior.</done>
</task>

</tasks>

<verification>
- Run `npm exec vitest run test/assessment/assessment-session-route.test.ts test/assessment/mobile-flow.test.ts`
- Run `npm run typecheck`
- Run `npx playwright test test/e2e/mobile-assessment.spec.ts -g "restores draft after refresh"`
- Confirm `src/features/assessment/use-assessment-draft.ts` fetches `/api/assessment-session`
- Confirm `src/features/assessment/assessment-experience.tsx` does not rely on browser-only storage for canonical recovery
</verification>

<success_criteria>
- FLOW-04 is satisfied by route-backed cookie + PostgreSQL draft recovery
- The client hydrates and autosaves through explicit server boundaries
- Mobile-browser verification proves refresh recovery in the real flow
</success_criteria>

<output>
After completion, create `.planning/phases/03-mobile-assessment-flow/03-03-SUMMARY.md`
</output>
