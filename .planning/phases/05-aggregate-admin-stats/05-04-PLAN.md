---
phase: 05-aggregate-admin-stats
plan: 04
type: execute
wave: 3
depends_on:
  - "05-03"
files_modified:
  - src/app/admin/page.tsx
  - src/app/admin/admin-stats-dashboard.tsx
  - test/admin/admin-page.test.tsx
  - test/e2e/admin-stats.spec.ts
  - test/e2e/public-result.spec.ts
autonomous: true
requirements:
  - STAT-01
  - STAT-02
  - STAT-03
  - STAT-04
  - STAT-05
  - STAT-06
must_haves:
  truths:
    - "An authenticated operator can open a protected admin screen and inspect aggregate daily, type, wing, and restart metrics."
    - "The admin screen renders hidden/suppressed cells safely instead of exposing raw low-volume counts."
    - "The end-to-end flow proves a shared-result restart event can be triggered in the browser and later observed from the protected admin surface."
  artifacts:
    - path: "src/app/admin/page.tsx"
      provides: "Protected admin stats page entry point"
    - path: "src/app/admin/admin-stats-dashboard.tsx"
      provides: "Dashboard rendering for daily series and suppressed distributions"
    - path: "test/admin/admin-page.test.tsx"
      provides: "Regression coverage for protected dashboard rendering"
    - path: "test/e2e/admin-stats.spec.ts"
      provides: "Browser proof for admin login and dashboard visibility"
    - path: "test/e2e/public-result.spec.ts"
      provides: "Browser proof that shared-result restart behavior feeds the admin metrics story"
  key_links:
    - from: "src/app/admin/page.tsx"
      to: "src/app/api/admin/stats/route.ts"
      via: "Protected page loads only the suppressed stats DTO contract"
      pattern: "api/admin/stats|getAdminStats"
    - from: "src/app/admin/admin-stats-dashboard.tsx"
      to: "src/domain/admin-stats/suppression.ts"
      via: "UI renders hidden states from DTO flags instead of calculating suppression"
      pattern: "hidden|suppressed"
    - from: "test/e2e/public-result.spec.ts"
      to: "test/e2e/admin-stats.spec.ts"
      via: "End-to-end verification spans restart event generation and admin visibility"
      pattern: "restart|admin stats"
---

<objective>
Ship the protected admin dashboard and full-story verification so Phase 05 ends with a usable, privacy-safe operator surface instead of only an API.

Purpose: The phase goal is a protected admin screen, not just data plumbing.
Output: Admin page and dashboard components, render/browser tests, and end-to-end proof for both protected access and restart-metric visibility.
</objective>

<execution_context>
@/home/ubuntu/Project/Enneagram/.codex/get-shit-done/workflows/execute-plan.md
@/home/ubuntu/Project/Enneagram/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-aggregate-admin-stats/05-RESEARCH.md
@.planning/phases/05-aggregate-admin-stats/05-VALIDATION.md
@src/app/results/[publicId]/public-result-restart-cta.tsx
@src/features/assessment/use-assessment-draft.ts
@test/e2e/public-result.spec.ts

<interfaces>
From src/features/assessment/use-assessment-draft.ts:
```ts
export function useAssessmentDraft()
```

The public anonymous flow remains unchanged. The admin page must observe its aggregate outputs through the protected stats API instead of reaching into draft state or public-result internals.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Lock the protected admin dashboard rendering contract</name>
  <read_first>
    @src/app/admin/login/page.tsx
    @src/app/api/admin/stats/route.ts
  </read_first>
  <files>test/admin/admin-page.test.tsx, test/e2e/admin-stats.spec.ts</files>
  <behavior>
    - Test 1: unauthenticated visits are redirected to `/admin/login`.
    - Test 2: authenticated visits render daily stats, type distribution, wing distribution, and restart metrics from suppressed DTOs.
    - Test 3: hidden buckets are shown with explicit privacy-safe copy rather than exact counts.
  </behavior>
  <action>Write render and browser coverage first for the final Phase 05 surface. The tests should prove the dashboard consumes the protected aggregate contract and that the browser can complete the admin login flow before reading stats.</action>
  <verify>
    <automated>npm exec vitest run test/admin/admin-page.test.tsx</automated>
  </verify>
  <done>The final operator surface and redirect behavior are explicit in tests before the UI is built.</done>
</task>

<task type="auto">
  <name>Task 2: Implement the admin stats dashboard and full end-to-end verification</name>
  <read_first>
    @test/admin/admin-page.test.tsx
    @test/e2e/admin-stats.spec.ts
    @test/e2e/public-result.spec.ts
  </read_first>
  <files>src/app/admin/page.tsx, src/app/admin/admin-stats-dashboard.tsx, test/admin/admin-page.test.tsx, test/e2e/admin-stats.spec.ts, test/e2e/public-result.spec.ts</files>
  <action>Implement the protected admin screen for STAT-01 through STAT-06. Render the already-suppressed DTOs from `/api/admin/stats` without recalculating privacy rules in the client. Keep the UI operational and low-risk: daily starts/completions/restarts plus primary-type and wing distribution sections are enough, with clear hidden-cell messaging when suppression applies. Extend Playwright so one scenario proves the protected login/dashboard path and another proves a shared-result restart event can be generated and then observed from the admin surface. Include `npm run build` in verification to catch App Router integration issues before phase sign-off.</action>
  <verify>
    <automated>npm exec vitest run test/admin/admin-page.test.tsx test/admin/admin-auth.test.ts test/admin/admin-stats-route.test.ts test/admin/admin-stats-repository.test.ts test/assessment/assessment-session-route.test.ts test/assessment/score-route.test.ts test/assessment/public-result.test.ts && npm run typecheck && npm run build && npx playwright test test/e2e/admin-stats.spec.ts test/e2e/public-result.spec.ts</automated>
  </verify>
  <done>The product now includes a usable protected admin stats page with browser/server proof for auth, aggregate metrics, restart tracking, and suppression behavior.</done>
</task>

</tasks>

<verification>
- Run `npm exec vitest run test/admin/admin-page.test.tsx test/admin/admin-auth.test.ts test/admin/admin-stats-route.test.ts test/admin/admin-stats-repository.test.ts test/assessment/assessment-session-route.test.ts test/assessment/score-route.test.ts test/assessment/public-result.test.ts`
- Run `npm run typecheck`
- Run `npm run build`
- Run `npx playwright test test/e2e/admin-stats.spec.ts test/e2e/public-result.spec.ts`
- Confirm the admin page never renders raw low-count cells
- Confirm the browser flow covers both protected admin access and restart-event generation
</verification>

<success_criteria>
- STAT-01 is satisfied by the protected admin dashboard entry flow
- STAT-02 through STAT-05 are visible on the admin page as aggregate-only metrics
- STAT-06 remains enforced in UI because the page only consumes already-suppressed DTOs
</success_criteria>

<output>
After completion, create `.planning/phases/05-aggregate-admin-stats/05-04-SUMMARY.md`
</output>
