---
phase: 05-aggregate-admin-stats
plan: 04
type: execute
wave: 4
depends_on:
  - "05-01"
  - "05-02"
  - "05-03"
files_modified:
  - src/app/admin/page.tsx
  - src/app/admin/login/*
  - test/admin/admin-dashboard.test.ts
  - test/e2e/admin-stats.spec.ts
autonomous: true
requirements:
  - STAT-01
  - STAT-02
  - STAT-03
  - STAT-04
  - STAT-05
  - STAT-06
must_haves:
  truths:
    - "Authenticated operators can read the aggregate dashboard in one protected screen."
    - "The dashboard communicates suppression clearly instead of silently omitting sensitive cells."
    - "Browser verification proves login protection and aggregate visibility end-to-end."
  artifacts:
    - path: "src/app/admin/page.tsx"
      provides: "Protected aggregate admin dashboard"
    - path: "test/admin/admin-dashboard.test.ts"
      provides: "Render coverage for stats sections and suppression messaging"
    - path: "test/e2e/admin-stats.spec.ts"
      provides: "End-to-end proof for login protection and dashboard visibility"
  key_links:
    - from: "src/app/admin/page.tsx"
      to: "src/db/repositories/admin-stats-repository.ts"
      via: "Protected admin page renders only privacy-suppressed aggregate DTOs"
      pattern: "getAdminStats|hidden"
---

<objective>
Deliver the protected aggregate admin dashboard and end-to-end verification.

Purpose: Phase 05 is only complete when an authenticated operator can actually reach and read the privacy-safe stats screen in the running app.
Output: `/admin` dashboard UI, suppression-aware presentation, and Playwright proof for login plus aggregate rendering.
</objective>

<execution_context>
@/home/ubuntu/Project/Enneagram/.codex/get-shit-done/workflows/execute-plan.md
@/home/ubuntu/Project/Enneagram/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-aggregate-admin-stats/05-RESEARCH.md
@.planning/phases/05-aggregate-admin-stats/05-VALIDATION.md
@src/domain/admin-auth/*
@src/domain/admin-stats/*
@src/db/repositories/admin-stats-repository.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Lock dashboard sections and suppression messaging in tests</name>
  <read_first>
    @src/domain/admin-stats/*
    @test/e2e/public-result.spec.ts
  </read_first>
  <files>test/admin/admin-dashboard.test.ts, test/e2e/admin-stats.spec.ts</files>
  <acceptance_criteria>
    - Tests define the protected dashboard sections for daily starts/completions/restarts and type/wing distributions.
    - Tests define visible suppression messaging for hidden cells.
    - Browser tests define the unauthenticated redirect and successful login path.
  </acceptance_criteria>
  <action>Write failing render and browser tests that lock the expected protected dashboard experience before implementing UI. Keep the scope intentionally operational, not exploratory: the page should read as an internal stats console, not a consumer feature surface.</action>
  <verify>
    <automated>npm exec vitest run test/admin/admin-dashboard.test.ts</automated>
  </verify>
  <done>The final operator experience is explicit and regression-guarded before UI implementation starts.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Implement the protected admin dashboard and browser verification</name>
  <read_first>
    @src/app/admin/layout.tsx
    @src/db/repositories/admin-stats-repository.ts
    @test/admin/admin-dashboard.test.ts
    @test/e2e/admin-stats.spec.ts
  </read_first>
  <files>src/app/admin/page.tsx, src/app/admin/login/*, test/admin/admin-dashboard.test.ts, test/e2e/admin-stats.spec.ts</files>
  <acceptance_criteria>
    - `/admin` renders the aggregate stats sections using the repository DTOs only.
    - Hidden/suppressed buckets are labeled clearly and do not leak raw counts through adjacent totals.
    - Playwright proves unauthenticated access is blocked and authenticated access reveals the dashboard.
    - Build and typecheck pass.
  </acceptance_criteria>
  <action>Implement a compact operator dashboard for Phase 05. Prefer server rendering with minimal client logic. The page should surface daily starts/completions/restarts plus type/wing distributions and clear suppression messaging, without drifting into raw analytics tooling or export flows.</action>
  <verify>
    <automated>npm exec vitest run test/admin/admin-dashboard.test.ts && npm run typecheck && npm run build && npx playwright test test/e2e/admin-stats.spec.ts</automated>
  </verify>
  <done>Phase 05 ships a protected, privacy-aware aggregate dashboard with end-to-end verification.</done>
</task>

</tasks>

<verification>
- Run `npm exec vitest run test/admin/admin-dashboard.test.ts`
- Run `npm run typecheck`
- Run `npm run build`
- Run `npx playwright test test/e2e/admin-stats.spec.ts`
- Confirm unauthenticated `/admin` access redirects to `/admin/login`
- Confirm suppression messaging appears when buckets are hidden
</verification>

<success_criteria>
- STAT-01..06 are delivered through one protected aggregate admin dashboard
- The dashboard remains aggregate-only and privacy-safe
</success_criteria>

<output>
After completion, create `.planning/phases/05-aggregate-admin-stats/05-04-SUMMARY.md`
</output>
