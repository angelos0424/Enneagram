---
phase: 05-aggregate-admin-stats
plan: 03
type: execute
wave: 3
depends_on:
  - "05-01"
  - "05-02"
files_modified:
  - src/domain/admin-stats/*
  - src/db/repositories/admin-stats-repository.ts
  - test/admin/admin-stats-repository.test.ts
  - test/admin/admin-dashboard.test.ts
autonomous: true
requirements:
  - STAT-02
  - STAT-03
  - STAT-04
  - STAT-05
  - STAT-06
must_haves:
  truths:
    - "Admin stats are aggregated and privacy-suppressed before the UI sees them."
    - "Starts, completions, restart clicks, primary-type distribution, and wing distribution all come from clearly owned server data sources."
    - "Small cells cannot leak through totals or raw DTOs."
  artifacts:
    - path: "src/domain/admin-stats"
      provides: "DTOs, suppression policy, and date-range/query contracts"
    - path: "src/db/repositories/admin-stats-repository.ts"
      provides: "Aggregate read model for dashboard data"
    - path: "test/admin/admin-stats-repository.test.ts"
      provides: "Regression coverage for grouped counts and suppression semantics"
  key_links:
    - from: "src/db/repositories/admin-stats-repository.ts"
      to: "src/db/schema.ts"
      via: "Starts and restart clicks read from append-only events, while completions and distributions read from stored results"
      pattern: "admin_stats_events|assessment_results"
    - from: "src/domain/admin-stats"
      to: "src/db/repositories/admin-stats-repository.ts"
      via: "Suppression policy is applied before dashboard DTOs leave the server layer"
      pattern: "threshold|hidden"
---

<objective>
Build the privacy-preserving aggregate read model for the admin dashboard.

Purpose: The dashboard cannot be trusted until every displayed metric is sourced, grouped, and suppressed consistently in one server data boundary.
Output: Repository/domain layer for daily starts/completions/restarts and type/wing distributions with centralized small-cell suppression.
</objective>

<execution_context>
@/home/ubuntu/Project/Enneagram/.codex/get-shit-done/workflows/execute-plan.md
@/home/ubuntu/Project/Enneagram/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-aggregate-admin-stats/05-RESEARCH.md
@.planning/phases/05-aggregate-admin-stats/05-VALIDATION.md
@src/db/schema.ts
@src/db/repositories/assessment-result-repository.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define the aggregate DTOs and privacy suppression rules in tests</name>
  <read_first>
    @.planning/phases/05-aggregate-admin-stats/05-RESEARCH.md
    @src/db/schema.ts
  </read_first>
  <files>src/domain/admin-stats/*, test/admin/admin-stats-repository.test.ts</files>
  <acceptance_criteria>
    - Tests cover daily starts, completions, and restart-click aggregation.
    - Tests cover primary-type and wing distribution aggregation.
    - Tests cover suppression of buckets under the configured floor and the omission/masking of totals when hidden cells would be derivable.
  </acceptance_criteria>
  <action>Define the admin stats DTOs and suppression policy from tests first. Make the hidden-cell behavior explicit before implementing SQL/repository logic, including the rule that the UI must never receive unsuppressed low-volume buckets.</action>
  <verify>
    <automated>npm exec vitest run test/admin/admin-stats-repository.test.ts</automated>
  </verify>
  <done>The privacy contract is concrete enough to constrain every later repository and UI decision.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Implement the aggregate repository and server-facing dashboard model</name>
  <read_first>
    @src/domain/admin-stats/*
    @test/admin/admin-stats-repository.test.ts
  </read_first>
  <files>src/domain/admin-stats/*, src/db/repositories/admin-stats-repository.ts, test/admin/admin-stats-repository.test.ts, test/admin/admin-dashboard.test.ts</files>
  <acceptance_criteria>
    - The repository returns daily starts, completions, restart clicks, primary-type distribution, and wing distribution from the intended sources.
    - Suppression is applied in the server data layer before any dashboard component or route consumes the results.
    - Typecheck passes after the shared DTOs are introduced.
  </acceptance_criteria>
  <action>Implement a single aggregate read boundary that joins the Phase 05 data sources into dashboard-ready DTOs. Use query-time grouping, not precomputed daily rollups. Keep time bucketing and suppression logic centralized so the UI stays a pure presenter.</action>
  <verify>
    <automated>npm exec vitest run test/admin/admin-stats-repository.test.ts test/admin/admin-dashboard.test.ts && npm run typecheck</automated>
  </verify>
  <done>The repo has a privacy-safe, dashboard-ready aggregate read model that later UI work can render directly.</done>
</task>

</tasks>

<verification>
- Run `npm exec vitest run test/admin/admin-stats-repository.test.ts test/admin/admin-dashboard.test.ts`
- Run `npm run typecheck`
- Confirm low-volume cells never leave the repository/domain layer as raw counts
</verification>

<success_criteria>
- STAT-02, STAT-03, STAT-04, and STAT-05 are computable from server-owned aggregate DTOs
- STAT-06 is enforced centrally before rendering
</success_criteria>

<output>
After completion, create `.planning/phases/05-aggregate-admin-stats/05-03-SUMMARY.md`
</output>
