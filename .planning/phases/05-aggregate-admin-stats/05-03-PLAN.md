---
phase: 05-aggregate-admin-stats
plan: 03
type: execute
wave: 2
depends_on:
  - "05-01"
  - "05-02"
files_modified:
  - src/domain/admin-stats/schema.ts
  - src/domain/admin-stats/suppression.ts
  - src/db/repositories/admin-stats-read-repository.ts
  - src/app/api/admin/stats/route.ts
  - test/admin/admin-stats-repository.test.ts
  - test/admin/admin-stats-route.test.ts
autonomous: true
requirements:
  - STAT-02
  - STAT-03
  - STAT-04
  - STAT-05
  - STAT-06
must_haves:
  truths:
    - "An authenticated admin can fetch date-based starts and completions without seeing raw low-volume cells."
    - "An authenticated admin can fetch primary-type and wing distributions as aggregates only."
    - "Restart-click metrics from shared-result traffic are included in the protected aggregate payload."
    - "Small-cell suppression is centralized before data reaches the UI layer."
  artifacts:
    - path: "src/domain/admin-stats/suppression.ts"
      provides: "Single privacy policy for hidden low-count cells"
    - path: "src/db/repositories/admin-stats-read-repository.ts"
      provides: "Aggregate read model for starts, completions, type, wing, and restart metrics"
    - path: "src/app/api/admin/stats/route.ts"
      provides: "Protected JSON boundary for suppressed admin stats DTOs"
    - path: "test/admin/admin-stats-repository.test.ts"
      provides: "Regression coverage for SQL grouping and suppression behavior"
    - path: "test/admin/admin-stats-route.test.ts"
      provides: "Regression coverage for protected stats API behavior"
  key_links:
    - from: "src/app/api/admin/stats/route.ts"
      to: "src/domain/admin-auth/session.ts"
      via: "Protected API requires the shared admin session helper before reading data"
      pattern: "requireAdminSession"
    - from: "src/db/repositories/admin-stats-read-repository.ts"
      to: "src/domain/admin-stats/suppression.ts"
      via: "Repository returns already-suppressed DTOs instead of raw counts"
      pattern: "suppress|hidden|threshold"
    - from: "src/db/repositories/admin-stats-read-repository.ts"
      to: "src/db/schema.ts"
      via: "Daily/grouped queries join event data and assessment_results distributions"
      pattern: "date_trunc|groupBy|assessmentResults|adminStatsEvents"
---

<objective>
Build the aggregate read model and protected stats API so the admin dashboard consumes one suppressed, privacy-safe data contract.

Purpose: Phase 05 only works if stats are aggregated and centrally suppressed before the UI sees them.
Output: Date-range/admin DTO schemas, shared suppression policy, aggregate repository, protected `/api/admin/stats` route, and route/repository regression coverage.
</objective>

<execution_context>
@/home/ubuntu/Project/Enneagram/.codex/get-shit-done/workflows/execute-plan.md
@/home/ubuntu/Project/Enneagram/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-aggregate-admin-stats/05-RESEARCH.md
@.planning/phases/05-aggregate-admin-stats/05-VALIDATION.md
@src/db/schema.ts
@src/db/repositories/assessment-result-repository.ts
@src/app/api/assessment-session/route.ts

<interfaces>
From src/db/repositories/assessment-result-repository.ts:
```ts
export interface AssessmentResultRepository {
  save(snapshot: AssessmentResultSnapshotDraft): Promise<AssessmentResultRecord>;
  findById(id: string): Promise<AssessmentResultRecord | null>;
  findByPublicId(publicId: string): Promise<AssessmentResultRecord | null>;
}
```

From src/db/schema.ts:
```ts
export type AssessmentResultRecord = typeof assessmentResults.$inferSelect;
```

The new stats read repository should follow the same repository boundary style, but it must return aggregate DTOs only, never raw result rows.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Lock the suppressed aggregate DTO contract in repository tests</name>
  <read_first>
    @src/db/repositories/assessment-result-repository.ts
    @.planning/phases/05-aggregate-admin-stats/05-RESEARCH.md
  </read_first>
  <files>test/admin/admin-stats-repository.test.ts</files>
  <behavior>
    - Test 1: daily series returns starts/completions/restarts grouped by day with hidden low-count cells per STAT-06.
    - Test 2: primary-type distribution returns visible buckets and a suppressed remainder path rather than leaking exact low counts.
    - Test 3: wing distribution follows the same shared suppression policy, not a duplicated widget-specific rule.
  </behavior>
  <action>Write repository-first regression coverage for STAT-02 through STAT-06. The tests should describe a single suppression threshold and prove that unsuppressed raw SQL rows do not escape the repository boundary.</action>
  <verify>
    <automated>npm exec vitest run test/admin/admin-stats-repository.test.ts</automated>
  </verify>
  <done>The aggregate DTO and suppression policy are pinned in tests before the protected API consumes them.</done>
</task>

<task type="auto">
  <name>Task 2: Implement the centralized suppression layer and protected admin stats API</name>
  <read_first>
    @test/admin/admin-stats-repository.test.ts
    @src/domain/admin-auth/session.ts
  </read_first>
  <files>src/domain/admin-stats/schema.ts, src/domain/admin-stats/suppression.ts, src/db/repositories/admin-stats-read-repository.ts, src/app/api/admin/stats/route.ts, test/admin/admin-stats-repository.test.ts, test/admin/admin-stats-route.test.ts</files>
  <action>Implement the aggregate read model for STAT-02, STAT-03, STAT-04, STAT-05, and STAT-06. Add a small shared domain module for date-range parsing and DTO types, implement suppression once in `src/domain/admin-stats/suppression.ts` with the research default threshold of `5`, and make the repository return already-suppressed daily and distribution payloads. Protect `/api/admin/stats` with `requireAdminSession()` from Plan 05-01, and ensure the route never returns raw low-count cells, raw event rows, or per-link identifiers.</action>
  <verify>
    <automated>npm exec vitest run test/admin/admin-stats-repository.test.ts test/admin/admin-stats-route.test.ts && npm run typecheck</automated>
  </verify>
  <done>The repo now exposes one protected JSON contract that combines daily starts/completions, type/wing distributions, and restart metrics with centralized suppression applied before serialization.</done>
</task>

</tasks>

<verification>
- Run `npm exec vitest run test/admin/admin-stats-repository.test.ts test/admin/admin-stats-route.test.ts`
- Run `npm run typecheck`
- Confirm `/api/admin/stats` rejects unauthenticated requests
- Confirm hidden cells are suppressed in the repository/API payload, not left for React to decide
</verification>

<success_criteria>
- STAT-02, STAT-03, STAT-04, and STAT-05 are satisfied by one protected aggregate API
- STAT-06 is satisfied by a centralized suppression layer before UI render
- The admin dashboard can consume a stable, already-suppressed DTO contract in the next wave
</success_criteria>

<output>
After completion, create `.planning/phases/05-aggregate-admin-stats/05-03-SUMMARY.md`
</output>
