---
phase: 05-aggregate-admin-stats
plan: 01
type: execute
wave: 1
depends_on:
  - "04-04"
files_modified:
  - src/env.ts
  - src/domain/admin-auth/*
  - src/app/admin/login/*
  - src/app/admin/layout.tsx
  - test/admin/admin-auth.test.ts
autonomous: true
requirements:
  - STAT-01
must_haves:
  truths:
    - "Only authenticated operators can reach `/admin` pages or admin data loaders."
    - "Admin auth is app-owned and testable in-repo, not delegated to external reverse-proxy assumptions."
    - "The admin boundary uses a server-only session primitive and does not introduce user accounts."
  artifacts:
    - path: "src/domain/admin-auth"
      provides: "Signed admin session issuance, verification, and cookie helpers"
    - path: "src/app/admin/login"
      provides: "Minimal operator login flow"
    - path: "src/app/admin/layout.tsx"
      provides: "Protected admin route boundary"
    - path: "test/admin/admin-auth.test.ts"
      provides: "Regression coverage for login, logout, and guard behavior"
  key_links:
    - from: "src/app/admin/layout.tsx"
      to: "src/domain/admin-auth"
      via: "Protected admin routes require a valid signed session before rendering"
      pattern: "requireAdminSession"
---

<objective>
Establish the minimal protected admin boundary for Phase 05.

Purpose: Every later stats artifact depends on a trustworthy operator-only entry point that is enforced in application code and verifiable in tests.
Output: Env-backed admin credential contract, signed admin session cookie helpers, login flow, and protected `/admin` routing.
</objective>

<execution_context>
@/home/ubuntu/Project/Enneagram/.codex/get-shit-done/workflows/execute-plan.md
@/home/ubuntu/Project/Enneagram/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-aggregate-admin-stats/05-RESEARCH.md
@.planning/phases/05-aggregate-admin-stats/05-VALIDATION.md
@src/env.ts
@src/app/layout.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define the admin auth contract and failing protection tests</name>
  <read_first>
    @src/env.ts
    @.planning/phases/05-aggregate-admin-stats/05-RESEARCH.md
  </read_first>
  <files>src/env.ts, src/domain/admin-auth/*, test/admin/admin-auth.test.ts</files>
  <acceptance_criteria>
    - Admin env requirements are explicit and validated.
    - Session signing and verification are encapsulated in a reusable domain boundary.
    - Tests fail until wrong-password, no-session, and valid-session cases are covered.
  </acceptance_criteria>
  <action>Start with the auth contract, not the page UI. Add failing tests that define the login payload, session cookie semantics, invalid-session rejection, and happy-path verification before implementing the domain helpers.</action>
  <verify>
    <automated>npm exec vitest run test/admin/admin-auth.test.ts</automated>
  </verify>
  <done>Admin auth behavior is explicit and regression-guarded before any protected page exists.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Implement `/admin/login` and protect the admin route tree</name>
  <read_first>
    @src/domain/admin-auth/*
    @src/app/layout.tsx
    @test/admin/admin-auth.test.ts
  </read_first>
  <files>src/app/admin/login/*, src/app/admin/layout.tsx, src/domain/admin-auth/*, test/admin/admin-auth.test.ts</files>
  <acceptance_criteria>
    - Operators can submit a minimal login form and receive a signed `HttpOnly` admin session cookie.
    - `/admin` routes redirect unauthenticated users to `/admin/login`.
    - Protected route loaders depend on the shared auth helper rather than ad hoc cookie parsing.
    - `npm run typecheck` passes.
  </acceptance_criteria>
  <action>Implement the smallest app-owned admin access boundary that can protect Phase 05 work: one login page, one cookie session helper set, one protected admin layout or equivalent route guard, and a logout path if needed for testability. Keep it server-only and do not add user tables.</action>
  <verify>
    <automated>npm exec vitest run test/admin/admin-auth.test.ts && npm run typecheck</automated>
  </verify>
  <done>The repo now has a stable, testable operator-only boundary for all admin stats work.</done>
</task>

</tasks>

<verification>
- Run `npm exec vitest run test/admin/admin-auth.test.ts`
- Run `npm run typecheck`
- Confirm unauthenticated `/admin` access redirects to `/admin/login`
- Confirm session issuance uses server-only cookie helpers, not client storage
</verification>

<success_criteria>
- STAT-01 is satisfied by an app-owned protected admin boundary
- Later plans can reuse a shared admin session helper rather than re-implementing authorization
</success_criteria>

<output>
After completion, create `.planning/phases/05-aggregate-admin-stats/05-01-SUMMARY.md`
</output>
