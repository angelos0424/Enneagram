---
phase: 05-aggregate-admin-stats
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/domain/admin-auth/session.ts
  - src/domain/admin-auth/schema.ts
  - src/app/api/admin/session/route.ts
  - src/app/admin/login/page.tsx
  - src/app/admin/layout.tsx
  - test/admin/admin-auth.test.ts
autonomous: true
requirements:
  - STAT-01
must_haves:
  truths:
    - "Only an authenticated admin can reach `/admin` or fetch `/api/admin/*` data."
    - "An operator can log in through a dedicated admin boundary without introducing end-user accounts."
    - "Invalid or missing admin credentials do not leak the stats dashboard."
  artifacts:
    - path: "src/domain/admin-auth/session.ts"
      provides: "Signed admin session cookie issuance, verification, and clearing helpers"
    - path: "src/app/api/admin/session/route.ts"
      provides: "Dedicated login/logout server boundary for the admin session"
    - path: "src/app/admin/layout.tsx"
      provides: "Server-side guard for protected admin routes"
    - path: "src/app/admin/login/page.tsx"
      provides: "Minimal admin login entry point"
    - path: "test/admin/admin-auth.test.ts"
      provides: "Regression coverage for session signing, invalid login, and route protection"
  key_links:
    - from: "src/app/admin/layout.tsx"
      to: "src/domain/admin-auth/session.ts"
      via: "Server guard rejects unauthenticated requests before rendering children"
      pattern: "requireAdminSession|redirect\\(\"/admin/login\"\\)"
    - from: "src/app/api/admin/session/route.ts"
      to: "src/domain/admin-auth/schema.ts"
      via: "Login route validates password input and env-backed credential contract"
      pattern: "adminLoginSchema|safeParse|parse"
---

<objective>
Create the protected admin auth boundary for Phase 05 so every later stats surface hangs off a single server-owned session contract.

Purpose: Satisfy STAT-01 first and keep the rest of the phase from building on an exposed dashboard.
Output: Signed admin session helpers, dedicated login/logout route, protected `/admin` layout, and auth regression coverage.
</objective>

<execution_context>
@/home/ubuntu/Project/Enneagram/.codex/get-shit-done/workflows/execute-plan.md
@/home/ubuntu/Project/Enneagram/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-aggregate-admin-stats/05-RESEARCH.md
@.planning/phases/05-aggregate-admin-stats/05-VALIDATION.md
@src/app/api/assessment-session/route.ts
@src/app/api/assessments/score/route.ts

<interfaces>
From src/app/api/assessment-session/route.ts:
```ts
export async function GET()
export async function POST(request: Request)
export async function DELETE()
```

From src/app/api/assessments/score/route.ts:
```ts
export async function POST(request: Request)
```

Use the same Next.js route-handler + cookie pattern for admin auth rather than inventing a separate server.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Lock the admin session contract and protected-route expectations</name>
  <read_first>
    @src/app/api/assessment-session/route.ts
    @.planning/phases/05-aggregate-admin-stats/05-RESEARCH.md
  </read_first>
  <files>test/admin/admin-auth.test.ts</files>
  <behavior>
    - Test 1: valid admin credentials issue an HttpOnly signed session cookie and allow `requireAdminSession()` to resolve.
    - Test 2: invalid credentials return a 401-style route result and do not set the cookie.
    - Test 3: missing or invalid session state redirects `/admin` requests to `/admin/login`.
  </behavior>
  <action>Write admin-auth regression coverage first for STAT-01. Mirror the repo's existing route-handler test style, but keep the contract minimal: one env-backed admin password, one signed cookie, no user accounts, no database table, no reuse of result `adminToken` values.</action>
  <verify>
    <automated>npm exec vitest run test/admin/admin-auth.test.ts</automated>
  </verify>
  <done>The Phase 05 auth boundary is described by failing or incomplete tests before implementation starts.</done>
</task>

<task type="auto">
  <name>Task 2: Implement the signed admin session boundary and `/admin/login` guard</name>
  <read_first>
    @test/admin/admin-auth.test.ts
    @.planning/phases/05-aggregate-admin-stats/05-RESEARCH.md
  </read_first>
  <files>src/domain/admin-auth/session.ts, src/domain/admin-auth/schema.ts, src/app/api/admin/session/route.ts, src/app/admin/login/page.tsx, src/app/admin/layout.tsx, test/admin/admin-auth.test.ts</files>
  <action>Implement the dedicated admin auth boundary for STAT-01 using `jose` per the research, not hand-rolled cookie signing. Validate the login payload and env contract in a small schema module, issue an `HttpOnly`, `sameSite=lax`, short-lived signed cookie from `/api/admin/session`, and add an `/admin/layout.tsx` guard that redirects unauthenticated requests to `/admin/login`. Keep this boundary separate from public anonymous flow cookies and from `assessment_results.adminToken`.</action>
  <verify>
    <automated>npm exec vitest run test/admin/admin-auth.test.ts && npm run typecheck</automated>
  </verify>
  <done>The repo now has a reusable `requireAdminSession()` boundary and a protected `/admin` shell that blocks unauthenticated access.</done>
</task>

</tasks>

<verification>
- Run `npm exec vitest run test/admin/admin-auth.test.ts`
- Run `npm run typecheck`
- Confirm `/admin/layout.tsx` and `/api/admin/session` share the same session helper
- Confirm the plan does not introduce end-user accounts or reuse result `adminToken`
</verification>

<success_criteria>
- STAT-01 is satisfied by a dedicated admin login route and server-side guard
- Missing or invalid admin session state redirects away from the protected dashboard
- Later Phase 05 plans can call a shared `requireAdminSession()` helper instead of re-implementing auth
</success_criteria>

<output>
After completion, create `.planning/phases/05-aggregate-admin-stats/05-01-SUMMARY.md`
</output>
