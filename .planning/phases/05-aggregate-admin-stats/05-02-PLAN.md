---
phase: 05-aggregate-admin-stats
plan: 02
type: execute
wave: 2
depends_on:
  - "05-01"
  - "04-04"
files_modified:
  - src/db/schema.ts
  - drizzle/*
  - src/db/repositories/admin-stats-event-repository.ts
  - src/app/api/assessment-session/route.ts
  - src/app/results/[publicId]/public-result-restart-cta.tsx
  - src/app/api/admin-stats/restart/route.ts
  - test/admin/admin-stats-events.test.ts
  - test/e2e/public-result.spec.ts
autonomous: true
requirements:
  - STAT-02
  - STAT-05
must_haves:
  truths:
    - "Assessment starts and shared-result restart clicks become durably measurable instead of inferred from transient state."
    - "Restart analytics are recorded at a dedicated successful server boundary, not a fragile client-side click handler."
    - "The current user flow remains intact while gaining analytics instrumentation."
  artifacts:
    - path: "src/db/schema.ts"
      provides: "Append-only event table contract for admin stats"
    - path: "src/db/repositories/admin-stats-event-repository.ts"
      provides: "Narrow write boundary for admin stats events"
    - path: "src/app/api/assessment-session/route.ts"
      provides: "Start-event recording when a fresh draft session is created"
    - path: "src/app/api/admin-stats/restart/route.ts"
      provides: "Dedicated shared-result restart boundary that both records and resets"
    - path: "test/admin/admin-stats-events.test.ts"
      provides: "Regression coverage for event recording semantics"
  key_links:
    - from: "src/app/api/assessment-session/route.ts"
      to: "src/db/repositories/admin-stats-event-repository.ts"
      via: "Fresh draft creation records `assessment_started` only when a new session is created"
      pattern: "assessment_started"
    - from: "src/app/api/admin-stats/restart/route.ts"
      to: "src/db/repositories/admin-stats-event-repository.ts"
      via: "Successful result-origin restarts record `assessment_restart_clicked` before redirecting home"
      pattern: "assessment_restart_clicked"
---

<objective>
Add the persistent event source Phase 05 needs for starts and restart clicks.

Purpose: Historical starts and shared-result restarts are not reconstructable from the current schema, so the admin dashboard needs an explicit durable event source before any aggregate UI can be trusted.
Output: Event table, write repository, fresh-start restart route, and regression coverage for event recording.
</objective>

<execution_context>
@/home/ubuntu/Project/Enneagram/.codex/get-shit-done/workflows/execute-plan.md
@/home/ubuntu/Project/Enneagram/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-aggregate-admin-stats/05-RESEARCH.md
@.planning/phases/05-aggregate-admin-stats/05-VALIDATION.md
@src/db/schema.ts
@src/app/api/assessment-session/route.ts
@src/app/results/[publicId]/public-result-restart-cta.tsx
@test/assessment/assessment-session-route.test.ts
@test/e2e/public-result.spec.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Lock the event schema and write boundaries in tests</name>
  <read_first>
    @src/db/schema.ts
    @test/assessment/assessment-session-route.test.ts
    @test/e2e/public-result.spec.ts
  </read_first>
  <files>src/db/schema.ts, test/admin/admin-stats-events.test.ts, test/e2e/public-result.spec.ts</files>
  <acceptance_criteria>
    - Tests define the append-only event table shape and accepted event types.
    - Tests make explicit that starts are recorded only on fresh draft creation.
    - Tests make explicit that restart clicks are recorded only through a dedicated shared-result restart boundary.
  </acceptance_criteria>
  <action>Add failing tests around the event table contract and the intended write semantics before touching routes. Do not instrument the generic delete path for every caller; define a separate result-origin restart boundary so Phase 05 measures the intended behavior only.</action>
  <verify>
    <automated>npm exec vitest run test/admin/admin-stats-events.test.ts</automated>
  </verify>
  <done>The measurement contract for starts and restart clicks is explicit and regression-guarded.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Implement event persistence and wire the server mutation boundaries</name>
  <read_first>
    @src/app/api/assessment-session/route.ts
    @src/app/results/[publicId]/public-result-restart-cta.tsx
    @test/admin/admin-stats-events.test.ts
  </read_first>
  <files>src/db/schema.ts, drizzle/*, src/db/repositories/admin-stats-event-repository.ts, src/app/api/assessment-session/route.ts, src/app/api/admin-stats/restart/route.ts, src/app/results/[publicId]/public-result-restart-cta.tsx, test/admin/admin-stats-events.test.ts, test/e2e/public-result.spec.ts</files>
  <acceptance_criteria>
    - The server records `assessment_started` only when a new draft session is created.
    - The public-result restart loop uses a dedicated server route that records `assessment_restart_clicked`, clears draft state, and returns the visitor to `/`.
    - Existing public-result restart behavior remains browser-proven after the route change.
    - Build and typecheck remain green.
  </acceptance_criteria>
  <action>Implement the event table, write repository, and route wiring. Keep the result-page CTA behavior user-equivalent, but move it onto a dedicated server-owned restart path so analytics do not depend on client navigation timing. Do not over-collect metadata.</action>
  <verify>
    <automated>npm exec vitest run test/admin/admin-stats-events.test.ts test/assessment/assessment-session-route.test.ts && npm run typecheck && npm run build && npx playwright test test/e2e/public-result.spec.ts</automated>
  </verify>
  <done>Phase 05 has a durable, privacy-scoped event source for starts and shared-result restart clicks.</done>
</task>

</tasks>

<verification>
- Run `npm exec vitest run test/admin/admin-stats-events.test.ts test/assessment/assessment-session-route.test.ts`
- Run `npm run typecheck`
- Run `npm run build`
- Run `npx playwright test test/e2e/public-result.spec.ts`
- Confirm restart-click recording lives on a dedicated result-origin server route
</verification>

<success_criteria>
- STAT-02 has a durable starts source
- STAT-05 has a durable restart-click source
- Existing assessment and public-result flows stay intact after instrumentation
</success_criteria>

<output>
After completion, create `.planning/phases/05-aggregate-admin-stats/05-02-SUMMARY.md`
</output>
