---
phase: 05-aggregate-admin-stats
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/db/schema.ts
  - drizzle/0003_phase5_admin_stats_events.sql
  - drizzle/meta/0003_snapshot.json
  - drizzle/meta/_journal.json
  - src/db/repositories/admin-stats-event-repository.ts
  - src/app/api/assessment-session/route.ts
  - src/app/api/assessments/score/route.ts
  - src/app/results/[publicId]/public-result-restart-cta.tsx
  - test/assessment/assessment-session-route.test.ts
  - test/assessment/score-route.test.ts
  - test/assessment/public-result.test.ts
autonomous: true
requirements:
  - STAT-02
  - STAT-05
must_haves:
  truths:
    - "The system records historical assessment starts even after draft rows are later deleted."
    - "The system records successful shared-result `검사해보기` restarts at the server mutation boundary."
    - "Completion timelines can be read without depending on client-only telemetry."
  artifacts:
    - path: "src/db/schema.ts"
      provides: "Append-only admin stats event table definition"
    - path: "src/db/repositories/admin-stats-event-repository.ts"
      provides: "Reusable event-write boundary for stats events"
    - path: "src/app/api/assessment-session/route.ts"
      provides: "Server-owned write path for start and restart analytics"
    - path: "src/app/api/assessments/score/route.ts"
      provides: "Completion write hook or completion timeline alignment"
    - path: "src/app/results/[publicId]/public-result-restart-cta.tsx"
      provides: "Explicit restart request that identifies the shared-result source surface"
    - path: "test/assessment/assessment-session-route.test.ts"
      provides: "Regression coverage for start and restart event persistence"
  key_links:
    - from: "src/app/api/assessment-session/route.ts"
      to: "src/db/repositories/admin-stats-event-repository.ts"
      via: "Fresh draft creation writes `assessment_started`, shared-result reset writes `assessment_restart_clicked`"
      pattern: "record.*assessment_started|record.*assessment_restart_clicked"
    - from: "src/app/results/[publicId]/public-result-restart-cta.tsx"
      to: "src/app/api/assessment-session/route.ts"
      via: "Restart CTA calls the explicit reset endpoint with a source-surface marker"
      pattern: "sourceSurface=public_result|public_result"
---

<objective>
Persist the append-only event source for Phase 05 so starts and shared-result restart clicks are durable before the aggregate dashboard is built.

Purpose: STAT-02 and STAT-05 cannot be reconstructed from the current draft lifecycle because draft rows are deleted and restart clicks are not stored at all.
Output: New event table and repository, route-level event writes for starts/restarts, and regression coverage for server-owned analytics capture.
</objective>

<execution_context>
@/home/ubuntu/Project/Enneagram/.codex/get-shit-done/workflows/execute-plan.md
@/home/ubuntu/Project/Enneagram/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-aggregate-admin-stats/05-RESEARCH.md
@.planning/phases/05-aggregate-admin-stats/05-VALIDATION.md
@src/db/schema.ts
@src/app/api/assessment-session/route.ts
@src/app/api/assessments/score/route.ts
@src/app/results/[publicId]/public-result-restart-cta.tsx
@test/assessment/assessment-session-route.test.ts
@test/assessment/score-route.test.ts

<interfaces>
From src/db/schema.ts:
```ts
export const assessmentResults = pgTable("assessment_results", { ... })
export const assessmentDraftSessions = pgTable("assessment_draft_sessions", { ... })
```

From src/app/api/assessment-session/route.ts:
```ts
export async function POST(request: Request)
export async function DELETE()
```

The new event source must attach to these existing server mutation boundaries rather than client telemetry.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Lock the admin-stats event schema and write-path expectations</name>
  <read_first>
    @src/db/schema.ts
    @src/app/api/assessment-session/route.ts
    @src/app/api/assessments/score/route.ts
  </read_first>
  <files>test/assessment/assessment-session-route.test.ts, test/assessment/score-route.test.ts</files>
  <behavior>
    - Test 1: creating a brand-new canonical draft session records an `assessment_started` event exactly once.
    - Test 2: reusing an existing canonical draft does not double-count a start event.
    - Test 3: successful score submission either records or cleanly exposes completion timing without relying on deleted draft rows.
  </behavior>
  <action>Extend route-level regression coverage first for STAT-02. Tests should prove event writes happen only after the server knows the action succeeded. Do not introduce client beacons, referrer capture, IPs, user agents, or per-link identifiers.</action>
  <verify>
    <automated>npm exec vitest run test/assessment/assessment-session-route.test.ts test/assessment/score-route.test.ts</automated>
  </verify>
  <done>Server-owned start/completion analytics expectations are explicit in tests before schema and repository work lands.</done>
</task>

<task type="auto">
  <name>Task 2: Implement append-only start and restart event persistence</name>
  <read_first>
    @test/assessment/assessment-session-route.test.ts
    @test/assessment/score-route.test.ts
    @src/app/results/[publicId]/public-result-restart-cta.tsx
  </read_first>
  <files>src/db/schema.ts, drizzle/0003_phase5_admin_stats_events.sql, drizzle/meta/0003_snapshot.json, drizzle/meta/_journal.json, src/db/repositories/admin-stats-event-repository.ts, src/app/api/assessment-session/route.ts, src/app/api/assessments/score/route.ts, src/app/results/[publicId]/public-result-restart-cta.tsx, test/assessment/assessment-session-route.test.ts, test/assessment/score-route.test.ts, test/assessment/public-result.test.ts</files>
  <action>Implement the append-only `admin_stats_events` boundary for STAT-02 and STAT-05. Add the Drizzle schema + migration, create a focused event repository, record `assessment_started` only when `POST /api/assessment-session` creates a new canonical draft, and record `assessment_restart_clicked` only when the shared-result restart path succeeds on the server. Update `public-result-restart-cta.tsx` to identify the `public_result` source surface when it invokes the existing reset boundary. Keep completion alignment simple per the research: either mirror a completion event on successful score save or explicitly document/use `assessment_results.created_at` so the later aggregate layer has one authoritative source.</action>
  <verify>
    <automated>npm exec vitest run test/assessment/assessment-session-route.test.ts test/assessment/score-route.test.ts test/assessment/public-result.test.ts && npm run typecheck</automated>
  </verify>
  <done>Historical starts and shared-result restart clicks are now durably persisted through server mutation boundaries with checked-in Drizzle artifacts.</done>
</task>

</tasks>

<verification>
- Run `npm exec vitest run test/assessment/assessment-session-route.test.ts test/assessment/score-route.test.ts test/assessment/public-result.test.ts`
- Run `npm run typecheck`
- Confirm the event table remains append-only and stores no IP, user-agent, referrer, or `publicId`
- Confirm start events are not double-counted for existing draft reuse
</verification>

<success_criteria>
- STAT-02 has a durable start timeline source independent of deleted draft rows
- STAT-05 has a durable restart-click event source tied to the shared-result surface
- Later aggregate queries can read starts/restarts from server-owned persistence rather than browser telemetry
</success_criteria>

<output>
After completion, create `.planning/phases/05-aggregate-admin-stats/05-02-SUMMARY.md`
</output>
